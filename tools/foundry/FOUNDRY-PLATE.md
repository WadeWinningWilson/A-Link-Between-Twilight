# FOUNDRY PLATE — open work, read by foundry_timer.py

<!-- The timer ticks while items are open and disarms when the plate is
     empty. "- [ ]" = open, "- [x]" = done. Keep rows ACTIONABLE — an item
     blocked on another lane or the user gets moved to the BLOCKED section
     (plain text, invisible to the timer) so the timer does not wake this
     lane to stare at someone else's dependency. That is the known gap
     integrator_timer.py documents at its tick 28; handled here by
     convention since day one rather than patched in later. -->

- [x] Fold the instrument estate into the trace-method doc (`ww-plugin-outset.md` §How ports land cites no tools/foundry instrument; next instance will re-derive the manual path)
- [x] Promote Decoder's ten-symptom type-signal bank + single-case-switch crack (rows 440/441) out of `aj1-progress.md` §Round 19 into standalone methods doctrine
- [x] Positive-control rule fold-in: every absence-based check in the estate needs a known-present probe (zero-bake magic-grep lesson, Housing/Engine's correction)
- [x] Fork-strip ledger lint (see `mods-src/ww_donor_disc/FORK-STRIP-LEDGER.md` §Lint hook): STRIPPED rows <-> dedicated commits, both directions
- [ ] Board pass each wake: rows naming FOUNDRY since last read; monitor census (pulse age AND pid, per false-ALIVE lesson); re-arm anything of mine that died
- [ ] AURORA-PATCH-LEDGER: build-verify step for aurora_patch_check.py (byte-verify != builds; user flagged the lwood candidate will likely need build refinement, not just extraction)
- [ ] AURORA hookability re-sweep: per standing directive, re-measure the ~4%/87%/9% split whenever the lwood candidate's source branch or the plugin's hook surface changes; goal is shrinking the patch-only share, not just reporting it
- [x] E3 kit retool (ROADMAP-E1-E6 analysis): replace mount-era rule 1 (dExtNpcMount model resolve → receiver getRes choke point), tree-shims rule 8 (→ plugin-local shims), restate TEV rule 4 plugin-side; fix stale MOD/SCRATCH paths in actor_kit.py
- [x] Kit plugin-target landing mode: --land-plugin emitting a registry.cpp above-enum row + trace-method scaffold instead of f_pc/files.cmake edits (era banner in actor_kit.py names this gap)
- [x] Deep era-retool of enemy_port_kit (mount rule era-split like actor_kit's) + port_kit K2 plugin-emit counterpart; space_kit bake-chain paths reviewed against zero-bake (banners are markers, not the retool)
- [x] E4 port_matrix: BUILD it (does not exist — zero hits repo-wide); join l_objectName → procname → owning TU → decomp status → placement count from existing census artifacts; queue generated, not chosen
- [x] E3 first-compile-rate instrument: per-batch success counter + failure-cause classifier (baseline recorded: batch 1 = 5/6 failing)

## BLOCKED (not timer-visible)

- pool_align.py re-run certification — waits on DECODER (row ~453): rerun on a measured TU, confirm numbers unchanged under my control-refactor
- Fork-strip retroactive mapping — waits on HOUSING/ENGINE + HISTORY naming fork paths (ledger rows UNMAPPED → MAPPED)
- Strip commits + milestone commit — wait on LWOOD landing (user hold)
