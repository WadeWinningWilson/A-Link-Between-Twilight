# Room bake report — Room0.arc -> C:\Users\xxxxx\AppData\Roaming\TwilitRealm\Dusklight\model_replacements\WW-Crew-Restoration\files\res\Stage\R_DL01\R00_00.arc (§267)

mode: DRY-RUN

- SCLS `sea` -> `F_DL01` (spawn=1 room=44) [SCLS] **[ROOM-MAP NEEDED: host hosts 2 donor stages — donor room 44 must be retargeted]**
- ACTR `KNOB00` DEFERRED (proc not in receiver l_objectName — stays CSV-side)
- ACTR `swood` DEFERRED (proc not in receiver l_objectName — stays CSV-side)
- ACTR `swood` DEFERRED (proc not in receiver l_objectName — stays CSV-side)
- ACTR `Lamp` DEFERRED (proc not in receiver l_objectName — stays CSV-side)
- ACTR `Ba1` BAKED
- ACTR `Ba1` BAKED
- ACTR `MPot` DEFERRED (proc not in receiver l_objectName — stays CSV-side)
- ACTR `MOsara` DEFERRED (proc not in receiver l_objectName — stays CSV-side)
- ACTR `MKoppu` DEFERRED (proc not in receiver l_objectName — stays CSV-side)
- ACTR `MKoppu` DEFERRED (proc not in receiver l_objectName — stays CSV-side)
- ACTR `Ba1` BAKED
- ACTR `Ba1` BAKED
- ACTR `SPitem` DEFERRED (proc not in receiver l_objectName — stays CSV-side)
- ACT7 `Ba1` BAKED
- SCOB `TagEv` BAKED
- SCOB `TagEv` BAKED
- SCOB `bonbori` BAKED
- SCOB `ky_tag1` BAKED

**10 baked · 0 already present (dedup) · 9 deferred (unported procs) · 0 skipped (unmapped SCLS dests).**

> DN-1 fence: placement/exit chunks only — no BG/collision written. Rerun after each actor port to shrink the deferred list (idempotent).
