# WW Bridge Tool — live state

**Tip (overwrite, don't append):** Bridge **§111 acceptance drives CLEAR** — tool **0.33.0**; `accept --ini …` external launcher + log assertions (scrubs `DUSK_DRIVE*`; no `src/` drive code). Pairs with §109 `verdict.ini`.

| Field | Value |
|-------|--------|
| **status** | accept harness shipped; loss-protection stands |
| **next** | Engine/History author verdict.ini; use `accept --launch`/`--watch` on ships |
| **do-not** | Put drive automation in `src/`; add remotes to mod/albt-bridge; push while Ivan gated |
| **tool_root** | `C:\Users\xxxxx\Documents\albt bridge` |
| **tool_version** | **0.33.0** |
| **decomp** | `D:\XXXXXXX\WW DP` (`WW_DECOMP`) |
| **ww_extract** | `D:\XXXXXXX\Ex WW` (`WW_EXTRACT`) |
| **schema_version** | **2** |
| **updated** | 2026-07-23 |

## Commands (recent)

| Cmd | Role |
|-----|------|
| `accept --ini …` | **Acceptance drive** (launch/watch + log assert) |
| `verdict --ini …` | Score a log once (no launch) |
| `nledger extract\|build` | №-ledger drafts + views |

## Open (not Bridge)

| Item | Owner |
|------|--------|
| Per-build `verdict.ini` + accept on ships | Engine / History |
| Ivan literal → gate CLEAN → push | Engine / user |
| Classify nledger drafts | History |

## Closed this lane

… · **§111 accept 0.33.0** · §110 loss-protection · verdict 0.32 · nledger 0.31 · …
