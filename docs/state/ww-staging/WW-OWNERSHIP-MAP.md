# WW OWNERSHIP MAP — the adjudicated authority

> **CUT 2026-08-16 from the completed worksheet (162/162 verdicts:
> user 12 · Housing 19 · History/Bridge 131). The data of record is
> [`ww-ownership-map.json`](ww-ownership-map.json); this file is a view.**
> This map REPLACES the four partitions as the WW-ownership authority —
> they survive only as evidence columns in the worksheet. Provenance
> (axis B) is NOT here: it lives in per-file `KIT-LINEAGE` headers,
> parsed by `kit_laws.lineage()`. **A file absent from this map is
> UNKNOWN and blocks — absence is not a pass (№31-C).**

## Categories

| category | files | meaning |
|---|---|---|
| `ww-port` | 129 | WW work; migrates with the plugin (provenance per KIT header) |
| `shared-per-hunk` | 18 | dual-purpose TU; WW hunks migrate, native hunks stay |
| `albw-skins` | 8 | ALBW skins track (TP's own WW-leftover assets; user ruling) |
| `albw` | 5 | ALBW mod work; never enters WW accounting |
| `instrument` | 1 | live probe in an otherwise-upstream file; strip-set held; retire probe + strip-set row together, then upstream-native |
| `fork-host-plumbing` | 1 | fork-authored WW-enabling infra; stays in fork, not WW debt (may host seam-seed symbols) |

## The unresolved residue, named


Everything else is decided. When the three above are split per-hunk,
this map has no pending entries.
