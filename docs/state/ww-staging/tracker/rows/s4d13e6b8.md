id:            s4d13e6b8
symbols:       daObjMshokki_c::break_proc, dPa_name::jpaName
tu:            src/d/actor/d_a_obj_mshokki.cpp
doorway:       EXISTS
destination:   PLUGIN
portable:      DECOMPILED
byte-true:     DIVERGENT
linked:        REGISTERED
verified:      MATCH
citations:
  - src/d/actor/d_a_obj_mshokki.cpp:231 particle_id[] uses the NATIVE port enum members (read 2026-08-17)
  - include/d/d_particle_name.h:39 ID_AK_JN_BREAKMAJUCUP00 = 0x013, PLATE00 = 0x014, POT00 = 0x015; header says "enum names made up based on debug strings" (read 2026-08-17)
  - src/d/d_particle_name.cpp:38 jpaName[] index 0x13 = "AK_JN_breakMajuCup00.jpa", 0x14 = "...Plate00.jpa", 0x15 = "...Pot00.jpa" (read 2026-08-17)
  - "D:/XXXXXXX/WW DP/include/d/d_particle_name.h:216 donor dPa_name::ID_AK_JN_BREAKMAJUPLATE00 = 0x043F, CUP00 = 0x0440, POT00 = 0x0441 (read 2026-08-17)"
review-verdict: |
  HISTORY/BRIDGE AUDIT 2026-08-17 — the §327 donor-values-convention audit I
  owed and had not run. ** VERDICT: THE CONVENTION IS CORRECT, AND IT IS NOW
  VERIFIED RATHER THAN ASSERTED. **

  THE QUESTION: §327's comment says "donor ids are dPa_name::-scoped; the port
  enum is global — Values are the NATIVE port members." Donor and receiver ids
  are numerically unrelated (donor PLATE/CUP/POT = 0x043F/0x0440/0x0441;
  receiver CUP/PLATE/POT = 0x013/0x014/0x015) AND differently ORDERED, so no
  offset maps one to the other. Using a same-named member is the §604 trap
  shape: the shared name may lie.

  WHY THAT LOOKED DANGEROUS: the receiver's enum carries its own warning —
  "enum names made up based on debug strings" — and its values are POSITIONAL,
  assigned by alphabetical rank rather than attested. A reconstructed name with
  a positional value is exactly the thing that can silently select a different
  effect.

  IT DOES NOT LIE HERE, AND THE PROOF IS THE FILENAME TABLE RATHER THAN THE
  NAME: dPa_name::jpaName[] resolves those very indices to real assets —
    0x13 -> "AK_JN_breakMajuCup00.jpa"
    0x14 -> "AK_JN_breakMajuPlate00.jpa"
    0x15 -> "AK_JN_breakMajuPot00.jpa"
  Each enum member lands on the .jpa file of the same name. The identity is
  attested by the receiver's own asset table, not by a decomp inference, and
  the ordering difference is explained: the receiver's table is alphabetical by
  filename, the donor's is its own order. Different tables, same effects.

  SO THE PORT FIRES THE ACTUAL breakMajuCup/Plate/Pot effects, and calling the
  receiver's particle system with the receiver's own id is the correct
  zero-bake shape — no donor id is smuggled across.

  THE ONE THING THIS DOES NOT PROVE, stated so it is not over-read: that the
  .jpa CONTENTS are identical between TP and WW. It proves same-named assets
  exist and are correctly selected. Given the ~450KB of byte-identical shared
  data between the two games that is very likely, but it is not what I measured.

  RECOMMENDATION (cosmetic, not a defect): §327's comment rests on member
  names. It would be stronger citing the jpaName[] resolution, since that is
  what actually makes the mapping safe — and a future reader hitting the
  "names made up" warning will otherwise re-open this audit.
created:       HISTORY/BRIDGE 2026-08-17 s327-audit
