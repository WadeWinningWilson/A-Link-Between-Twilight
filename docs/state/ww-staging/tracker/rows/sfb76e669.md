id:            sfb76e669
symbols:       dBootStage_add
tu:            src/d/d_ext_npc_mount.cpp
doorway:       ABSENT-unhookable
destination:   PATCH
portable:      ABSENT
linked:        NOT
provenance:    see-file
negative-control: |
  dBootStage_add is VANILLA'S OWN symbol and is NOT EXPORTED, so no plugin can
  reach the stage-registration path without a patch. Removal is observable as
  the Outset black screen - scene tears down, zero resources requested, no
  donor-disc serve after the warp.
citations:
  - src/d/d_ext_npc_mount.cpp:11927 registration entry point (read 2026-08-16)
  - dBootStage_add is not exported - see the S970 correction of S956's routing optimism (read 2026-08-16)
  - RE-VERIFIED POST-MERGE against the epoch-2 host at c880d46fb5: StageService (sdk/include/mods/svc/stage.h) exposes patch_actor / delete_actor / add_actor / remove_actor_edit, all keyed (stage, room, layer). It is ACTOR-level only - there is NO stage-registration entry point, so this verdict survives on a fresh measurement rather than by inheritance (read 2026-08-18)
created:       HISTORY/BRIDGE 2026-08-16 A3-seed
notes: |
  NARROWED 2026-08-18. This row used to carry TWO symbols under one verdict:
  dExtWwSave_registerWwStage (ours) and dBootStage_add (vanilla's). They have
  OPPOSITE verdicts and only one of them is a statement about the receiver.

  dExtWwSave_registerWwStage moved to s815faa6d with destination FORK — it is
  absent from vanilla because WE WROTE IT, which is not evidence of an
  unhookable doorway. This row keeps ONLY the vanilla symbol, whose
  unhookability is a real property of the receiver.

  WHAT SURVIVES THE EPOCH-2 MERGE: everything here. The merge added a
  StageService and the obvious worry was that it obsoleted this verdict. It
  does not — that service edits ACTORS within a stage and never registers a
  stage. Checked rather than assumed, because every doorway verdict in this
  tracker predates c880d46fb5 and a verdict measured against a host that has
  since moved is worth exactly one re-read.
