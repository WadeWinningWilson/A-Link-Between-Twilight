id:            s6a91c73f
symbols:       daObjHami2_c
tu:            src/d/actor/d_a_obj_hami2.cpp
doorway:       EXISTS
destination:   UPSTREAM
portable:      DECOMPILED
byte-true:     BYTE-TRUE
linked:        NOT
verified:      MATCH
citations:
  - "D:/XXXXXXX/WW DP/config/GZLE01/build.sha1 de364dda26847a39e8570d030efb587284e70c7e build/GZLE01/d_a_obj_hami2/d_a_obj_hami2.rel — manifest UNMODIFIED vs upstream (read 2026-08-17)"
  - "D:/XXXXXXX/WW DP/build/GZLE01/d_a_obj_hami2/d_a_obj_hami2.rel 5288 bytes, sha1 de364dda26847a39e8570d030efb587284e70c7e — hashed independently (run 2026-08-17)"
  - "D:/XXXXXXX/WW DP/configure.py ActorRel(Matching, \"d_a_obj_hami2\") — flipped from NonMatching (read 2026-08-17)"
  - docs/DECODER-BRIEFING.md Phase 5 'One merged PR before ko1 is attempted' — the gate this result now presses against (read 2026-08-17)
  - "D:/XXXXXXX/WW DP/build/{GZLJ01,GZLP01}/d_a_obj_hami2/d_a_obj_hami2.rel — CROSS-VERSION: matches each version's own build.sha1; both manifests git-clean; perturbation control passes on both artifacts (run 2026-08-17)"
review-verdict: |
  HISTORY/BRIDGE REVIEW 2026-08-17 — ** PASS. THIRD GATE-(A) CLEAN RESULT. **

  Verified as the previous two: claimed == manifest == artifact, all
  de364dda…, 5288 bytes, hashed independently; the expected-hash manifest is
  git-clean so the target is upstream's and not the reviewed lane's;
  perturbation control at four offsets, all diverged.

  RE-CERTIFICATION SWEEP IS NOW SELF-MAINTAINING. It reads every BYTE-TRUE row
  in this directory and recovers the TU and hash from the row's own citations,
  so certifying a TU enrols it automatically. A hardcoded list was the obvious
  build and it would have silently aged out the first time someone forgot to
  extend it. Both prior certifications re-verified clean under it.

  THE HEURISTIC TEST I ASKED FOR CAME BACK POSITIVE, ON A DELIBERATE TRIAL:
  hami3 (byte-matched sibling) supplied the 6-state event machine, the mono1
  joint callback and the MoveBG idioms; 21 of 25 functions hit 100% on FIRST
  COMPILE. That is 2-for-2 on pre-declared trials (sub2 18/18, hami2 21/25).
  I called it "one confirmed win, not a law" after sub2 and asked for a
  deliberate test rather than a retrospective one. This is that test and it
  passed. Two is a pattern, not yet a law — but it is now standing queue
  procedure and that is the right weight for the evidence.

  ** THE FINDING THAT MATTERS BEYOND THIS TU: THE PR DECISION IS NOW ON THE
  ESTATE'S CRITICAL PATH. ** Briefing Phase 5 reads "One merged PR before ko1
  is attempted", and ko1 is the charter's own target — the Outset NPC line the
  whole port needs. So the queue cannot reach its objective until a PR is
  authorised AND merged upstream. The lane is honouring the gate correctly and
  continuing on objects meanwhile; nothing is stalled today. But MERGE LATENCY
  IS OUTSIDE THIS ESTATE'S CONTROL, and the gate as written spends it before
  the NPC line can start.

  THE GATE HAS TWO SEPARABLE COMPONENTS AND ONLY ONE OF THEM IS SLOW:
    · MECHANICAL — does the work survive CI on all four versions
      (GZLE01/GZLP01/GZLJ01/D44J01)? Objective, fast, fully in our hands.
    · SOCIAL — do maintainers accept the naming and style, e.g. the invented
      Prm_e/mEventId/mState names flagged for rename? Real value CI cannot
      give, and unbounded in time.
  Splitting them — gate ko1 on CI-green rather than on merge — would preserve
  the mechanical proof and unblock the charter's target. NOT MY CALL: Phase 5
  is Foundry's briefing and the PR itself is the user's authorisation. Raised
  as an option with its trade-off named, not amended.
created:       HISTORY/BRIDGE 2026-08-17 decoder-review

cross-version-2026-08-17: |
  THREE-VERSION PROOF, verified independently by History/Bridge after the
  user supplied the GZLJ01/GZLP01 disc trees. This TU now matches its OWN
  expected hash on GZLE01, GZLJ01 and GZLP01 — three separate manifests, each
  confirmed git-clean so no target was authored by the lane under review, and
  a perturbation control passing on every artifact.

  SCOPE, STATED EXACTLY: 3 TUs x 4 versions = 12 combinations. NINE are built
  and matching. THREE are UNBUILDABLE — D44J01 has 65 files and no
  files/RELS.arc, and dtk dol split refuses on precisely that.

  SO THE HONEST STATUS IS MatchingFor("GZLE01","GZLJ01","GZLP01"), NOT bare
  `Matching`. Under DECODER-BRIEFING 2b, bare `Matching` asserts byte-identity
  on ALL versions, and the demo cannot be checked. This is not a placeholder:
  it is the correct terminal label unless the D44J01 extraction is completed.

twelve-of-twelve-2026-08-17: |
  ** CONDITION MET. THE MatchingFor NOTE ABOVE IS NOW SUPERSEDED. **
  I wrote that MatchingFor("GZLE01","GZLJ01","GZLP01") was the correct terminal
  label "unless the D44J01 extraction is completed". The user completed it
  (orig/D44J01 went 65 files -> 2632 with files/RELS.arc), the demo build ran,
  and I verified it the same way as the other nine:
    · D44J01 artifact d_a_obj_hami2 (cf947424afb7...) matches config/D44J01/build.sha1
    · that manifest is git-clean, so the target is upstream's not the lane's
    · perturbation control at four offsets passes on the artifact
  ALL FOUR manifests unmodified; ALL TWELVE combinations (3 TUs x 4 versions)
  built and matching.

  SO BARE `Matching` IS NOW THE EVIDENCE-COMPLETE LABEL under DECODER-BRIEFING
  2b, and configure.py's existing `ActorRel(Matching, ...)` line is honest
  exactly as it stands — no relabel needed. The over-claim I raised this
  morning was real when I raised it and has been closed by measurement rather
  than by argument.
