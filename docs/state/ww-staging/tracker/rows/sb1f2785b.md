id:            sb1f2785b
symbols:       JMessage::TControl, JMessage::TResource, JMessage::TResourceContainer, JMessage::TSequenceProcessor, JMessage::TRenderingProcessor
tu:            src/JSystem/JMessage/processor.cpp
doorway:       ABSENT-hookable
destination:   PLUGIN
portable:      NONMATCHING
linked:        NOT
provenance:    see-file
negative-control: |
  W1 is load-bearing rather than assumed: dMesg_tControl EXTENDS JMessage::TControl
  at 0x3C, so removing the engine does not degrade rendering - tag interpretation,
  branching and message-code resolution have no other implementation and nothing
  draws at all. Observable as no textbox, not as a mis-drawn one.
citations:
  - D:/XXXXXXX/WW DP/configure.py Object(Matching, JSystem/JMessage/control.cpp) (read 2026-08-16)
  - D:/XXXXXXX/WW DP/configure.py Object(Matching, JSystem/JMessage/data.cpp) (read 2026-08-16)
  - D:/XXXXXXX/WW DP/configure.py Object(NonMatching, JSystem/JMessage/processor.cpp) (read 2026-08-16)
  - D:/XXXXXXX/WW DP/configure.py Object(NonMatching, JSystem/JMessage/resource.cpp) (read 2026-08-16)
  - docs/state/ww-message-donor-spec.md 7 W1 dMesg_tControl extends TControl at 0x3C (read 2026-08-16)
notes: |
  THE WIRE IS SPLIT AND THE ROW CARRIES THE PESSIMISTIC FLOOR. PORTABLE is
  NONMATCHING because the wire AS A WHOLE cannot be taken verbatim - but 2 of its
  4 TUs are Matching and MUST NOT BE RECONSTRUCTED:
    CITABLE VERBATIM : control.cpp, data.cpp
    RECONSTRUCTION   : processor.cpp, resource.cpp
  Recorded this way because the schema PORTABLE domain has no SPLIT value and
  the floor rule (SPEC-row-store P4) requires the pessimistic bucket, never a
  silent one. The enumeration is the part that keeps the floor from costing work.

  THE DIALECT DIVERGENCE - THE REASON THIS WIRE MUST BE PORTED IN PARALLEL RATHER
  THAN SATISFIED BY THE RECEIVER'S COPY, MEASURED NOT ASSUMED:
    DONOR    processor.cpp dispatches case 0x00 0x01 0x02 0x03 0x05 0x06 0x1A
             0xF5-0xFF 0xFFFF
    RECEIVER processor.cpp dispatches 0xF5-0xFF only, and contains NO 0x1A
             handling anywhere - nor in d_msg_class / d_msg_object / d_msg_flow,
             so it is ABSENT rather than relocated.
  0x1A IS WW'S TAG ESCAPE BYTE. Donor TProcessor::on_tag_() builds the 24-bit tag
  from current[2..4] with payload at current[5], confirming the spec wire format
  0x1A <len> <group> <code:u16BE> from donor source.

  So running WW text through the receiver's processor is not a stylistic bridge -
  it cannot see WW's tags at all. Today d_ext_dmesg.cpp bridges it by hand-mapping
  three tag families and emitting the receiver's 0x1B escapes; every OTHER WW tag
  silently emits nothing. An incomplete transcoder fails INVISIBLY, which is why
  "it renders" was never evidence it was correct.

  NOTE ON THE DECOMP'S OWN SEMANTICS: configure.py line 364 reads
  "NonMatching = False  # Object does not match and should not be linked" - a
  NonMatching TU is not merely imperfect, it is absent from the decomp's own link.
created:       HISTORY/BRIDGE 2026-08-16 message-build W1
