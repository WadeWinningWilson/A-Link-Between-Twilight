id:            s2f8ac419
symbols:       daObjMsdan2_c::Mthd_Create, daObjMsdan2_c::Mthd_Execute, daObjMsdan2_c::Mthd_Delete
tu:            src/d/actor/d_a_obj_msdan2.cpp
doorway:       EXISTS
destination:   UPSTREAM
portable:      DECOMPILED
byte-true:     BYTE-TRUE
linked:        NOT
verified:      MATCH
citations:
  - "D:/XXXXXXX/WW DP/config/GZLE01/build.sha1 416e1c4b893d5986f42b67246700df8266f1407d build/GZLE01/d_a_obj_msdan2/d_a_obj_msdan2.rel — manifest UNMODIFIED vs upstream, git status clean (read 2026-08-17)"
  - "D:/XXXXXXX/WW DP/build/GZLE01/d_a_obj_msdan2/d_a_obj_msdan2.rel 1744 bytes, sha1 416e1c4b893d5986f42b67246700df8266f1407d — hashed independently (run 2026-08-17)"
  - "D:/XXXXXXX/WW DP/configure.py:1772 ActorRel(Matching, \"d_a_obj_msdan2\") — status flipped from NonMatching (read 2026-08-17)"
  - docs/DECODER-BRIEFING.md 2b six-value status vocabulary; Matching is the goal state (read 2026-08-17)
  - docs/state/ww-staging/CALLS.md DECODER (A)-clean row with invocation and per-symbol proof (read 2026-08-17)
  - "D:/XXXXXXX/WW DP/build/{GZLJ01,GZLP01}/d_a_obj_msdan2/d_a_obj_msdan2.rel — CROSS-VERSION: matches each version's own build.sha1; both manifests git-clean; perturbation control passes on both artifacts (run 2026-08-17)"
review-verdict: |
  HISTORY/BRIDGE REVIEW 2026-08-17 — ** PASS. THIS IS THE CAMPAIGN'S FIRST
  GATE-(A) CLEAN RESULT AND THE FIRST BYTE-TRUE ROW IN THIS STORE. **

  VERIFIED INDEPENDENTLY, NOT TRANSCRIBED:
    · the built .rel is 1744 bytes and hashes to
      416e1c4b893d5986f42b67246700df8266f1407d — I hashed the artifact myself
    · config/GZLE01/build.sha1 carries that exact hash for that exact path

  ** THE CHECK THAT ACTUALLY MATTERS, because the other two are circular
  without it: IS THE EXPECTED-HASH MANIFEST ITSELF UNTOUCHED? ** A hash that
  matches a manifest someone edited proves nothing. `git status --porcelain
  config/GZLE01/build.sha1` is EMPTY — the target comes from upstream, not from
  the lane being reviewed. The comparison is genuinely independent.

  AND THE WORKING TREE IS MINIMAL AND CORRECT: exactly three modified files —
  configure.py, the TU's header, the TU's .cpp. No build-system edits, no
  manifest edits, nothing unrelated. That is what a legitimate one-TU flip
  looks like and it is what an illegitimate one does not.

  MY OWN METHOD WAS CONTROLLED: I flipped one byte at four offsets (0x000000,
  0x000040, 0x000368, 0x0006CF) and every perturbation diverged. The check can
  detect a difference, so its agreement is evidence rather than an artifact of
  a broken comparison.

  ONE HONEST CAVEAT, WHICH THE LANE RAISED ITSELF AND WHICH I AFFIRM: the
  header gained INVENTED names (Prm_e, mEventId@0x298, mState@0x29C). Invented
  names do not affect codegen, so BYTE-TRUE is unharmed — but they are not
  donor-attested and must be renamed at PR time, exactly as flagged. Declaring
  them rather than letting them pass as donor vocabulary is the right call.

  NOT MINE TO GRANT: the upstream PR. Submission is outward-facing and waits on
  the user's explicit authorization. The lane flagged it rather than parking it
  silently, which is the correct handling; it is escalated, not approved.
created:       HISTORY/BRIDGE 2026-08-17 decoder-review

cross-version-2026-08-17: |
  THREE-VERSION PROOF, verified independently by History/Bridge after the
  user supplied the GZLJ01/GZLP01 disc trees. This TU now matches its OWN
  expected hash on GZLE01, GZLJ01 and GZLP01 — three separate manifests, each
  confirmed git-clean so no target was authored by the lane under review, and
  a perturbation control passing on every artifact.

  SCOPE, STATED EXACTLY: 3 TUs x 4 versions = 12 combinations. NINE are built
  and matching. THREE are UNBUILDABLE — D44J01 has 65 files and no
  files/RELS.arc, and dtk dol split refuses on precisely that.

  SO THE HONEST STATUS IS MatchingFor("GZLE01","GZLJ01","GZLP01"), NOT bare
  `Matching`. Under DECODER-BRIEFING 2b, bare `Matching` asserts byte-identity
  on ALL versions, and the demo cannot be checked. This is not a placeholder:
  it is the correct terminal label unless the D44J01 extraction is completed.

twelve-of-twelve-2026-08-17: |
  ** CONDITION MET. THE MatchingFor NOTE ABOVE IS NOW SUPERSEDED. **
  I wrote that MatchingFor("GZLE01","GZLJ01","GZLP01") was the correct terminal
  label "unless the D44J01 extraction is completed". The user completed it
  (orig/D44J01 went 65 files -> 2632 with files/RELS.arc), the demo build ran,
  and I verified it the same way as the other nine:
    · D44J01 artifact d_a_obj_msdan2 (c343a7d7dad2...) matches config/D44J01/build.sha1
    · that manifest is git-clean, so the target is upstream's not the lane's
    · perturbation control at four offsets passes on the artifact
  ALL FOUR manifests unmodified; ALL TWELVE combinations (3 TUs x 4 versions)
  built and matching.

  SO BARE `Matching` IS NOW THE EVIDENCE-COMPLETE LABEL under DECODER-BRIEFING
  2b, and configure.py's existing `ActorRel(Matching, ...)` line is honest
  exactly as it stands — no relabel needed. The over-claim I raised this
  morning was real when I raised it and has been closed by measurement rather
  than by argument.
