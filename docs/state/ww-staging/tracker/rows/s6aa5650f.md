id:            s6aa5650f
symbols:       daDemo00_c, g_profile_DEMO00
tu:            src/d/actor/d_a_demo00.cpp
doorway:       EXISTS
destination:   SPLIT
portable:      UNKNOWN
linked:        NOT
provenance:    see-file
citations:
  - src/d/actor/d_a_demo00.cpp KIT-LINEAGE mixed, KIT-DONOR per-hunk, KIT-DONOR-REF zeldaret/tww@1d57f04 (read 2026-08-17)
  - docs/NEVER-PUSH-STRIP-SET.md d_a_demo00 LISTED under the UNION basis (read 2026-08-17)
  - docs/state/ww-staging/tracker/rows/ absent - 10 of 11 KIT-LINEAGE mixed TUs carry a row, this was the 11th (read 2026-08-17)
  - docs/state/ww-staging/tracker/rows/ destination precedent for KIT-LINEAGE mixed: 6 SPLIT / 4 PLUGIN; sibling d_demo.cpp SPLIT (read 2026-08-17)
notes: |
  THE ONLY MEASURED GAP IN THE PATCHER'S INPUT SET. Found by cross-checking the
  31 TUs that declare a donor source under a receiver-native filename against
  `tracker/rows/`: 30 of 31 already carried a row. This was the one that did not.

  IT IS AN OMISSION, NOT A CATEGORY. Checked before filing, because a missing row
  for a whole CLASS of file would mean the tracker handles that class elsewhere
  and a new row would be noise: of the 11 TUs declaring `KIT-LINEAGE: mixed`,
  10 carry a row. This is the 11th. Nothing about `mixed` exempts a TU.

  `destination: SPLIT` IS ADJUDICATED FROM PRECEDENT, NOT GUESSED, AND IS
  OVERTURNABLE BY THE OWNING LANE. The schema rightly refuses a blank, so leaving
  it empty would have left the tracker at 121 of 122 - the exact incompleteness
  this row exists to close. The evidence: of the 11 `mixed` TUs, 6 are SPLIT and
  4 PLUGIN, and the nearest siblings are decisive - `d_demo.cpp`, THE SAME
  SUBSYSTEM, is SPLIT, as are `d_a_bg`, `d_a_vrbox`, `d_a_vrbox2`, `d_a_swhit0`.
  SPLIT also means literally what `KIT-DONOR: per-hunk` describes: the TU divides
  across destinations rather than belonging to one. If the owning lane reads the
  hunks and rules PLUGIN, overturn this - the row is the record, not the verdict.

  WHY IT MATTERS FOR THE PATCHER: 2,103 lines at `KIT-DONOR: per-hunk` means the
  donor/receiver boundary runs THROUGH this file rather than around it, so the
  seam cannot be settled by classifying the TU as a whole. Per-hunk accounting is
  what the row will need, and that is exactly why it should not be bulk-filled.

  NOT A PUSH-GATE ISSUE. The strip set already LISTS this file under its UNION
  basis (filename rules union declared lineage). The gap is in the SEAM TRACKER,
  which feeds the patcher - a different instrument with a different job. My
  earlier row claiming the push gate was blind to 31 files is RETRACTED and void.
created:       FOUNDRY 2026-08-17 patcher-input-completeness
