id:            s2b6b1a01
symbols:       JUTFont, JUTResFont, JUTCacheFont, JUTFont::drawString_size_scale
tu:            src/JSystem/JUtility/JUTFontData_Ascfont_fix12.cpp
doorway:       ABSENT-hookable
destination:   PLUGIN
portable:      DECOMPILED
linked:        NOT
provenance:    see-file
negative-control: |
  The TWO-FONT claim is falsifiable rather than decorative: drop mRubyFont (0x40)
  and keep mMainFont (0x3C) and the box still renders every ordinary line - only
  furigana silently vanish. That is why a one-font port passes a casual read and
  is still wrong; the failure is invisible on any message without ruby text.
citations:
  - D:/XXXXXXX/WW DP/configure.py:1018 Object(Matching, JSystem/JUtility/JUTFont.cpp) (read 2026-08-16)
  - D:/XXXXXXX/WW DP/configure.py:1019 Object(Matching, JSystem/JUtility/JUTResFont.cpp) (read 2026-08-16)
  - D:/XXXXXXX/WW DP/configure.py:1012 Object(Matching, JSystem/JUtility/JUTCacheFont.cpp) (read 2026-08-16)
  - D:/XXXXXXX/WW DP/configure.py:1032 Object(Matching, JUTFontData_Ascfont_fix12.cpp) originally a .s file (read 2026-08-16)
  - D:/XXXXXXX/WW DP/src/JSystem/JUtility/JUTFont.cpp:1-59 full body, dtk address ranges 802C1DA4-802C2044 (read 2026-08-16)
  - docs/state/ww-message-donor-spec.md 7 W2 mMainFont 0x3C / mRubyFont 0x40 (read 2026-08-16)
notes: |
  ENTIRE FAMILY IS Matching - 4 of 4, so the donor TUs are citable verbatim.

  CORRECTED SAME DAY: this row first read "THIS WIRE NEEDS NO RECONSTRUCTION AT
  ALL" because the receiver has JUTFont in libs/JSystem/src/JUtility/. THAT IS A
  SEAM TO LABEL, NOT A REASON TO SKIP THE PORT. The receiver having a working
  subsystem never made the ja1Voice audio redirect anything but a bridge, and the
  same rule binds here: port the donor's JUTFont IN PARALLEL, additively.

  WHAT THE BRIDGE LOOKS LIKE TODAY: d_ext_dmesg.cpp:167 constructs the RECEIVER's
  JUTResFont over a DONOR .bfn (rock_24_20_4i_usa.bfn / hyrule.bfn). Donor data,
  receiver subsystem - the bridge shape, and it is on the owed list.

  WHY THIS ROW EXISTS AT ALL RATHER THAN BEING SKIPPED AS "DONE": the base TU is
  59 lines and scores zero on every non-matching pattern, which is ALSO the exact
  signature of a stub. It was read in full before this verdict was written - real
  bodies, dtk address ranges - because "0 markers" and "empty file" are the same
  number. The family, not the base class, is the wire: JUTFont.cpp alone is an
  abstract base with 5 functions and no glyph source.
created:       HISTORY/BRIDGE 2026-08-16 message-build W2
