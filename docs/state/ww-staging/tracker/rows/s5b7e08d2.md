id:            s5b7e08d2
symbols:       daObjMsdan_c::Mthd_Create, daObjMsdan_c::Mthd_Execute, daObjMsdan_c::Mthd_Delete
tu:            src/d/actor/d_a_obj_msdan.cpp
doorway:       EXISTS
destination:   UPSTREAM
portable:      DECOMPILED
byte-true:     EQUIVALENT
linked:        NOT
verified:      UNKNOWN
citations:
  - "D:/XXXXXXX/WW DP/configure.py ActorRel(NonMatching, \"d_a_obj_msdan\") — upstream status unchanged, as the lane stated (read 2026-08-17)"
  - "D:/XXXXXXX/WW DP/configure.py ActorRel(Matching, \"d_a_obj_msdan_sub\") — a BYTE-MATCHED sibling in the same family (read 2026-08-17)"
  - "D:/XXXXXXX/WW DP/build/GZLE01/d_a_obj_msdan2/d_a_obj_msdan2.rel sha1 416e1c4b893d5986f42b67246700df8266f1407d — re-hashed AFTER the msdan work, certification s2f8ac419 still valid (run 2026-08-17)"
  - docs/state/ww-staging/CALLS.md DECODER msdan row, 99.95% fuzzy, 4-row add-association residue (read 2026-08-17)
review-verdict: |
  HISTORY/BRIDGE REVIEW 2026-08-17 — ACCEPTED as EQUIVALENT, and the residue's
  equivalence is PROVEN, not argued. This is the strongest REASONING of any
  Equivalent case in the store.

  THE RESIDUE: the target associates (swSave + size<<16) + i*0x100; ours
  associates (swSave + i*0x100) + size<<16. The lane called this "associativity
  of u32 +". That is right, and I closed the one gap the phrasing leaves:
  associativity of SIGNED int addition is only guaranteed absent overflow, so I
  bounded the operands. Worst case is (0xFF<<16) + 0xFF + 31*0x100 = 0xFF1FFF
  = 16,719,871 against INT_MAX 2,147,483,647 — OVERFLOW IS IMPOSSIBLE ON THIS
  DOMAIN, so the signed/unsigned question never arises. Then exhaustively over
  the real domain (size 0..255, swSave in {0,1,0x7F,0xFF}, i 0..31): ZERO
  groupings disagree. The equivalence is UNCONDITIONAL.

  EQUIVALENCE-STRENGTH RANKING, now that the store holds four of these. They
  must not be read as equal-confidence just because three share a label:
    1. d_a_obj_msdan2 (s2f8ac419)  BYTE-TRUE   — hash verified, manifest clean
    2. d_a_obj_msdan  (this row)   EQUIVALENT  — PROVEN by algebra + exhaustion
    3. d_a_msw        (s3e5f21a7)  EQUIVALENT  — PROVEN by upstream's own
                                    MatchingFor("D44J01") byte-match
    4. d_a_lod_bg     (s7c1a0bd3)  EQUIVALENT  — ASSERTED from a dead-register
                                    claim I declined to verify
  2 and 3 are proofs of different kinds — one algebraic, one empirical. 4 is
  not a proof at all and should not be cited as though it were.

  MY OWN EARLIER CERTIFICATION RE-CHECKED, because the tree moved under it:
  d_a_obj_msdan2.rel still hashes to 416e1c4b... after the msdan work. Row
  s2f8ac419 stands. A certification is only as good as its last re-measure.

  TREE SCOPE VERIFIED MINIMAL: five modified files — configure.py plus the two
  TUs' headers and sources. Nothing unrelated.

  NOT VERIFIED BY ME: the 99.95% fuzzy and 8/9-at-100% figures. Those are
  objdiff measurements I did not re-run; I am recording them as the lane's,
  not adopting them as mine.

  LEAD THE LANE HAS NOT USED: d_a_obj_msdan_sub is ALREADY Matching upstream.
  Their next target (msdan_sub2) therefore has a BYTE-MATCHED SIBLING in the
  same family to read idioms from — the sibling-template route the briefing
  names, available here without any decoding cost.
created:       HISTORY/BRIDGE 2026-08-17 decoder-review
