# Donor pre-cast census (kit_laws --donor-precast, tale §817)

Every `(J3D*)dComIfG_get*Res` site in donor actor source. TRANSLATE =
J3DModelData, must route through the acquirer family at port time (DN-3);
anm types are mount-parsed receiver-side (§816) and keep the donor idiom.

- **d_a_kanban.cpp**: :1021 J3DModelData [TRANSLATE]
