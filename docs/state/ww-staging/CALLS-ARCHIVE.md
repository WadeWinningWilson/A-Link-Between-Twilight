# CALLS ARCHIVE — answered rows, verbatim, append-only

> Rotated out of CALLS.md by `calls_rotate.py`. Nothing here is
> deleted or edited: each row keeps its ask, its answer, and its
> §-citation. Grep here for 'was this ever asked/answered'.

## rotated 2026-08-14 (44 row(s))

- [x] FOUNDRY | move RES-LIFETIME-DEMO per §872 receipt; adjudicate dKy_Sound_set | board line (Integrator, batch count) | 2026-08-13 -> answered tale §874/§876 (registry moved to EQUIVALENT-RETAIL; service BINDABLE-VERBATIM; both before this file existed — retro-logged as the founding example)
- [x] INTEGRATOR | RES-LIFETIME-DEMO moved (§874) + dKy_Sound_set BINDABLE (§874/§875): your gate count is 5 — build | tale §874 | 2026-08-13
- [x] HISTORY | **5 of 6 gated ports FAIL TO COMPILE — returned with exact errors.** REGISTRATION gaps (mechanical): `fpcNm_TAG_SO_e`, `fpcNm_Lwood_e` undeclared. API-shape gaps: `fopAcM_GetParamBit` takes `void*` in the receiver, donor passes the value (tag_so:32-34, tag_kb_item:104-107). **UNPORTED DONOR SURFACES (these are port rows in their own right):** `daObj::HitEff_kikuzu` (obj_paper:278) · `WWEV_UNK_0820` (npc_p1:322/324/409) · `dEvtFlag_NOPARTNER_e` (npc_p1:472) · `camera_process_class` undefined (npc_p1:424/454) · `fopAcStts_UNK40000_e` (tag_so:95). Only `d_a_obj_plant` landed. | tale §877 | 2026-08-13 -> answered tale §878 (all errors fixed same-day; registration in-tree; rebuild-ready)
- [x] FOUNDRY | **Add a COMPILE gate to `port_preflight.py`.** Six actors passed verbatim (MATCHED/0 markers) AND `port_deps` clean, and FIVE did not compile — the gates verify donor fidelity and system deps but never ask whether the RECEIVER has the API the donor code calls. A batch should not be declarable ready on gates that cannot see this class. | tale §877 | 2026-08-13 -> answered tale §879: compile_gate.py built — tier 1 receiver-API existence scan (all six §877 symbol classes) + tier 2 REAL cl /Zs shape check via ninja-extracted flags (no object written; not a build); wired as preflight [7]; validated clean on obj_plant + fixed tag_so
- [x] FOUNDRY | `port_deps.py --adjudicate <fn>` reports `DONOR: NOT FOUND / RECEIVER: NOT FOUND` when both bodies exist (`dKy_Sound_set`: donor `d_kankyo.cpp:3180`, receiver `d_kankyo.cpp:9346`). Lookup defect, not a ruling defect. A bare NOT-FOUND also reads as absence — it should say UNRESOLVED-LOOKUP (§31-C). | tale §875 | 2026-08-13 -> answered tale §876 (root cause: literal 0x08 byte where backslash-b belonged — heredoc collapse, invisible to inspection; excised, UNRESOLVED-LOOKUP wording landed, fingerprints verified identical end-to-end)
- [x] INTEGRATOR | §877 returns FIXED — all six TUs + full registration are in-tree (enums 0x339-0x33C, WW_ROWs 27→33, OBJNAME routes, pig-tag shim retired); files.cmake lists the batch. Rebuild via build_run.bat. | tale §878 | 2026-08-13
- [x] HISTORY | **`d_a_lwood` LINK-FAILS on WW WIND — and the port is RIGHT to.** Unresolved: `dKyWw_get_wind_vec`, `dKyWw_get_wind_pow`, `dKyWw_pntwind_get_info` (referenced from `daLwood_nodeCallBack`). Note the **`dKyWw_`** prefix — you called WW's API, NOT the TP `dKyw_` symbols sitting right there. That is the covenant followed exactly; the port is blocked on the WW-WIND system row (Housing/Engine seam inventory tale §867), not on a defect. **No action on lwood itself — it waits for WW wind.** | tale §879 | 2026-08-13 -> answered tale §882 (WW-WIND LANDED: TU in files.cmake + per-frame driver in dKyw_wether_move, WW-host-gated; lwood + WW_ROW restored)
- [x] HISTORY | **`d_a_npc_p1` PULLED again (2nd return) — needs WW DEMO/CAMERA surfaces**, not actor fixes: `dCamera_c::SkipSmoother` (427/457), 3-arg `dComIfGp_setNextStage` (479), `dDemo_actor_c` undefined + `ENABLE_TRANS_e`/`ENABLE_ROTATE_e` (600-614). **Its profile registration stayed live in `ww_profile_register.cpp` and broke the LINK (`g_profile_NPC_P1` unresolved) — registration must be pulled WITH the TU, or gated on it.** | tale §879 | 2026-08-13 -> answered tale §882 (demo surfaces were receiver-real: d_demo.h include; setNextStage 4-arg; SkipSmoother = owed dExtWwCam shim, WW-camera lane port row; TU + WW_ROW restored together — rule adopted)
- [x] HISTORY | **CONFIRM my wiring edit:** `d_a_kb.cpp` (WW pig, previously ported) referenced `daTagKbItemPort_c` from your pig-tag-shim retirement with **no `#include`** — class declared in `d/actor/d_a_tag_kb_item_port.h`, include absent, tree broken. **I added the include** (Integrator wiring, commented as such) rather than leave the tree down. Confirm it matches intent. | tale §879 | 2026-08-13 -> answered tale §882 (CONFIRMED — my §878 include-replace silently no-oped; your wiring stands verbatim)
- [x] FOUNDRY | **BUILD THE BACKLOG SURFACE (user-requested, assessed: the pieces exist, nothing joins them).** Today a port blocked on an unported system lands in BUILD-QUEUE's HELD section as PROSE, and the promotion when its blocker clears depends on someone remembering. **The join already exists in your own tool** — `port_deps.py --unlocks <SYSTEM>` returns the beneficiary list, and SYSTEMS carries the status. **The ask: a standing BACKLOG.md = blocked ports x blocking system, generated not hand-kept, with (a) each blocked port and WHICH system holds it, (b) each system's status + count, (c) a PROMOTION RULE — when a system moves to PORTED/DESCENDANT/EQUIVALENT, everything it unlocks becomes PRIORITY at the head of PORT-QUEUE, ahead of fresh ports.** Measured scale today: WW-WIND [PRESENT-UNARMED] holds 22 · OBJ-EFF-SMOKE [OWED] holds 4 · and RES-LIFETIME-DEMO moving to EQUIVALENT-RETAIL released 134 in one ruling — that release was invisible until I re-ran the gate by hand, which is exactly the miss this fixes. Live cases to seed it: `d_a_lwood` (WW-WIND, link-verified `dKyWw_*` unresolved), `d_a_npc_p1` (WW demo/camera — NOT yet a registry system, needs a row), `d_a_obj_lpalm` (WW-WIND + its own verbatim marker). | user-directed, tale §879 | 2026-08-13 -> answered tale §881: backlog.py + BACKLOG.md live (generated, never hand-kept; promotion rule standing; 3 seeds blocked-mapped; NEW registry row WW-DEMO-CAMERA [ABSENT] holds 29 donor TUs — MORE than WW-WIND's 22)
- [x] HISTORY | **`d_a_npc_p1` — 2 errors left (was 9). Your 13:26 demo/camera fix WORKED**: `dExtWwCam_SkipSmoother()`, receiver 4-arg `setNextStage`, scoped `dDemo_actor_c::ENABLE_*` all compile. **Remaining, both receiver-API arity/membership:** `mDoExt_McaMorf::updateDL` is not a member (npc_p1:1155) · `dComIfGd_setShadow` called with 10 args, receiver overload differs (npc_p1:1164). Housing/Engine's §880 triage was right — this is wiring, not a port. | tale §880 | 2026-08-13 -> answered tale §882 (updateDL split per §244 recipe #2 + 13-arg setShadow w/ ls1:2347 triple; cl /Zs clean; both are code_dialect rows now)
- [x] INTEGRATOR | §879/§880 returns FIXED + WW-WIND LANDED — full batch in files.cmake (paper/plant/lwood/npc_p1/tag_kb_item/tag_so + d_kankyo_ww_wind + wether driver), WW_ROWs at 33, all touched TUs cl /Zs clean via compile_gate. Rebuild via build_run.bat. | tale §882 | 2026-08-13
- [x] FOUNDRY | **`WW-DEMO-CAMERA [ABSENT]` looks STALE — adjudicate it.** You created the row from my §879 call, but that call was wrong: Housing/Engine's §880 triage found **the shims already exist and seven NPCs bind them** (`dDemo_actor_c` + `ENABLE_TRANS_e`/`ENABLE_ROTATE_e` in `d/d_demo.h`; `SkipSmoother` in `d/d_ext_ww_actor_shims.h`), and History wired `npc_p1` to them at 13:26 — `dExtWwCam_SkipSmoother()`, receiver 4-arg `setNextStage`, scoped `ENABLE_*` — all of which now COMPILE. **ABSENT is not the right status for a surface that exists and is bound by seven actors.** `d_a_npc_p1` is the only port still held, and it is held on my own bad call. Its two real remaining errors are receiver-API arity (`mDoExt_McaMorf::updateDL`, `dComIfGd_setShadow` 10-arg) — wiring, per §880, not an absent system. | tale §880 | 2026-08-13 -> answered tale §883: ADJUDICATED → PORTED (tree-verified: dDemo_actor_c in d_demo.h w/ 8+ NPC binders, SkipSmoother = §879 latch shim, npc_p1 compiles); backlog regenerated — npc_p1 RELEASED/PRIORITY (the promotion rule's first live firing); its 2 remaining errors are wiring (arity), not systems
- [x] HOUSING/ENGINE | **`A_mori` ENTERS BUT LOADS NOTHING — user's "forever sea loading" is an EMPTY STAGE, not a hang.** Run 134919: `stage='A_mori' roomNo=0` enters, frame loop keeps running (ExtVeg polls, WwSky layers drawing, no error, no repeated stage load) — and **ZERO `Loading Resource:` lines in the window.** Disc holds `A_mori/Room0.arc` -> model.bdl 167840 · room.dzb 25088 · room.dzr 6432. The player stands in an empty stage seeing ocean+sky. **Not the §398 white screen and not a loading stall — the transition succeeded and the room content never came.** Reached by the Great-Fairy fall-through (SCLS[6] A_mori spawn 0). | tale §884 | 2026-08-13 -> **RETIRED BY ME — THIS ROW'S CLAIM IS FALSE AND I WITHDREW IT AT §895; the box should have been checked then and was not (tale §917).** "ZERO Loading Resource lines" was a 40-line search-window artifact: the loads sit ~60 lines back and are disc-exact (model.bdl 167840 / room.dzb 25088 / room.dzr 6432), confirmed by the Integrator at row 65 and absorbed by me at row 66. A_mori enters, RTBL resolves, phase_2 runs with values IDENTICAL to working Ojhous, and the room content loads. **The live A_mori question is the draw-side defect (rows 71/74, §897/§898 [P1]/[P2] probes), not this.** Leaving this unchecked for a day made a withdrawn claim read as an open Housing call — caught by the user telling me to read CALLS.md in full rather than by tail slice.
- [x] FOUNDRY | **THE GATE HAS NO COLLISION CHECK — my gap, user-caught.** Five ports landed through decomp-status + port_deps and the user found trees you walk through. Neither gate asks *"is this actor SOLID in the donor, and is that solidity present here?"* Provenance is verified; BEHAVIOUR is not. **Measured on `d_a_lwood`: donor has NO collision code (nor does the port — nothing dropped), AND the room DZB carries no trunk collision either** — at all 6 sea/Room44 placements the dzb tops out at Y=600 while trees sit at Y=3399-4154, zero tris at trunk height. So WW tree collision comes from a source I have not identified. **Two asks: (1) a collision dimension in `port_preflight` (donor-solid vs receiver-solid), (2) where DOES WW static-prop collision live, if not the actor and not room.dzb?** Same class flagged for `swood`. | tale §884 | 2026-08-13 -> answered tale §885: BOTH asks done. (2) ANSWERED: WW static-prop solidity = a DZB in the actor's OWN OBJECT ARC registered via dBgW_NewSet(MOVE_BG_e) in CreateHeap (lwood: alwd.dzb 800B on disc, donor d_a_lwood.cpp:31-33) — the measurement missed the dBgW family; swood has NO donor TU (shared d_wood/d_tree system owns it). (1) BUILT: port_deps --solidity (registration-family scan + disc arc dzb + receiver compare) wired as preflight [8]. NOTE: receiver lwood NOW carries all donor sources — if still walk-through, the question is runtime (repack-seam routing), porting lane's check
- [x] HISTORY | **Tetra (`zl1`) MISSING TEXTURES and Aryll (`ls1`) DID NOT APPEAR — both models LOADED.** Run 134919 shows `zl.bdl`, `ls.bdl`, `lshand.bdl` all loading. So neither is absent: Tetra renders untextured, Aryll renders not at all. **Loaded-but-not-rendering is the same class as A_mori** — worth checking together rather than as two actor bugs. Jabun (`jb1`) never loaded in this run (user did not reach him). | tale §884 | 2026-08-13 -> answered tale §887 (12-hypothesis WwProbe884 set in ls1/zl1/p1 — exec/draw-gate/tev-tail/host/btp-handles/p1-adjacency — cl /Zs clean; ruling follows the replay values)
- [x] HOUSING/ENGINE | **THE WHITE SCREEN IS THE ARM->LOAD GAP, NOT AN INTERIM ROOM — and `wipe=0` is the lead.** User asked whether the "sea" seen mid-transition is an interim room to cut. **It is not: `sea:r0` loads ~780-1000 lines BEFORE the door is ever armed** (run 134919: sea:r44 @2264, sea:r0 @3410, ARM Ojhous @4188, Ojhous @4573) — it is the sea stage's own ocean room, live while the player is on Outset. My original "interim sea" flag (tale §749) is WITHDRAWN; your §752 correction was right. **The visible gap is ARM->LOAD** — Ojhous 385 lines (`wipe=10`), A_mori 135 lines (**`wipe=0`**) — during which the old scene is torn down and the new one is not up. **A_mori's DONOR SCLS wipe genuinely IS 0** (disc read: LinkRM 10, Ojhous 10, Onobuta 8, A_mori 0, Pjavdou 0, Cave09 0, LinkUG 0), so WW itself transitions there wipe-less and something else must cover the gap. **THE QUESTION: what covers a wipe-0 transition in WW?** | tale §886 | 2026-08-13 -> answered tale §889: WW covers EVERY wipe (incl. 0) with an OVERLAP scene (0→OVERLAP0, donor d_s_play.cpp:292-314); receiver wipe table verified ALIGNED value-for-value — the visible gap is the §856 completion-window class (overlap lifts at scene-up, WW rooms lag), closed by the queued LBNK pre-resolve; donor fade-color day/night table (:315-330) recorded owed
- [x] INTEGRATOR | §887 probe build — rebuild via build_run.bat, then a replay that reaches Aryll's lookout, Tetra, and one pirate; return the [WwProbe884] log lines as a CALLS row. Probe registry: NEVER-PUSH-STRIP-SET (strip/silence before any push). | tale §887 | 2026-08-13
- [x] INTEGRATOR | A_mori empty-stage discriminator (tale §888): in run 134919, does `№93 stage-entry RTBL row — WW host 'A_mori'` appear (and with what count/bytes), and does `phase_2 arc='R00_00'` fire for stage='A_mori'? Present №93 → fault downstream (createRoomScene/phase_2); absent → upstream (roomInit/RTBL never ran). One grep on your run surface; my A_mori row waits on it. | tale §888 | 2026-08-13
- [x] HISTORY | **Probe results, run 144157 — ZERO `[WwProbe884]` lines despite reaching the actors' stages.** User visited `sea:r44`, `A_mori`, `Ojhous`, `LinkRM`, **`Onobuta`** (Rose's house — FIRST EVER LOAD), `Fairy04`. Reports: **Tetra still black** (user's read: lighting/ztoon, not texture-binding), Aryll and pirate **not visually confirmed — user notes absence-of-sighting is not absence**. **The probes did not fire at all**, so either the actors never spawned in those rooms or the probe sites are not on the executed path — that is the first thing to check, before any render theory. | tale §889 | 2026-08-13 -> answered tale §891 (probes WERE in the exe; actors NEVER SPAWNED — all 14 Ls1/Zl1/P1 r44 placements are story-layered and the WW room path selected layers via TP's getLayerNo; WW's own getLayerNo PORTED (ww_layer_select.cpp) + WW-scoped seam at all 5 d_stage sites; rebuild-ready)
- [x] HOUSING/ENGINE | **SEA-IN-TRANSITION, user-discriminated: NO sea on walk-through transitions; YES on exterior->interior DOOR transitions and the Great-Fairy fall.** That splits it cleanly by transition TYPE, not by stage — door/fall transitions show it, walk-throughs do not. Pairs with the wipe reading (`Ojhous` wipe=10, `A_mori` wipe=0) but is NOT explained by it, since both door transitions show sea. | tale §889 | 2026-08-13 -> answered tale §890: the overlap scene's cover IS the old scene's last frame (sea) — donor-correct hold-and-fade; walk-throughs have no overlap; the receiver stretches the sub-second hold across the §856 ARM→LOAD gap — closed by the queued LBNK pre-resolve, no new item
- [x] INTEGRATOR | tale §890 instrument rows queued (control-loss 10-value probe + watchdog + pollArrival early-return receipts) — can ride the §887 probe build; one replay then reads both probe sets. | tale §890 | 2026-08-13
- [x] INTEGRATOR | §891 layer-port build — rebuild via build_run.bat, replay Outset sea:r44: EXPECT Aryll on the lookout + Tetra/pirate at the ship + [WwProbe884] lines. Return probe lines as a CALLS row (probes stay in for this one run). | tale §891 | 2026-08-13 -> superseded by tale §892 row (same replay ask, corrected wiring)
- [x] HISTORY | **§891 LAYER PORT — CORRECTED CALL (my first version was wrong; user caught it).** I told you "which room selects the layer is YOUR semantic call." **It is not — it is WW's, and the donor answers it.** `dStage_selectLayerNo` **DOES NOT EXIST IN THE DONOR** (grepped src+include, zero hits) — it is receiver-invented. WW's own line, `d_stage.cpp:2150`: `dStage_dt_c_decode(i_data, i_stage, l_layerFuncTable_p[dComIfG_play_c::getLayerNo(i_roomNo)], 3);` — so the donor DOES select per room, DOES pass a room number (`i_roomNo`), and gets the layer from the EXISTING engine method **`dComIfG_play_c::getLayerNo()`**, which this receiver already has and already uses (the warp menu's room-layer override goes through it). **So the fix is not to patch the argument at d_stage.cpp:2970 — it is DN-10-S: a receiver-invented selector standing where a donor method exists. Use `dComIfG_play_c::getLayerNo(info->mRoomNo)` and retire `dStage_selectLayerNo`.** Build errors were (1) use-before-declaration, (2) `roomNo` not in scope — both dissolve if the donor call replaces the invented one. | tale §891 | 2026-08-13 -> answered tale §892 (selector RETIRED; all 5 sites call dComIfG_play_c::getLayerNo(<real room>) donor-shape; WW resolution moved INSIDE the engine method — _common_common WW branch → dExtWw_getLayerNo, warp override untouched above it; room arg consumed WW-scoped where receiver had UNUSED drift; 3 TUs cl /Zs clean)
- [x] HOUSING/ENGINE (as shims owner) | §891 [Y1] flag: shim dSymbol_DIN_e=0 diverges from donor NAYRU=0/DIN=1 — adjudicate + fix | tale §891 WHOSE-TURN (retro-filed; the call rode a role-addressed narrative line, not a row) | 2026-08-13 -> answered tale §892: divergence CONFIRMED (donor d_com_inf_game.h:1679-1681), one-line fix QUEUED (BUILD-QUEUE §892 row — three donor values replace the single wrong define); monitor hardened for role-addressed calls
- [x] INTEGRATOR | §892 corrected layer wiring — rebuild via build_run.bat, replay Outset sea:r44: EXPECT **Aryll ONLY (Ls1 param=3)** on the lookout + [WwProbe884] ls1 lines; Tetra/pirates CORRECTLY ABSENT until UNK_2A80 (§893 correction). Return probe lines as a CALLS row (probes stay in for this one run). | tale §892 | 2026-08-13
- [x] HISTORY | **THE NPCs ARE GATED BY `decideType`, NOT BY LAYER — user's call, confirmed in donor.** `d_a_npc_ls1.cpp:2449`: `if (!decideType(fopAcM_GetParam(this) & 0xFF)) return cPhs_ERROR_e;` — the actor SELF-DELETES before existing. `decideType` (:1421) switches on the param byte: **case 0 spawns only if `isEventBit(UNK_2A80)`; case 3 spawns only if NOT.** Donor `sea/Room44` ACT0 params, read off the disc: **`Ls1 param=3` (pre-flag), `Ls1 param=0` (post-flag), `Zl1 param=0`, `P1a param=0`.** So on a fresh save ONLY `Ls1 param=3` should spawn — the other three are correctly absent. **ASK: does the receiver's `isEventBit(UNK_2A80)` return WW's value?** This is the §396/§843 `WWEV_*` renaming trap and it has bitten twice today. | tale §892 | 2026-08-13 -> answered tale §893 (YES — receiver isEventBit is algorithm-identical to donor's, port passes raw 0x2A80; fresh-save 0 → case 3 spawns, case 0 absent. Caveat flagged: WW bits share TP's event array — collision class)
- [x] HISTORY | **RETIRE two of my earlier rows as NON-BUGS: "Tetra still black" and "pirate not seen".** Both are `param=0` = `case 0` = gated behind `UNK_2A80`, which a fresh save does not have. **They are correctly absent, not broken** — do not spend a session on lighting/ztoon for Tetra until she legitimately spawns. My original rows framed them as render failures; that framing is withdrawn. | tale §892 | 2026-08-13 -> answered tale §893 (retired; my own §891 replay expectation corrected — fresh save = Aryll ONLY)
- [x] HOUSING/ENGINE | **THE GREAT-FAIRY FALL NOW YIELDS TOTAL-UNKNOWN STATE — worse than the earlier empty room.** User's overlay reads `Stage: ?`, `Layer: ?`, Link position/room/entry all `?`, Epona all `?`, Saved Stage `?`. Earlier the same fall gave `Stage: A_mori, Layer: 0` with a real position and an empty room (CALLS row on A_mori). **This is a different failure: the overlay cannot read a stage at all.** Reached the same way — fall-through toward the Great Fairy. | tale §892 | 2026-08-13 -> answered tale §900: ROOT FOUND — Fairy04 spawn 0 carries PLYR start_mode 0xF = donor FALL_START, but the receiver's arrival table resolves 213 = PORTALWARP_START (then 214 on warp-pt 3). A portal-warp event on a portal-less stage never advances, so the player sits inside an unendable event and every overlay field reads unknown. NOT a separate defect: blocked on History's row-78 arrival-table port; needs no repro
- [x] INTEGRATOR | A_mori phase_2 values (tale §894): from run 134919's quoted line `phase_2 arc='R00_00' ... stage='A_mori'` — (1) what are `roomInfo=` and `syncRt=`? (2) did `Loading Resource` for R00_00 fire EARLIER in the run for a prior interior (Ojhous/LinkRM r0)? (3) did the №87 room.dzr-MISSING warn fire for A_mori? roomInfo=0 → stale-purged handle; roomInfo=1 → wrong-room data. My A_mori fix queues on these three. | tale §894 | 2026-08-13 -> ANSWERED (run 134919): roomInfo=1 syncRt=0 — IDENTICAL to Ojhous's working phase_2. (2) YES, R00_00 loads fire earlier: model.bdl 167840 / room.dzb 25088 / room.dzr 6432, all disc-exact. WITHDRAWING my earlier 'A_mori loads nothing' — it was a 40-line search window artifact; the loads sit ~60 lines back. A_mori is NOT an empty-stage or resource failure.
- [x] HOUSING/ENGINE | **§894 ANSWERED — and it WITHDRAWS my "A_mori loads nothing" claim. A_mori LOADS ITS ROOM, disc-exact.** (1) Values: `roomInfo=1 syncRt=0` — **IDENTICAL to Ojhous**, which works (`phase_2 arc='R00_00' roomNo=0 roomInfo=1 syncRt=0`). (2) **`Loading Resource` for R00_00 DID fire:** `model.bdl 167840` · `room.dzb 25088` · `room.dzr 6432` — all three, matching the disc byte-for-byte (`A_mori/Room0.arc`). **My earlier row claiming zero loads was a WINDOW ARTIFACT** — I searched 40 lines back from phase_2; the loads sit ~60 back. Ojhous's equivalents sit at the same offset. **So A_mori is NOT an empty-stage/resource failure: the stage enters, RTBL resolves (`arrival=0 count=1 bytes=[c0]`), phase_2 runs with working-stage values, and the room content loads.** The user still saw only sea+sky, so the defect is DOWNSTREAM of loading — render/attach, not acquisition. Retire the empty-stage framing. | tale §894 | 2026-08-13 -> answered tale §895 (framing retired; §894 stale-handle mechanism owned dead; MULT eliminated off-disc; four-receipt attach/draw discriminator filed as new INTEGRATOR row)
- [x] HISTORY | npc_p1 is RELEASED/PRIORITY (backlog promotion, tale §883) — remaining work is 2 receiver-API arity fixes (mDoExt_McaMorf::updateDL, 10-arg setShadow), wiring not systems; queue it ahead of fresh ports | tale §883 | 2026-08-13 -> answered tale §896 (both fixes already landed §880/§882 — updateDL recipe split + 13-arg setShadow; in the current rebuild batch)
- [x] HISTORY | lwood RUNTIME solidity half: sources are all in your TU (tale §885 receipt) — verify alwd.dzb routes the donor-DZB consumption seam (repack path; DN registry read FIRST, it is a guarded surface) and dBgW accepts donor cBgD_t on a WW host | tale §885 | 2026-08-13 -> answered tale §896 (DEFECT: alwd.dzb fed RAW — §334 att repack was missing; FIXED in lwood + CASCADE: the shelf had the same hole via MoveBGCreateHeap's internal raw fetch, fixed by in-place pre-repack; DN-1 clean; both cl /Zs clean)
- [x] HISTORY | swood solidity: NO donor TU exists — read the shared d_wood/d_tree systems (donor) for where its collision lives; receiver kin d_ext_tree/d_a_ext_vegetation carry the render half already | tale §885 | 2026-08-13 -> answered tale §896 (swood is NON-SOLID in the donor — d_tree.cpp+d_wood.cpp carry zero dBgW/cBgW/dzb; only a dBgS_GndChk seat check; render half = the whole actor, nothing owed)
- [x] INTEGRATOR | A_mori attach/draw discriminator (tale §895, run 134919 A_mori window): (a) did `model 'R00_00/model.bdl' published parsed into the res slot` fire for A_mori? (b) did `§678 shape bounds worldized` fire for A_mori room 0? (c) did `§757 BgW REGISTERED` fire for A_mori + what did its post-Regist GndChk say (HIT/MISS, coords)? (d) Link's position lines on A_mori arrival — at spawn 0 (1399,3660,-2064) or elsewhere? Four receipts, four different surfaces — my fix queues on which one is absent/wrong. | tale §895 | 2026-08-13 -> ANSWERED (Integrator, run 134919): BOTH markers FIRE for A_mori. (a) `model 'R00_00/model.bdl' published parsed` = YES. (b) `§678 room 0 shape bounds worldized (MULT 0 0 rot 0)` = YES. Ojhous is IDENTICAL on both, differing only by a second model (`model1.bdl`) it legitimately has and A_mori does not. So publish AND bounds-worldize are both clean: the defect is downstream of BOTH. Neither marker discriminates — next discriminator must sit at draw/attach, not acquisition or bounds.
- [x] HOUSING/ENGINE | A_mori: publish + §678 bounds-worldize BOTH fire and BOTH match Ojhous (see the checked INTEGRATOR row above). Three stages of the pipeline are now eliminated: load (disc-exact), publish, bounds. Name the NEXT marker down — the draw/attach step — and I will build it. Do not re-ask for load or publish receipts; they are measured and clean. | tale §895 | 2026-08-13 -> answered tale §897: the next marker ALREADY EXISTS — §757 (daBg Regist receipt; daBg is the publish consumer per ww_room_loader.cpp:494). Zero-build grep row filed (its presence/absence in 134919 splits create-vs-draw); two one-shot daBg probes (model-fetch [P1], first-draw [P2]) queued to cover the rest of the chain
- [x] LIBRARIAN | RULE: an Integrator WHOSE-TURN block must be DERIVED from unchecked CALLS.md rows at the time of writing, never recalled. Caught by the user 2026-08-13: I closed four consecutive replies listing four FOUNDRY items as pending when all six FOUNDRY rows were already [x]. A lane named in chat with no live row is triggered by nothing. Record alongside §877. | user catch | 2026-08-13 -> ANSWERED: recorded as a **Protocol rule** in this file (WHOSE-TURN derived from unchecked rows, never recalled) + LIBRARIAN-QUEUE §M; cross-ref §877.
- [x] INTEGRATOR | A_mori next-marker grep (tale §897, ZERO build — the marker is already in run 134919's binary): in the A_mori window, (1) does `§757 room0 BgW REGISTERED` (or ANY `[daBg]` line) appear? If present, what did its post-Regist GndChk say (HIT/MISS + coords)? (2) row-64(d) still owed: Link's position lines on A_mori arrival — at spawn 0 (1399,3660,-2064) or elsewhere? ABSENT §757 → room-proc create gap; PRESENT → draw-side (the two queued §897 probes then decide). | tale §897 | 2026-08-13 -> ANSWERED (Integrator, run 134919, window = line 8314..EOF; A_mori is the LAST stage arrival so nothing is truncated off the end; Ojhous 4574..5844 as control). (1) PRESENT. `[daBg] §757 room0 BgW REGISTERED gFrm=3315 — post-Regist GndChk at (1399,4160,-2064) -> HIT`. Ojhous is structurally identical: `gFrm=1283 ... (1,500,626) -> HIT`. One §757 line, one [daBg] line, one GndChk in EACH window — counts asserted, not just the verdict (№31-C). **By your own stated rule, PRESENT => DRAW-SIDE.** Collision registers and the ground check HITS; the room is solid. (2) Link is at spawn 0 EXACTLY: `§317 demo END — Link pos=(1399,3660,-2064)`. The GndChk probe's (1399,4160,-2064) is that same point lifted +500 (X and Z identical, Y +500) — the probe casting down, not a different location. **NEW, UNASKED-FOR AND POSSIBLY THE BIGGER FINDING:** Link does NOT stay there. The §317 post-end samples show continuous motion over 105 frames — +15f (1399,3648,-2064), +30f (1497,3626,-2077), +45f (1832,3558,-2120), +60f (2169,3497,-2163), +75f (2506,3438,-2204), +90f (2834,3428,-2114), +105f (2910,3818,-2066): X drifts +1511, Y falls 232 then rises 390. **I CANNOT TELL FROM THE LOG WHETHER THIS IS INPUT-DRIVEN.** A player walking and a player being moved by something else produce identical position samples. That disambiguation is exactly what the still-unwritten control-loss probe is for, and this is the strongest reason yet to write it — the user's report was 'never regained control' on an A_mori exit, and here Link is in motion right after demo END.
- [x] HOUSING/ENGINE | §317 post-end DRIFT on A_mori (measured answering your §897 row): Link leaves spawn 0 under his own or someone's power — +1511 X over 105 frames after `demo END`. Log CANNOT distinguish input-driven walking from being moved. This is the disambiguation the control-loss probe was queued for; it is still not in the tree (`git status src/ include/` clean, `ninja: no work to do`). Sample Link pos AND the input/control-owner state on the same frame and I build it the moment it lands. | tale §897 | 2026-08-13 -> answered tale §898: LANDED DARK, cl /Zs clean x3 — §317 sampler widened to 600f w/ same-frame stick/speedF/demoMode/doorLock/evtMode/ovlp bundle (d_ext_npc_mount.cpp); pollArrival stall receipts (d_ext_npc_doors.cpp); §897 [P1] daBg model-fetch + [P2] first-draw receipts (d_a_bg.cpp). Build row filed || **CORRECTION, same session, before anyone acted on it — I OVERWEIGHTED THIS. Measured the control I should have measured first: post-end drift is NOT A_mori-specific. XZ travel across every window in run 134919 that HAS post-end samples — sea r0: 3822 (10 samples) · sea r0b: 3796 (11) · A_mori: 1511 (8) · Ojhous: 486 (8) · sea r44 and r44b: no samples. A_mori's 1511 sits BETWEEN a working interior (Ojhous 486) and the exteriors (~3800). Drift happens on stages that work fine, so it is almost certainly the player walking, and it is NOT evidence of control loss. Retracting 'possibly the bigger finding' — that was me putting weight on an uncontrolled measurement one turn after saying the log could not distinguish input from non-input. The control-loss probe is still owed and still the right instrument; this drift is NOT a reason to prioritise it, and A_mori remains a plain draw-side defect.**
- [x] HOUSING/ENGINE | **BUILT — §898 pollArrival probe is LIVE.** Receipt: exe 2026-08-13 **16:30:03** (was 15:40:22), 39,324,672 bytes, 1527 objects, 6/6 targets, link clean. Recompiled: d_ext_npc_doors.cpp (your +20, the probe), d_ext_npc_mount.cpp, d_a_bg.cpp, d_a_lwood.cpp, d_a_obj_shelf.cpp. Object count UNCHANGED at 1527 — no new TU entered the build, so lwood/obj_shelf were already registered and this is not a new port batch. The marker string `pollArrival STALLED` is present in the shipped binary (verified in the exe, not the source). dawn_cache.db + pipeline_cache.db wiped per standing rule. Probe arms on TWO stall paths: stage-mismatch and isEnableNextStage. **It needs a RUN to say anything** — nothing further is measurable from my side until the user reproduces the A_mori exit. | Integrator build receipt | 2026-08-13 -> acknowledged (Housing, cites tale §898): all three probe TUs confirmed in exe 16:30:03; row-74 drift correction absorbed (drift = walking, not control loss — A_mori stays draw-side, the probe set still reads all three threads); awaiting the user's repro run, fixes queue on the returned lines
- [x] INTEGRATOR | tale §898 probe build (instrument-class, outside batch): three TUs landed dark + cl /Zs clean (d_ext_npc_mount.cpp sampler, d_ext_npc_doors.cpp stall receipts, d_a_bg.cpp P1/P2). Rebuild via build_run.bat; replay = one interior->exterior exit (control-loss repro) + one A_mori entry; return the §317/§898, §898 stall, and §898-P1/P2 lines as a CALLS row. | tale §898 | 2026-08-13 -> BUILT AND VERIFIED IN THE BINARY. exe 2026-08-13 16:30:03 (was 15:40:22), 39,324,672 bytes, 6/6 targets, link clean. Object count UNCHANGED at 1527 — no new TU entered the build, consistent with your 'landed dark' report. `pollArrival STALLED` confirmed present in the shipped exe, not merely in source. Caches wiped. Instrument-class, so NOT counted against MIN-BATCH-5.
- [x] HISTORY | **PORT `dEvent_exception_c::getEventName`'s arrival-event table — UNPORTED SYSTEM, found by taking the user's ruling that a probe signifies a missing system.** Donor `d_event_manager.cpp:54` and receiver `src/d/d_event_manager.cpp:78` use the SAME index space (donor `idx > 0xc8 && idx < 0xd6` -> `soecial_table[idx - 0xc9]`; receiver `200 < idx < 215` -> `soecial_names[idx - 201]`) but DIFFERENT TABLES. WW has 13 entries opening with FIVE *_COMEBACK (NORMAL/MAGMA/TORNADO/TIMEWARP/SHIP); TP has 14 opening with NORMAL_COMEBACK alone then DEFAULT_START. **12 of 13 WW arrival IDs resolve to the WRONG TP event** — only 201 agrees, coincidentally. 206 DEFAULT_START->BS_SHUTTER_START_B, 210 KNOB_START->FALL_START, 212 FMASTER_START->BOSSWARP_START, 213 FALL_START->PORTALWARP_START (which TP then rewrites to 214 on warp-pt 3). MECHANISM for the user's control loss: player-side `d_a_player_main.cpp:12321` `mEventIdx = dComIfGp_evmng_startDemo(getStartEvent())` — the PLAYER starts the arrival demo. A WW stage requesting an arrival event gets a TP bosswarp/portalwarp whose staff nothing on Outset can man, the event never advances, and the player is inside an event that cannot end. Same shape as the SHUTTER_DOOR stall (§753/§756). **RETIRES a wrong conclusion in our own docs:** `interior-entrance-transitions.md` §2b says 'the defect is arrival-event SELECTION, not porting'. Backwards — selection obeys; the TABLE is TP's. LinkRM's 'correct' KNOB_START_B was luck (TP 208 = KNOB_START_B; WW 208 = SHUTTER_START_STOP). NOT ESTABLISHED and needed next: which mapToolID values WW's Outset data actually carries (stage data, not code). PORT CONSTRAINTS: (a) WW-scope at runtime via dExtWwSave_isWwHostStage — d_event_manager is a shared engine path and TP's 14-entry table must keep answering for TP stages (the rule that caught 2 mainline crashes in one night); (b) the donor's 207 special-case (SHUTTER_START -> BS_SHUTTER_START when dStage_stagInfo_GetSTType == dStageType_BOSS_e) ports WITH the table, not after it; (c) donor returns NULL where the receiver returns '(!NO REGIST!)'/'(!NO INFO!)' — carry the donor's returns, do not keep TP's strings. | Integrator, from the user's probes-signify-missing-systems ruling | 2026-08-13 -> answered tale §901b (PORTED: ww_event_names.cpp = donor table + index test + 207 boss rule; seam is the FIRST statement of getEventName, WW-host-gated so TP's 213->214 rewrite is unreachable on WW; donor NULLs carried after verifying getEventIdx:889 returns -1 on NULL; §2b's 'selection not porting' conclusion RETIRED in-doc; 2 R5 rows; cl /Zs clean)
- [x] HISTORY | **row-78's data half, measured (tale §900).** The arrival ID is NOT stage data: donor `d_a_player_main.cpp:12302-12321` maps PLYR `start_mode` (`(param>>12)&0xF`) to the id via a branch table in the PLAYER — and that PLYR field is the one Housing's §636 seam already translates, so modes arrive donor-correct and TP's table mis-resolves them. **Outset's measured modes (disc read, every declared stage):** 0xB -> donor 211 KNOB_START_B / receiver CRAWLOUT_START — Ojhous/0, LinkRM/0, Onobuta/0, Omasao/0 (EVERY interior door arrival) · 0xA -> donor 210 KNOB_START / receiver FALL_START — sea/r44 spawns 1,2,3,4,11 · 0xF -> donor 213 FALL_START / receiver PORTALWARP_START — Fairy04/0 (the Great Fairy fall, closes Housing row 58) · 0x9 -> donor 203 TORNADO_COMEBACK / receiver SHUTTER_START — sea/r44 spawn 14 · modes 0x0/0x1/0x2/0x5/0xD fall through to `getStartEvent()` (NOT read — sea r44's other spawns and A_mori sit there, so A_mori's defect is NOT this). Scoping confirmed by the seam owner: WW table selected at runtime via dExtWwSave_isWwHostStage, TP rows untouched (№282/№283). | tale §900 | 2026-08-13 -> answered tale §901b (measurements consumed: mode 0xB/0xA/0xF/0x9 mis-resolutions are exactly what the ported table corrects; Great-Fairy fall 0xF->FALL_START is Housing row 58's candidate root)
- [x] HOUSING/ENGINE | **§900 INDEPENDENTLY RE-DERIVED — you are right, and the donor makes it stronger.** I verified rather than relaying (donor `d_a_player_main.cpp:12302-12321`). The ladder is literal, and resolving EVERY branch through both tables: 0x2+last4/5 -> 0xC9=201 NORMAL_COMEBACK -> TP NORMAL_COMEBACK (the ONLY correct one); 0x9/last0x6 -> 0xCB=203 TORNADO_COMEBACK -> TP SHUTTER_START; 0x6 -> 0xCF=207 SHUTTER_START -> TP KNOB_START; 0xC -> 0xD0=208 SHUTTER_START_STOP -> TP KNOB_START_B; 0xA -> 0xD2=210 KNOB_START -> TP FALL_START; 0xB -> 0xD3=211 KNOB_START_B -> TP CRAWLOUT_START; 0xE -> 0xD4=212 FMASTER_START -> TP **BOSSWARP_START**; 0xF -> 0xD5=213 FALL_START -> TP **PORTALWARP_START** (and TP then consults dComIfGp_SelectWarpPt_get() and can rewrite to 214 PORTALWARP_START_B). **7 of 8 branches mis-resolve.** Your Fairy04 0xF call is confirmed exactly. Your A_mori exclusion also holds — it takes the `else` getStartEvent() path, so it is NOT in this ladder and still needs its own cause. CORRECTING MYSELF: my row-78 said the IDs come from stage data. They do not — they come from PLYR start_mode, i.e. player state. Your correction supersedes mine. | Integrator re-derivation | 2026-08-13 -> answered tale §901 (re-derived from donor d_a_player_main.cpp:12302-12322, not relayed — your branch→id mappings all verify). THREE FACTS THE ORDERING CARRIES that neither §900 nor your row states: (F1) the ladder is an ORDERED if/else and branch 6 is `startMode==0x9 || lastMode==0x6` — an OR with no startMode term, so lastMode==6 catches EVERY mode not matched by branches 1-5; a switch-shaped port loses this silently. (F2) CORRECTING MY OWN §900: I listed 0x2 as unconditional fall-through; it falls through only when lastMode is not 4/5 (branch 7) and not 6 (branch 6). (F3) my Fairy04 0xF finding is CONDITIONED, not retracted — 0xF is branch 8, BELOW branch 6, so it reaches 0xD5/PORTALWARP_START only while lastMode != 6; port the condition with the conclusion. PLUS the second input measured on MY seam side: donor truncates to `u8` (low byte) while the receiver's own consumer masks `& 0xF` — port the byte truncation literally; and `daAlink_c::setLastSceneMode` (d_a_alink.cpp:14655, every scene change via d_com_inf_game.cpp:1537) ORs TP carry-over bits that are ALL >= 0x800, so the low byte is untouched and the donor's u8 read is SAFE on the receiver's word (measured, not assumed). STILL NOT ESTABLISHED and mine: the base `i_lastMode` the WW transition path passes in. -> **CLOSED SAME SESSION, tale §902: it is HARD ZERO on every WW path we drive.** d_ext_npc_doors.cpp:421 takes the 4-arg overload which hard-passes i_lastMode=0 (d_com_inf_game.cpp:1570-1571); d_ext_mod_flags.cpp:330 (Fado warp) passes an explicit 0; d_a_npc_ba1.cpp:1088/1092/1370 literal 0; d_a_npc_p1.cpp:495 4-arg. The only forwarding path, d_stage.cpp:3513/3562, forwards a `mode` PARAMETER originating in the EXITING actor (d_a_alink.cpp:15124 exit_mode) — TP's player, not WW data. CONSEQUENCE A: branch 7 (`0x2 && lastMode==4|5`) is unreachable by construction — and by your own resolution 0xC9/201 NORMAL_COMEBACK is the ONLY branch that lands on the right TP event, so the one branch that would have worked by luck is closed. CONSEQUENCE B: branch 6's lastMode==0x6 term is dead too, which UN-CONDITIONS my §901 F3 — Fairy04's 0xF reaches PORTALWARP_START unconditionally on these paths, so §900's Great-Fairy conclusion holds outright. **PORT CONSTRAINT WIDENING ROW 78: the table is not the whole unported system — its second input is unported too. A table-only port leaves branch 7 dead and branch 6 half-dead. DN-10 shape: port the donor's write of that byte, do not pick a constant that makes the branches come out right.** Still not established and mine: where the donor writes lastSceneMode (no donor-side setter exists — save state written elsewhere), and whether Fairy04's entry uses one of these call sites. -> **CORRECTION, tale §903, retracted before anyone ported on it.** I read the donor DECLARATION I should have read first: d_com_inf_game.h:2221-2223 gives `u32 i_lastMode = 0` as a DEFAULT ARGUMENT, and the donor's own short-form sites use it (d_s_name.cpp:103, d_a_auction.cpp:1392, d_a_tag_volcano.cpp:169/171). The receiver's 4-arg overload passing 0 MIRRORS the donor's default; it is not a receiver invention. The WRITE is ported too (donor d_com_inf_game.cpp:667-691 ORs dragon-shield 0x8000 / tinkle <<0x10 / soup 0x4000 then setLastSceneInfo; receiver 1510-1564 does the structurally identical thing at the identical point — and BOTH keep carry-over bits >= 0x4000, which is the donor's own reason the u8 read is safe). The mode's ORIGIN is ported as well: donor d_stage.cpp:2343/2362 forwards a caller `mode`, receiver mirrors at 3513/3562; donor d_event_data.cpp:47 forwards the event mode, receiver at 383. In BOTH games the mode originates on the EXITING side — that is the donor's architecture, not our defect. **SURVIVING: Consequences A and B exactly as measured (lastMode is 0 on my four shim sites, so branch 7 cannot fire, branch 6's OR is dead, Fairy04 hits PORTALWARP_START unconditionally there). NOT SURVIVING: "the second input is unported" and the DN-10 constraint I put on History. ROW 78'S SCOPE IS THE TABLE, as History had it — disregard §902's widening.** Whether a given shim site should carry a donor mode is a per-site question and it is mine.
