# BACKLOG — blocked ports x blocking system (GENERATED by backlog.py; never hand-edit)

> **PROMOTION RULE (standing):** when a system's registry status moves
> green (PORTED / DESCENDANT / EQUIVALENT-RETAIL), every port it held
> becomes **PRIORITY — ahead of fresh ports at the head of PORT-QUEUE** —
> and appears in RELEASED below until it lands. Regenerate after ANY
> registry move: `python tools/foundry/backlog.py`.

## RELEASED — blockers cleared, port to the queue head NOW

- **d_a_npc_p1** — all blocking systems green. PRIORITY. (2nd pull (CALLS return): SkipSmoother, 3-arg setNextStage, dDemo_actor_c, ENABLE_TRANS/ROTATE_e — demo/camera surfaces, not actor fixes.)

## BLOCKED — port x system, with the system's live status

- **d_a_lwood** held on **WW-WIND** [PRESENT-UNARMED]
    LINK-VERIFIED (CALLS return): dKyWw_get_wind_vec / dKyWw_get_wind_pow / dKyWw_pntwind_get_info unresolved — the port correctly calls WW's API; the field must land.
- **d_a_obj_lpalm** held on **WW-WIND** [PRESENT-UNARMED]
    Gate HOLD (tale §869) + own verbatim marker (1 Nonmatching on _execute, user ruling: not verbatim, not passed) — TWO holds, both must clear.

## SYSTEMS — status + measured demand (donor corpus --unlocks)

- **WW-WIND** [PRESENT-UNARMED] — holds 22 donor TU(s) corpus-wide
    unlocks on green: d_a_agb, d_a_bridge, d_a_dai_item, d_a_ghostship, d_a_goal_flag, d_a_himo3, d_a_ikari, d_a_kytag05, d_a_majuu_flag, d_a_mflft …
- SHADOW-SIMPLE2 [PORTED] — green; holds nothing
- MODEL-EMITTER [DESCENDANT] — green; holds nothing
- RES-LIFETIME-DEMO [EQUIVALENT-RETAIL] — green; holds nothing
- WW-DEMO-CAMERA [PORTED] — green; holds nothing
- **OBJ-EFF-SMOKE** [OWED] — holds 4 donor TU(s) corpus-wide
    unlocks on green: d_a_obj_barrel, d_a_obj_eff, d_a_obj_movebox, d_a_tsubo
- WW-LIGHTING [PORTED] — green; holds nothing
