# Cut Actors / Demo Restore — live state

**Run:** [Cut-Actors-Demo-Restore-Cursor-History.md](../Interconnected%20Chats/Cut-Actors-Demo-Restore-Cursor-History.md)  
**Control:** [run-control.md](run-control.md)  
**Process manual:** [WW-Restoration-Cookbook.md](../WW-Restoration-Cookbook.md) — the distilled ground-up pipeline (map→interiors→actors→limbs→animation→story gating); this ledger stays the home for run state + decisions.

---

| Field | Value |
|-------|--------|
| **status** | **№106 COMMIT LOCAL (`dce09e87ca`) — not pushed.** Renamed `d_ext_ww_save_guard.h` → `d_ext_save_guard.h` first; 74-file snapshot (all chats’ WIP). Exe greplist gate **0 hits**. Held: `outset_quests.txt` + scratch. User: always ask before push (alpha not ready). |
| **next** | **OUTSET RELOCATION + WW LIGHTING** (ladders/ledges №98 + WW palettes №96 + end offshore parking №76). History: lighting field-map + real FILI per room. Push only when user says. |
| **№32** | **SHIPPED** (prior). |
| **№34+35** | **SHIPPED.** Residuals: kusa/flower holes until WW grass port; Mesa outdoor door still absent; Omori shelved; wood/lamp holes; chest open behavior not wired (static box models). |
| **№28** | **B10 shipped:** size-mismatch twin ⇒ refuse + vanilla fallback (Linkle bad twin no longer boots AV). |
| **№31 LAW** | Standing: WW spaces = WW assets only. Prop wave uses library arcs only. **USER RULING (2026-07-19, FINAL — NO exceptions, currency included): RUPEES IN WW SPACES ARE WW RUPEES. No cross-pollination, period.** The TP wallet may be CREDITED by grant behavior (economy is a system), but the VISIBLE pickup object is WW's model. The earlier "currency exception" note is RESCINDED — it inverted the user's ruling. History delivering the WW rupee model source (tww d_a_item) in №36. |
| **№34** | **PROP/DOOR CORRECTIONS (History; user screenshots + arc censuses) + CANONICAL REFERENCES ADOPTED:** **(P1 the face-cone "grass" RETIRED)**: `Kusa.arc` contains only `long.bmd` — NOT field grass (that's the mustachioed thing in the yard). WW's cut-grass, like TP's, appears to be code-generated (no standalone model asset) — per №31 LAW: **kusa/flower-x rows = HOLES for now**; proper WW grass = future WW-side draw port. Un-map long.bmd immediately. **(P2 member-name fixes)**: mailbox = `Toripost.arc/vpost.bdl` (+`post_wait.bck` idle) — "not found" was a wrong member guess; **rocks**: `Piwa.arc/piwa.bdl`, `Always.arc/krock_00.bdl` + pebbles `obm_koisi1/2.bdl` (koisi rows); **CHESTS FOUND**: `Dalways.arc` — `boxa/boxb/boxc.bdl` (three tiers) + `boxopenbox.bck` open anims + takara flash — all five arcs NOW ADAPTED in library. **(P3 door rotation)**: census rows carry rotY (exported col `ry`) — apply it to Knob spawns; doors must face as WW authored. **(P4 BLACK-VOID WARP GUARD (G-guard for warps))**: A-press door → fade → if the interior create does NOT reach COMPLEATE+ready, ABORT: fade back to the island at the door, log the refusal — Link must NEVER be left controllable in darkness. Ferry the next `[Doors] enter` + `BG` log lines to History to root-cause why interiors aren't reaching viable rooms. **(REFERENCES — record for all future identity/params work, user-supplied)**: GameBanana WW modding actor/arc reference https://gamebanana.com/tuts/18805 + **the WW decomp https://github.com/zeldaret/tww (AUTHORITATIVE: actor sources reveal head-params semantics, arc bindings, msg ids)** — consult BEFORE guessing any WW name/param, alongside the Ivan-rule user pass. |
| **№35 IMPLEMENTATION PACKET (History research — data-complete; consult zeldaret/tww where marked):** | **(H1 HEADS — per-arc part table, ends the headless era):** `Ko` = kohead01/02 (done) + koeda/ko_balloon props; `Ym` = ym.bdl + **ymhead01/02 = TWO different men share the body** (+`ymkama` sickle = the grass-cutter's tool) — params byte picks head, exact bits from tww `d_a_npc_ym`; `Yw` = yw.bdl + **ywhead01 — SINGLE head, attach UNCONDITIONALLY** (the headless pink villager = this miss); `Ob` = ob.bdl + **oba_head.bdl unconditional**; `Ba` Grandma = complete in ba.bdl (+ba_cloth companion, no head split); `Bm` = **10 heads** (bmhead01-10 + wing/bag/knife/binder/arm props) — params-driven, tww `d_a_npc_bm` for the field. **RULE: single-head arcs attach it ALWAYS; multi-head via params; a body may never spawn headless.** **(H2 ARYLL'S HANDS — user called it):** `Ls` = ls.bdl + **separate `lshand.bdl`** — companion-attach ALWAYS (the mdarm pattern exactly). **(H3 IDENTITY REVELATION): `Zl` = TETRA** (zl.bdl + cloth.bdl — Zelda's actor prefix, decomp-confirmable) — the offshore T-pose was her; stays quest-gated. `P1` = the LARGE pirate trio (p1a/b/c_head + dora_stick gong). **(H4 RUPEE SPAWNS — CORRECTED; the prior text here INVERTED the user's ruling and is struck):** 14 `item` rows, params low byte = WW item id (01×6/02×4/03×3/04×1). **USER RULING (stated five times, FINAL): rupees in WW spaces are WW RUPEES — `Vlupy.arc/vlupy.bdl`, color by anim frame per item id. NO TP pickup actors in WW spaces. The grant CREDITS the TP wallet (economy is a system); the visible object is WW's. No carve-outs.** Matches №31 as amended. **(H5 QUEST-PROGRESS LAYER SCHEDULE, from the census matrix):** day-1 spawn set = base+ACT0 (+2 as same-day variant — verify vs tww); `Ah` (only layers 4-7) = a LATER-state villager → flag-gate; **P1a/Pirates/Zl1 on layers 8/9/a = the pirate-arrival/Aryll-taken/departure beats** → map layers→flags: `qs.pirates_ashore`(8), `qs.aryll_taken`(9), `qs.depart`(a/b); wire `[layers]` flag gates accordingly so the island's population SHIFTS with the story exactly as Nintendo authored. Exact layer semantics refine via tww + the user identity pass. |
| **№36 DECOMP PACKET (History — tww consulted directly; Cursor's stuck-list answered):** | **(A ARYLL HANDS — d_a_npc_ls1.cpp, mechanism quoted in full): the hand model is MATRIX-SLAVED, never animated.** Nintendo: body joints `ls_handL`/`ls_handR` have callbacks capturing their world mtx per frame (`mHand_L_Mtx`); the HAND model's same-named joints have callbacks INJECTING those matrices (`i_model->setAnmMtx(jnt, mHand_L_Mtx)`). There is NO hand BCK by design — stop seeking one. **Mount implementation: generic `companion_mode=joint_slave`** — after body anim update, for every companion joint whose NAME exists in the body skeleton, copy the body's anmMtx into the companion's joint (base TRMtx = body's). Fixes Ls hands; same mechanism serves heads (ym1 has `nodeCB_Head`) and likely Medli's arms. **(B HEADS — selection is BY CENSUS NAME, not params guessing):** tww header: **Ym = "NPC - Mesa & Abe" — Ym1 and Ym2 are TWO PROCS sharing one body ⇒ Ym1→ymhead01 (Mesa), Ym2→ymhead02 (Abe)**; Ko1/Ko2 → kohead01/02 likewise; single-head arcs (Yw/Ob) attach unconditionally. Replace `head_from_params` with per-census-name head mapping (01↔proc-1 ordering = strong assumption; user identity pass confirms). Heads attach via the SAME joint-slave mechanism to the body's `head` joint. **(C RUPEES, LAW-COMPLIANT — d_item_data.cpp quoted): ALL rupee colors = ONE model, `Vlupy.arc/vlupy.bdl`, color selected by ANIMATION FRAME** (bind the arc's brk/btk, set frame per item id: 01 green/02 blue/03 yellow/04 red/05 purple/06 orange/0x0F silver). Arc ADAPTED + in library (also Vhkak heart-piece, Vhutu container, VkeyN key for quests). Pickup = WW visual, grant credits TP wallet. **(D DOORS — d_stage.h quoted): TGDR entry = 0x24 CONFIRMED** (`name[8] + prmBase @0x08 + scale @0x20`, total 36B — not 0x30); SCLS = 0xC confirmed. Door pos = the entry's prmBase position; if the Knob visually offsets, the hinge-vs-center delta comes from the Knob model's own origin (bbox nudge), not the table. **(E BLACK ROOMS)**: unchanged — awaiting the `[Doors] enter` + `BG COMPLEATE/ABORT` log ferry from the next attempt; do not iterate blind. |
| **№37 NO-GREY-AREA PACKET (user-ordered; every value quoted from data/decomp — zero inference gaps):** | **(1 BLACK-ROOM PROBES — INSERT NOW, automatic, no ferry dependency):** on door A-press log `[Doors] req=<proc> resolved=<arc> anchor=<a> spawn=<s>`; interior create keeps D1 stage logs; AFTER create, ground-probe at spawn and log hit/miss+y; NO COMPLEATE or NO ground ⇒ ABORT + fade back to island at the door + log reason. Next black room self-diagnoses in one line. **(2 VILLAGER FIX TABLE — day-1 ACT0, one row per person; DUPLICATES ARE NINTENDO'S EVENT-GATED ALTERNATES (Ls1 decideType pattern), so same-name extra ACT0 rows get `disabled=1`, they are NOT extra people):** • **Ym1 = Mesa** (tww header "Mesa & Abe"): pos **(-194682, 356, 319100) ry=15837** params 00000000 — head **ymhead01** + optional `ymkama` sickle prop. • **Ym2 = Abe**: **(-201243, 293, 317626) ry=0** — head **ymhead02**. • **Yw1 = Sue-Belle**: KEEP **(-201308, 277, 316558) ry=22937** (params 00220000, type-byte 00 = default row); **DISABLE (-203752, 481, 317449)** (params 00ff0001 = event alternate — THE second Sue-Belle). Head **ywhead01 unconditional**. • **Ko1 = Joel** (tww header "Joel & Zill"): KEEP beach row **(-195607, 161, 318576) ry=-19115** (params 001e0000); **DISABLE (-203870, 495, 317220)** (00ff0002 alternate). Head **kohead01**. • **Ko2 = Zill**: **(-198181, 150, 319204) ry=1820** (00ff0000). Head **kohead02**. (01=Joel/02=Zill = proc-order per tww comment order; if faces disagree in the identity pass — Zill is the small snot-nosed one — swap the two heads, nothing else.) • **Ob1 = Rose**: **(-202248, 1088, 320170) ry=-13654** (000200ff, at the pig house level). Head **oba_head unconditional**. ALL heads/hands attach via №36-A `joint_slave`. Labels stay `? (unverified)` until identity.ini lock. **(3 DOOR HINGE — tww d_a_knob00.cpp QUOTED): pivot = the actor position itself, NO hinge offset** (`transS(pos) → YrotM(angle.y) → setBaseTRMtx`) — the model is authored hinge-at-origin. Model = KNOB frame `DOOR` bdl + knob variant `DOOR_A..H` selected by shapeType (TGDR params); **swing = KNOB BCK `dooropenadoor` rotating joint `DoorDummy`** (knob model rides that joint's anmMtx). Grandma's door (and all Outset doors): place at TGDR pos+rotY EXACTLY; any visual offset means the WRONG MEMBER was spawned — use the frame `DOOR` model, not a guessed bdl. **BONUS: play the real `dooropenadoor` BCK on A-press before the warp — authentic WW door-open for free.** **(4 FOLDER ALERT):** the real mod is currently at `WW-Crew-Restoration.DISABLED` (folder-absent test?); a fresh skeleton holds the live name — DO NOT build content against the skeleton; History's new item arcs (Vlupy/Vhkak/Vhutu/VkeyN) are copied into BOTH; re-enable the real folder before the next content build. |
| **№38 ENGINE LAW + SPAWN LEDGER (History; the data layer is AUDITED CLEAN — stop touching configs):** | **Audit result (machine-verified this session): ZERO socket_arg collisions across all 49 manifests; EVERY referenced member (model/idle/talk/brk/btk/door bck) exists in its arc; actor_map matches №37 exactly (heads, disables, rows). Therefore Ivan-as-rupees, uninvited Ganondorf, and headless villagers are ALL ENGINE-SIDE.** The architecture note: ~40 payloads multiplex ONE socket (NPC_HENNA0) by `socket_arg` — Ganondorf is arg **1**, so ANY defaulted/mismatched arg summons him; a loose resolver turns every miss into an imposter. **THREE ENGINE RULES (Cursor — these END the guessing class): E1 RESOLVER LAW: `resolveSocket(socket, arg)` matches BOTH exactly or REFUSES the spawn — no fallback, no first-match, no default payload, ever. A refusal = a hole + log line (G1-for-spawning; a missing rupee beats an Ivan).** **E2 SPAWN LEDGER: every spawn emits ONE line: `[Spawn] src={census:<name>@<pos>|door:<id>|cutactors|quest} proc=<> arg=<> manifest=<file> head=<model@joint|none|MISS:<reason>>` — any future imposter names its requester instantly.** **E3 HEAD HONOR: the spawner MUST consume actor_map's `head_model`/`head_joint` (audited present) via №36-A joint_slave, and the E2 `head=` field is the per-spawn proof; `MISS` reasons ferry to History.** Defense-in-depth applied by History (data): `gnd.ini` now `spawn_if_flag=beat.gnd_duel` — Ganondorf cannot spawn outside his beat even if a resolver bug recurs. **Verification = ONE warp**: grep `[Spawn]` — every line shows the intended manifest; any REFUSED line is an honest gap, not an imposter. ALSO: №35-H4's inverted rupee text STRUCK (it contradicted №31 and the user's five-times ruling — the doc must never carry dueling laws again; when amending a superseded entry, EDIT it, don't append a rival). |
| **№39** | **BLACK-ROOM LOGS CHECKED (History, log 074856): FIRST FULLY-GREEN DOOR TRANSIT** — `[Doors] req=EXT_BG4 resolved=Ojhous2 anchor=(0,0,0)` → pinned warp → BG COMPLEATE at own host → `ground hit y=2050` → `Link placed (-76053, 2050, -90227)`. The probe chain works; black-room class dead in current build (earlier voids = pre-№37 builds). Remaining: verify the other 7 doors via the same ledger lines during the user's next pass. **DIVISION OF LABOR (standing):** **History owns DATA+TRUTH** — WW facts (identities/params/members/placements/dialogue/decomp consults), ALL mod-folder config authorship (actor_map, manifests, doors.ini, dialogue), arc adaptation, audits (config↔arc, greplist, log forensics), root-cause diagnosis + fix SPECS. **Cursor owns ENGINE+BUILD** — resolver/ledger/joint_slave/door-actor/BG lifecycle/warp guards/dialogue service/audio guard implementation, builds + cache wipes, UI. **User owns** identity locks, doctrine rulings, acceptance. Rule of thumb: if it's a VALUE, History wrote it; if it's a BEHAVIOR, Cursor built it; if it's a NAME ON A FACE, the user said it. |
| **№40** | **№39 AMENDED (user caught it): the "green transit" was the WRONG DOOR — user pressed GRANDMA'S door (Link's house) and got Ojhous2.** History decoded Nintendo's own table: room.dzr SCLS = 10 exits ([0] LinkRM … [9] LinkUG basement) and all four KNOB00D doors carry IDENTICAL sentinel params (000ff0ff) ⇒ **destination is NOT in the placement params; the binding is positional — and `doors.ini`'s sections ARE correct** (Link's door = the high y=1275 row = `[linkrm]`, verified). **⇒ THE BUG IS ENGINE DOOR-SELECTION: user at (-201478,1275,320762) matched the `[ojhous2]` trigger ~5,000 units away. Cursor fix: bind each trigger to ITS spawned door instance — A-press selects THE DOOR PROP THE PLAYER IS FACING/NEAREST (max ~300u), never a global first-match scan; ledger already names the mismatch (`req=EXT_BG4` from Grandma's porch = the smoking line).** **VALUE CORRECTIONS (History, from the SCLS decode): (a) the forest door (KNOB03D) leads to stage `A_mori` — Outset's Forest of Fairies IS its own stage** (my earlier 'exterior upper forest' claim corrected; Omori/Forest-Haven shelving stands) — **`A_mori.arc` ADAPTED + in library; needs ext_bg9 manifest + [amori] door retarget.** (b) SCLS also lists `Pjavdou` (fairy fountain — entered from inside A_mori) and **`Cave09`** (the cliff cave — ADAPTED too) + `LinkUG` via interior hatch + Onobuta's SECOND spawn point (scls[5]) = its back door. Full authoritative door table now in the SCLS decode above. |
| **№41 MASTER OUTSTANDING QUEUE (answering "is everything communicated" — YES, now consolidated in ONE place; History value-debts CLOSED this entry):** | **HISTORY DELIVERED NOW:** `ext_bg9.ini` (A_mori manifest, dzb-derived spawn), `[amori]` door row (KNOB03D coords → EXT_BG9), **A_mori census extracted — AND IT CONTAINS THE MOBLINS: `mo2` ×4 + `Bk` ×3 + `gmos` ×3 + `kuro_t` swarms ×12 + `Zl1` TETRA ×2 (THE CRASH SITE — Q5's rescue beat lives in A_mori, not the exterior!) + P1a pirates ×2.** `Mo2.arc` + `Bk.arc` ADAPTED into the library — the run's founding quarry, placed by Nintendo, walkable soon (enemy actors spawn as static mounts v1; combat behaviors = audition-era; `Mo2_si` variant + gmos arc name = later). **CURSOR QUEUE (ordered):** 1. Verify dirty №36–40 play exe (FPS factory fixed 2026-07-19, field ~250): Grandma's door→LinkRM (facing-door select), `[Spawn]` ledger lines, heads present (E3 head-honor + joint_slave proof), Vlupy rupees, one Sue-Belle/Joel. 2. №38-E3 if not yet coded. 3. A_mori wiring (manifest+door are authored — engine just loads them) + its population (census above; enemies static-mount v1, `Zl1` crash-site rows stay `beat.tetra`-gated). 4. №33 dialogue unification (not yet confirmed shipped). 5. Quest spine → departure offer (cutscene-lite; Q5 rescue now sited in A_mori). 6. Residuals: Mesa's doorway trigger (no KNOB row exists — open doorway; place trigger during identity pass in-editor), interior hatches (LinkUG scls[9], Onobuta back door scls[5]), Pjavdou from inside A_mori, chest open behavior, Cave09 later. 7. M5b folder-absent smoke + M6 push-hygiene greplist gate. 8. Audio exemplar (Outset BGM via twin/redirect, size-guard now live). **USER QUEUE:** identity pass (Cycle+Lock), then the ONE Day-1 acceptance run. FPS factory = its own chat (respected, out of scope here). |
| **№42** | **A_MORI VALUES NOW MACHINE-READABLE (History closes its own gap — the census had been chat-prose only; that vacuum caused the guessing):** manifests authored: `npc_mo2.ini` (Moblin, `Mo2.arc/mo.bdl`, arg44, static v1), `npc_bk.ini` (Bokoblin, `bk.bdl`, arg45), `npc_lwood.ini` (WW tree, `Lwood.arc` adapted, arg46); actor_map gains `[mo2]/[Bk]/[lwood]`; explicit skip-list for unresolved arcs (kuro_t/gmos/Branch/swood — holes, no guessing); **sky-parked rule**: rows with y > host+20000 (Nintendo event parking, e.g. forest P1a at y=34707) ⇒ skip+log. **LAYER TRUTH from the census: day-1 forest (ACT0) = TETRA IN THE TREE + 3 BOKOBLINS — WW's actual opening fight; the MOBLINS live on ACT2/3 (later beats)** ⇒ `[Zl1]`'s GLOBAL `beat.tetra` gate REMOVED (it would have hidden the day-1 crash site; later-beat exterior rows stay gated via [layers]). **INCIDENT LOG (History error, self-reported): an encoding crash mid-write TRUNCATED actor_map.ini; fully REBUILT from preserved content (242 lines, 42 sections, verified) — lesson: UTF-8-explicit writes for all config authoring; if Cursor's in-progress build read the truncated file, REBUILD/relaunch against the restored one.** |
| **№44 SOCKET POOL DESIGN (History research — expansion is CHEAP and already precedented):** | **How minting works today (verified in source):** proc ids live in the `f_pc_name.h` X-macro list (~796 entries, ids to ~0x31A of a u16 space — 65,000 free); profiles resolve by INDEX-MATCHED array in `f_pc_profile_lst.cpp`; **the fork already appended three custom procs this way (ALBW_SHADE_WATCHER 0x318 etc.) — cost per socket = 1 enum line + 1 array line, compile-time, no dylink (statically linked), no save impact (mounts aren't save-persisted), greplist-safe with neutral names.** **THE DESIGN — `EXT_SLOT` POOL:** reserve a contiguous block `fpcNm_EXT_SLOT00_e..EXT_SLOT31_e` (32 to start), profiles macro-generated (`DEFINE_EXT_SLOT_PROFILE(n)` — each its own proc id, all sharing the mount's method table/class size). **Semantics: ONE SLOT = ONE PAYLOAD.** Manifests say `socket=AUTO` and the registry assigns free slots at scan (load-order, assignments logged) — or pin `socket=EXT_SLOT07` explicitly. **This structurally ends arg-multiplexing for new content** — the Ganondorf-is-arg-1 accident class becomes impossible (a slot holds one payload; a second claim = load-order top-wins + log, per A2). Retail-stub sockets stay with their historical tenants (MK=Ivan — the archaeology); bg payloads join the same pool (replacing KDK args). **Migration:** new content = AUTO immediately; the ~46 HENNA0-arg manifests migrate in ONE History data pass AFTER the current verify cycle (no churn mid-test). **Sizing:** 32 now; bump to 64 by changing the macro count when Windfall (W4) approaches — its town alone will want ~40. **Cursor build (small):** the enum/profile block + registry AUTO-assignment + `socket=AUTO` support; History then authors the migration pass. |
| **№45 HEADS: FIX DID NOT TAKE (History log-audit of Cursor's own post-rebuild boot 092849 — SAVING the user a wasted pass):** | Evidence: **67× `MISS:no_head_attach` — ALL folk (YM/YM2/YW/KO/KO2/OB) + LS (so Aryll's HANDS ride the same broken handoff)**; only 3 successes (p1a_head ×1, p2head01 ×2 — the early/unique spawns). Pattern = first-in binds, batch loses: **the pending-attach store still behaves single-slot/cleared-on-bind under the population lane's same-frame batched creates** — the race moved, not died. Also `src=unknown` on every ledger line (E2's requester field unwired). **REDESIGN (ends the class — pending handoff is the WRONG MECHANISM for static heads): heads/hands per census name are STATIC per (proc, arg) — so the mount should PULL, not be handed: at create-COMPLEATE, look up its OWN (proc,arg) in a scan-time registry built from actor_map sections (head_model/head_joint/companion) and attach directly. No transient handoff, no race by construction, batch-size-proof.** Keep pending ONLY for genuinely dynamic overrides (none exist yet). While in there: populate `src=` (census row name / door id / cutactors). Acceptance unchanged: zero `MISS:no_head_attach` for YM/YM2/YW/KO/KO2/OB/LS on a fresh warp; `[Spawn]` shows `head=<model>@head` for all six + `lshand` companion on LS. **User: hold the verify pass until this lands — the current build will still show headless folk.** |
| **№46 CRASH: SHADER EMITTER (History; log 094549):** | **Aurora's TEV→WGSL emitter generated invalid code — `.r` swizzle on a SCALAR f32** (`round(tev_overflow_f32(sampled0.a).r * 255.0)`) — while compiling a **TEV alpha-compare config (COMP_A8_GT: texture alpha vs register alpha) that TP content never exercises**; `CreateShaderModule` failed → crash. First-exercise class again, now in the RENDERER. Culprit correlation: died immediately after the FIRST-EVER renders of `NPC_ZL` (Tetra — newly ungated) and `NPC_P1` (large pirates); shader hash `d386602d549677f`. **Cursor, two fixes: (F1) the emitter swizzle — scalar returns must not get `.r` (likely one line in the alpha-compare emission path); (F2) RENDERER G-GUARD: a failed pipeline compile must SKIP-DRAW + log the material/model, never crash** — with WW materials incoming for the whole game, shader-compile failure must be survivable by design. Data mitigation available if needed for immediate testing: temp re-gate `Zl1`/suppress `P1a` (History can flip in one minute — say the word). **HEADS STATUS in this build: still the OLD mechanism** (ledger shows `MISS:no_head_attach` for ZL/P1 etc.) — the №45 pull-model redesign is NOT yet in; the one headed person the user saw = the pirate path (`p2head01`) working as before. №45 remains the heads fix of record. |
| **№47 ANIMATION + HEAD-SEAT (History diagnosis of the user's first headed pass — two engine defects, one data fix):** | **User report: rupees ✓, heads ON for all folk except the Rito; but heads float SLANTED ABOVE the bodies, and bodies stand in T-POSE until first interaction (talk wakes them).** These are two separate defects that compound: **(A) T-POSE = THE IDLE ANIM IS NEVER STARTED on population-lane spawns.** A T-pose is *no animation bound at all* (not frame 0 of a wait — wait frame 0 is a normal stance); talking force-binds the talk BCK, which is why interaction "fixes" them. Every folk manifest's `idle=` member is audited-present in its arc (№38). **Cursor fix: at create-COMPLEATE, bind+play the manifest `idle=` BCK (looping morf) on EVERY lane — population, door, cutactors — not just the interactive path; and keep the morf ticking every frame regardless of dialogue state.** The early socket exemplars idled correctly (Ivan did) — the population lane skipped the idle-start branch, that's the regression surface. **(B) SLANTED-FLOATING HEADS = WRONG ATTACH SEMANTICS — the head model's base TRMtx is being set AT the body's head-joint matrix. That double-transforms** (head models are authored at ROOT origin with their own internal `head` joint offset), producing exactly the observed float-above + joint-orientation tilt. **№36-A is the letter of the law and it is different: companion base TRMtx = the body's BASE TRMtx (same root as the body); then AFTER the body's anim calc each frame, for every companion joint whose NAME matches a body joint (`head`, `ls_handL`…), INJECT the body's anmMtx into the companion's joint via setAnmMtx. No other placement math.** That seats the head on the neck AND makes it track animation for free. Note the interaction: fixing (A) alone leaves heads slanted; fixing (B) alone leaves seated heads on frozen bodies — both land together, acceptance is one warp with everyone idling, heads seated, through a talk cycle (head must follow the talk anim too). **(C) QUILL (data, DONE by History): `[Bm1]` was the last `head_from_params` holdout — the pull registry rightly refused it. Census ACT0 params `00ffff00` low byte 0x00 → `bmhead01` now pinned in actor_map (zero-index→01-member, the Ym/Ko-proven ordering); Rito-postman-at-the-mailbox = Quill? (unverified — identity pass locks).** **(D) RESIDUALS logged, not this cycle:** menu-warp to LinkRM dropped Link in OPEN SPACE off the wooden floor (menu lane likely bypasses the door-pin + №37 ground-probe — route BOTH warp lanes through the same place-at-spawn+probe path; ferry the `[Doors]`/BG lines from that entry); Grandma's exterior door→interior still fails (№40 facing-door bind still unverified); ONE crash entering level-editor select mode over WW mounts (editor pick vs mount actors — separate chat owns the editor, defer). |
| **№48 HEAD SEAT v3 — NAME-MATCH REFUTED BY THE DATA (History JNT1 dump of every folk arc; №36-A's "match by name" generalization is WRONG and superseded):** | **User evidence: this build's heads sit ON THE GROUND at the bodies' feet (worse than the prior slant) — EXCEPT Rose, near-perfect. The JNT1 dump explains both:** `oba_head.bdl`'s single joint is literally named **`head`** — the ONLY head in the library whose joint name matches a body joint, so the name-match injection fired for her alone. **Every other head has a FOREIGN root joint name** — `ymhead01→ymhead1`, `ymhead02→ym_head02`, `kohead01→head2(+hanamizu1-3 snot)`, `kohead02→kohead02`, `ywhead01→head2(+hair1-3)`, `bmhead01→head02(+sidehairL/R)`, `p1a→p1a_head_all`, `p2head01→p2_head01` … — so injection never fires and the model renders at its base = the body root = the ground. Even `lshand.bdl` is foreign: `ls_handL/ls_handR` vs body `handL/handR` — NO companion in this library matches by name except Rose's. **THE MECHANISM (v3, final — covers every row of the dump with zero per-arc cases): each frame after the body's anim calc: M = body anmMtx of the actor_map `head_joint` (`head`); then REPLACE the companion model's ROOT joint (joint 0) computed matrix with M — setAnmMtx semantics, do NOT multiply the root's own local/bind transform on top — and recompute the companion's DESCENDANT joints from their locals under the overridden root** (that carries kohead01's snot joints, ywhead01's hair, bmhead01's sidehair). **History of the two failures, so the class closes: build N-1's slant = base-TRMtx × root-local MULTIPLY (the root's authored local transform rides on top); build N's floor-heads = name-match that never fires. REPLACE-at-root is the third and correct semantics.** **Multi-joint exception (the ONLY one): explicit per-joint maps — actor_map key `companion_slave_map=<companion_joint>:<body_joint>,…` — authored NOW for `[Ls1]` (`ls_handL:handL,ls_handR:handR`, `world_root`→body base). Precedence: if a slave_map exists, use it; else root-REPLACE at `head_joint`. (Name-equality needs no special case — Rose's `head` root gets the same result via root-REPLACE.)** **DEFERRED (logged, do not guess): Rito companions `bmarm` (real arms/hands — body `armL/armR` are stubs; likely map `armLloc:armL,armRloc:armR`, unverified) + `bmwing` (wing anchor joint unknown) — Quill ships head-only this cycle, sleeves and all; wings/arms = a later verified pass.** Idle-anim fix (№47-A) CONFIRMED WORKING by the user this same pass — animations play; do not touch that path while landing v3. |
| **№49 STRETCH = ENVELOPE SKINNING MISSED BY THE REPLACE (History; user pass on №48 v3 — heads ATTACHED ✓, mechanism right, pipeline stage wrong):** | **Evidence splits perfectly on the JNT1 dump: every stretching part is a MULTI-JOINT head** (Sue-Belle `head2+hair1-3`, Quill `head02+sidehairL/R`, Zill's booger `head2+hanamizu1-3` — which also drags his head down), **every clean head is SINGLE-JOINT** (ymheads, oba_head, kohead02). **Diagnosis: soft parts are ENVELOPE-SKINNED (EVP1/DRW1 — vertices weight-blended across joints). The v3 replace patches joint WORLD matrices post-calc, but the model's WEIGHTED/envelope matrix pool is built from the pre-replace matrices — so envelope vertices stay anchored at stale positions (ground/origin) while root-rigid vertices ride the head: the mesh stretches between them, and shared blended verts pull Zill's head downward.** **CURSOR FIX (v4 — a pipeline-stage move, not a new mechanism): the root/slave-map REPLACE must happen INSIDE the companion model's calc, at the joint-callback stage (TP's own nodeCB pattern — exactly where Nintendo did it in ls1), BEFORE the weighted-envelope matrix pool is built — then subtree recompute AND envelope build both consume the patched matrices. If the engine's J3D model has a per-joint calc callback, hook it; if not, re-run the envelope/weighted-mtx build after the replace. Acceptance: Sue-Belle's hair hangs at her head; Zill's booger dangles from his nose (Nintendo rigged it to drip — it should hang, not tether him to the earth); Quill's sidehair clean.** **(B QUILL'S ARMS — DATA NOW AUTHORED, engine already has the key): `[Bm1]` gains `companion=bmarm.bdl` + `companion_slave_map=armLloc:armL,armRloc:armR`** (the body's armL/armR are stubs; real arms/hands/sleeves live in bmarm — two mapped subtree roots, children recompute under them; loc↔stub pairing = 1:1 naming, unverified flag stands). Engine note: a row may now carry BOTH `head_model` AND `companion` — support both on one spawn. Wings stay deferred (no body wing joint identified — no guessing). **(C USER'S "what next" ANSWERED): stretch fix + arms = ONE Cursor pass (same subsystem, small); THEN DOORS (user's instinct is right — №40 facing-door bind + №47-D menu-warp ground probe are the last traversal blockers); talk-anim polish/part-swap-in-cutscene = later cycle, after doors.** |
| **№50 FOUR-FRONT PACKET (History; user's v4 pass — heads system 95% landed; bind-transform dump + Ko.arc census + tww d_a_knob00 decode):** | **(A ZILL'S SLANT — root cause found in the bind dump): `kohead01`'s root joint `head2` carries an AUTHORED bind rotation r=(-90°,-90°,0°) — the ONLY non-identity root in the entire library** (every other head root = identity, which is why everyone else seats perfectly under v4's REPLACE — and why Zill alone slants: REPLACE discards his authored rotation). **CURSOR v5 (final semantics, one-line change): PARENT-COMPOSE — companion joint world = body target-joint world × companion joint's own LOCAL BIND matrix.** Identity-root heads are numerically unchanged (no regression risk); Zill regains his −90/−90. Same rule for slave_map joints (their locals are identity/near-identity — bmarm, lshand unaffected). **(B KO IDENTITY — USER LOCKED, my swap reverted: Ko1 = ZILL (booger kohead01, confirmed right in play), Ko2 = JOEL — tww's "Joel & Zill" comment order does NOT map to proc order; History momentarily swapped the heads on that inference and the user caught it (Ivan rule vindicated again).** **(C JOEL'S BODY — the "copy of Zill's body" is Nintendo's authoring, and the fix is IN THE ARC: `ko02.bmt` = J3D MATERIAL OVERRIDE TABLE.** The kids share ONE `ko.bdl` mesh; Joel's distinct shirt = apply `ko02.bmt` materials over the same mesh. actor_map `[Ko2]` now carries `body_bmt=ko02.bmt` — **Cursor: BMT support on the mount body path** (TP's J3D stack has material-table machinery; check mDoExt/J3DMaterialTable before writing new code). **(D QUILL'S ARMS**: attached-but-T-pose is EXPECTED pre-v5 — `bmarm`'s joints are all identity-bind (authored straight out); under v5 parent-compose the arm subtrees inherit the body's `armL/armR` stub orientations AND ride the body BCK (body anims drive all 15 bm joints incl. the stubs) — arms should pose and move with idle after v5 with NO extra work. Rigid elbows = acceptable v1. "No arm collision" = WW-authentic (NPC collision is a body capsule, never limbs) — non-issue. **(E DOORS, tww d_a_knob00 decoded — the white-triangle mesh is explained): Nintendo's door is TWO models: `door.bdl` (1,792 bytes — the animated FRAME/CONTROLLER holding joint `DoorDummy`; the BCK plays on THIS) + `door_a..h` (the VISIBLE door geometry, drawn each frame at `setBaseTRMtx(door.bdl→getAnmMtx(DoorDummy))` — Nintendo's own line). The white triangle = drawing the bare controller (or an unslaved variant). Variant select: switch on shapeType, DEFAULT CASE = `door_a` (decomp's own default — Outset's sentinel-params doors take it; not a guess). Facing = `transS(pos) → YrotM(angle.y)` with census `ry` — no other facing logic exists.** **(F VOID ENTRY — DO NOT ITERATE BLIND (№36-E stands): the interior IS mounting (user sees the right room in the background) but Link's placement is off-shell. Ferry the `[Doors] enter` + `BG COMPLEATE` + `ground probe` log lines from ONE Grandma's-door attempt — the №37 probe chain names the bad transform in one line.** Head-anim future-polish note (logged, later cycle): heads have their OWN BCK sets synced by name (`kohead01_wait01` ↔ `ko_wait01`, `susuri` = the sniffle, `.btp` blinks) — the full Nintendo head system is animated, not just slaved. |
| **№51 DOOR LOG VERDICT (History; log 113148 — THE VOID CLASS IS DEAD; what remains is №40, still alive):** | **The one door transit in today's logs is FULLY GREEN end-to-end:** `[Doors] req=EXT_BG4 resolved=Ojhous2 spawn=(-53,40,-227)` → `dooropenadoor.bck` played on the door prop → pinned BG warp → `BG COMPLEATE EXT_BG4 host=(-76000,2000,-90000)` → `Link placed on BG spawn (-76053, 2050, -90227)` (ground-probe hit, y adjusted 2040→2050) → `20 actors spawned, 0 skipped`. **Every guard in the chain fired correctly. The "void" failure class does not exist in this build.** **THE ACTUAL REMAINING BUG IS №40, UNCHANGED: the user pressed A at GRANDMA'S door and the resolver bound `door='ojhous2'` — Sturgeon/Orca's house, the same distant-trigger steal as before.** The transit then works perfectly… into the wrong house. doors.ini remains verified-correct data. **CURSOR FIX (the one remaining door work item): A-press must bind to the DOOR PROP INSTANCE the player is FACING/nearest (≤~300u) — the props are spawned and known (`[ExtNpcDoors] spawned 5 Knob.arc door props`); resolve the trigger FROM the matched prop, never a global trigger scan. Add `dist=` and `prop=` to the `[Doors] req` ledger line so any future mismatch names itself.** **(USER'S HYPOTHESIS — half right, and the right half matters): the missing native prompt/animation did NOT cause the void (the log proves the warp chain green) — but the reason a distant trigger CAN steal the press is exactly that entry is trigger-scan-driven instead of door-prop-driven. Binding through the faced prop fixes selection now AND is the natural mount point for TP-native door interaction (prompt + Link's open animation + camera) as a later polish pass.** Side-note: the exterior's `4 skipped/unmapped` = the known kusa/flower holes (№34-P1), working as designed. |
| **№52 THE "VOID" IS A STUCK FADE + mailbox/door forensics (History; log 114650 + TGDR byte-audit):** | **(A THE BLACK ROOM = A FADE THAT NEVER LIFTS — the room is FINE.** Evidence: the transit is green END TO END — `door='ojhous2' dist=87` → 3 interior models loaded (16 mats) → `BG COMPLEATE` → `ground hit y=2050` → `Link placed (-76053,2050,-90227)` → **20 actors spawned incl. Ji1 (Sturgeon HIMSELF is standing in that dark room)**; the user's editor-camera readout matches Ojhous2's host exactly; UI renders at 284 FPS. The MENU lane completes its fade cycle (user SAW LinkRM through it); the DOOR lane's pinned warp fades out at A-press and **nothing ever fades back in.** **CURSOR FIX: after `Link placed` on the door lane, drive the same fade-in the menu lane uses; PLUS the completion of the №34-P4 G-guard: if control is enabled AND the screen is still faded AND placement completed → force fade-in + log.** The placement void died in №51; this is the last void, and it's cosmetic-lifecycle, not spatial.) **(B DOOR BIND VERDICT: №51 actually RESOLVED CORRECTLY this session** — the one entry was the free-standing door near the mailbox flat, and it correctly bound `'ojhous2'` at dist=87: **that door stands at Nintendo's EXACT TGDR position (-205170.5, 720.0, 316800.7) — History re-parsed room.dzr TGDR this session and ALL FIVE doors.ini rows are byte-identical to Nintendo's table (pos + ry).** The 4 preceding refusals were honest >300u presses. `prop='?'` = the prop key-stamping is unfinished — finish it so the ledger names the prop. **GRANDMA'S DOOR WAS NOT ACTUALLY TESTED this session** (no `'linkrm'` resolve appears in any log) — it's the acceptance case next pass.) **(C "MAILBOX REPLACED BY DOOR" = TWO UNRELATED FACTS, no replacement happened:** (1) the free-standing door near the mailbox spot is the LEGITIMATE ojhous2 TGDR door (above); (2) the mailbox itself didn't spawn because the engine reported `arcs/Toripost.arc missing — socket idle` — **but the arc IS PRESENT AND VALID** (in arcs/ since 00:58 today, RARC verified, `vpost.bdl`+`post_wait.bck`+2 more members confirmed; manifest `npc_tpost.ini` values all correct). Same boot even registered `provider NPC_TPOST ← arc=Toripost` successfully BEFORE the missing-file claim — **the existence check is looking in the wrong place (path-join bug). CURSOR: fix the arc-presence check to use the same resolution the provider registration used.** When it lands, mailbox and door will stand there together, as WW authored.) **(D USER'S QUESTION ANSWERED: the black room was OJHOUS2 (Orca/Sturgeon's), not Grandma's** — camera (-76062,2130,-90226) ≡ Ojhous2 host (-76000,2000,-90000); the door you pressed delivered exactly where it should. |
| **№53 NATIVE DOOR SYSTEM (USER-DIRECTED work order; History forensics of log 120722 + host audit — this replaces the trigger/prop hybrid that has now failed three different ways):** | **FORENSICS FIRST (what the log names): (1) KEY SCRAMBLE — the prop at GRANDMA'S PORCH is stamped `'ojhous2'`** (user at Grandma's, resolve `dist=83.7 prop='ojhous2'` → warped to Orca's room; TGDR/doors.ini positions are byte-audited clean, so the SPAWNER paired keys↔positions from two different iteration orders). **(2) Only ONE of 5 props registered for attention** (9 presses at other doors = `no doorAttention prop within 300u` refusals with the doors visibly RIGHT THERE). **(3) Watchdog fired but SPAMS every frame — make it one-shot. (4) EXIT drops Link into the SEA: all interiors are parked in a 2,000u row off the coast (hosts -70000…-88000, island at -60000) — user's screenshots show Ojhous2 and Onobuta (Rose + kid visible) as adjacent sea-islands; walking out of a shell = swimming home. (5) Mailbox CONFIRMED FIXED.** **THE WORK ORDER (user's design, ratified — TP native interaction wrapped around the pinned-warp backend): (A) PLAYER SIDE: use TP's OWN knob-door flow** — d_a_alink has native KnobDoor/door-open procs (TP Link opens Ordon house doors; grep alink for the proc set) + the native attention/demand category so the prompt reads door-open, not Speak. **(B) DOOR SIDE: the Knob prop becomes a real door interactible:** demand start → `dooropenadoor.bck` on the controller (already working) + Link's native open-and-step-through demo → **pinned BG warp fires mid-black (this backend STAYS — TP's stage system cannot load WW stages; the BG mount IS the room loader)** → place at interior spawn (spawn_rel + ground probe, already green) → one-shot fade-in → release control. **(C) EXIT IS A FIRST-CLASS DOOR:** interior-side door interactible at the room's own door position → same native flow → warp back to the exterior at doors.ini `ww_pos`, facing `ww_ry` outward + a step. **G-GUARD: if Link leaves the interior shell's AABB without a warp (the water case), teleport him to the exterior door — swimming home must be impossible.** **(D) FIX THE SCRAMBLE STRUCTURALLY: spawn prop + stamp key + register attention in ONE loop over ONE array (doors.ini file order); assert 1:1; boot-log all five: `[Doors] prop key=<section> pos=<xyz>` — any future scramble self-names. (E) PARKING PLAN (History-authored values — land WITH the exit-warp in the SAME build, never before, else far-sea softlock): move interior hosts 150k offshore, spaced 25–30k:** BG1 LinkRM (-60000,2000,-240000) · BG2 LinkUG (-30000,2000,-240000) · BG3 Ojhous (0,2000,-240000) · BG4 Ojhous2 (30000,2000,-240000) · BG5 Omasao (60000,2000,-240000) · BG6 Onobuta (90000,2000,-240000) · BG7 Omori (120000,3000,-240000, shelved) · BG8 Pjavdou (150000,2500,-240000) · BG9 A_mori (200000,0,-240000). WW-INTERNAL positions (spawns, placements, door offsets) unchanged — hosts are engine bookkeeping, not WW data; the law is untouched. History flips the manifests on Cursor's go-signal (one minute), or Cursor flips them in-build — values above are final. |
| **№54 DOOR FORENSICS ROUND 2 (History; log 122947 + interior PLYR extraction — THREE History data bugs found and FIXED, two engine items remain):** | **WINS FIRST: native OPEN prompt LIVE ✓; prop keys stamped correctly 1:1 ✓ (boot audit clean — the scramble is dead); A_MORI WORKS END-TO-END ✓ (user walked the day-1 forest: Tetra + Bokoblins at the parked host — the run's first fully-native WW stage transit); G-guards fired honestly on every failure (no voids, no strands). FORENSICS + FIXES (data, ALL AUTHORED THIS ENTRY): (1) "GRANDMA'S → ORCA'S" WAS HISTORY'S PAIRING BUG, not engine:** with keys clean, the user's presses resolved exactly the prop they stood at — **the door the user calls Grandma's (by the mailbox — WW canon agrees: the postbox stands at Link's house) was paired to Ojhous2 in doors.ini. RE-PAIRED: `[linkrm]` = the mailbox door (-205170,720,316801); the y=1275 hilltop door (900u from Rose's spot) = `[onobuta]`; the two mid-village doors (550u apart, 250u height apart = the Orca+Sturgeon two-story house) = `[ojhous2]` lower y=425 (Ojhous2.arc IS the sumo-ring room = Orca, ground) + `[ojhous]` upper y=675 (Sturgeon) — that last split `? (unverified)`, one-swap if the sweep disagrees. `[amori]` untouched (user-verified). (2) ONOBUTA'S `no_ground` ABORT = History's fantasy spawn point.** Interior spawns are Nintendo's PLYR entries selected by the SCLS spawn id (**PLYR chunk index = spawn id — proven by Onobuta: [0]=front ground door, [1]=back door at y=638, exactly as SCLS lists spawn 0/1**). **ALL 8 interior manifests now carry Nintendo's PLYR spawn + new `spawn_ry` key** (LinkRM (-289,375,83)@-32768 · LinkUG (349,0,-34)@-16384 · Ojhous/Ojhous2 (1,0,626)@-32768 · Omasao (127.3,0,625) · Onobuta front (301.7,0,620.3) [back: (150,638.2,325) for the scls[5] hatch later] · Pjavdou (0.2,-51.5,-2499.3) · A_mori PLYR[0] (1398.9,3659.6,-2063.9)@17658). **(3) OJHOUS `no_compleate` ABORT = History parked it at x=0** (engine reads 0 as unset) — **host moved to (10000,2000,-240000).** **ENGINE ITEMS REMAINING (Cursor, small): (4) ROOF PLACEMENT: every fail-safe/exit return probed from the SKY and landed Link on rooftops** (exit from Orca's → y=5339 = 619 ABOVE the door; both aborts placed on rooflines). **Rule: return placement = door pos + outward step (~150u along ww_ry, away from the wall) + probe from door_y+50 DOWN, clamped to ±250 of door_y — never a sky probe. (5) EXIT PROP PLACEMENT: `exit prop key=exit:ojhous2 pos=(spawn)` put the door INSIDE THE SUMO RING (the user's "Orca's spear") — place it at the spawn's WALL: spawn pos MINUS ~120u along spawn_ry facing (behind the player's landing), rotated to spawn_ry, using the new manifest `spawn_ry`. Also wire the exit key into the OPEN ledger line (10× `native OPEN → prop='?'`).** Consume `spawn_ry` for Link's facing on entry too (he should land facing INTO the room, as WW authored). |
| **№55 COVENANT AUDIT RATIFIED — WITH EXPANDED SCOPE (History critical review of the security chat's baseline; both findings REAL, both UNDERCOUNTED, two sites MISSED):** | **The hard gate is EXE-STRING CLASS: string literals that compile into the binary. Verified surface (History grep, this session): (L1 Aryll)** `editor.cpp:3251` help-text literal `display_name=Aryll` — ratified, one-line neutralize (e.g. `display_name=<name>`). **(L2 Vlupy — WIDER than reported: FIVE literals, not two:** `d_ext_npc_population.cpp` :399 strcmp, :406 hasPayload, :411 lookup, :424 pushPendingSpawn, :431 log — **plus** `d_ext_npc_mount.cpp:3358` log `"Vlupy pickup"`. **Fix direction ratified and extended:** `me.proc` already arrives resolved from actor_map (data), so the branch keys on the manifest flag (`man.pickupRupee`) and every call site uses `me.proc` generically — zero literals remain; logs print `me.proc`. `vlupyColorFrame` fn name = comment-class, optional rename.) **(MISSED-1: `d_ext_npc_doors.cpp` :437/:457 hardcode `"dooropenadoor.bck"`** — a WW anim member name shipping as exe bytes ×2; the manifest field for this ALREADY EXISTS (`doorOpenBck`, read at mount.cpp:3812) — route both call sites through the manifest, delete the literals.) **(MISSED-2: same file :723/:750 log literals name `Knob.arc`** — neutralize to "door arc".) **(EXEMPT after review:** Bokoblin/Moblin/Stalfos strings in cut-actor/flag/settings UI = TP's OWN species/flag vocabulary (TP has Bokoblins; E_OC labels are TP-decomp archaeology predating this run); alink `procDoorOpen`/particle `DoorOpen*` = TP native.) **(POLICY RULING NEEDED FROM USER — write the comment-class rule:** source COMMENTS naming WW things (Outset/A_mori/kohead/bmarm/Quill across mount/population/doors) do NOT compile into the exe and are the same class as the repo's docs, which already name everything freely. **Proposal: comments = allowed (docs-class), string literals = hard gate; the greplist gate scans literals + shipped tree, excludes docs/tools/comments — otherwise the gate drowns in its own noise.** If the user wants comment-purity too, that's a separate mechanical sweep, ~20 sites, zero risk.) **(GREPLIST COMMIT: History recommends YES** — it's config text (docs-class, no WW *assets*), and until committed the gate can't travel to CI or other machines; scope note above goes in the gate script.) **(M5b folder-absent smoke: user-driven; run it AFTER the №54 door build verifies, not mid-cycle.)** **✅ FIXES LANDED (History, user-directed, this session): editor.cpp example → `display_name=RealName`; population pickup block → `man.pickupRupee` flag + `me.proc` everywhere (all 5 literals gone; legacy TP_RUPEE alias dropped — actor_map is current); mount pickup log neutralized; doors.cpp ×2 → `playAnimNearest(..., NULL)` with manifest `door_open_bck` driving (npc_knob.ini carries it) + NULL/empty guard added in playAnimNearest; Knob.arc log strings ×2 neutralized. Post-fix grep: ZERO shipping WW literals in src/ — one quoted word remains inside a comment (population.cpp:81), comment-class, awaiting the user's ruling. NOTE: these edits need the NEXT rebuild to enter the exe.** |
| **№56 THE COMPLETE WW TRANSITION GRAPH (History — user-requested research, extracted from primary data; THE DOOR SYSTEM'S REMAINING VALUES ARE NOW NINTENDO'S OWN):** | **SPAWN-ID DECODE (the user's cutscene theory CONFIRMED BY THE BYTE):** PLYR entries carry their spawn id in the aux word @0x1C low byte (index ≠ id — №54's index assumption struck). **LinkRM spawn id 1 = (-255,0,1125) — GROUND FLOOR, EXACTLY co-located with the interior `KNOB00` door actor**; the upper-landing spawns are EVENT spawns (ids 0/200/201/202 — the `TagEv` cutscene triggers sit right beside them). **EVERY house interior has a `KNOB00` ACTR = the room's real door, and a PLYR standing on it** (LinkRM (-255,0,1125) · Ojhous/Ojhous2 (1,0,626) · Omasao (127,0,625) · Onobuta (301.7,0,620.3)). **RETURN GRAPH (interior SCLS → sea spawn id → Outset PLYR, all verified co-located with the porches):** LinkRM→1 (-205170,720,316800) ry=20206 · Ojhous→3 & Ojhous2→3 **(BOTH exit the LOWER door — WW canon: Sturgeon's room is reached by the INSIDE ladder from Orca's; the user's "Sturgeon's door → Orca's room" transit is CORRECT behavior, not a bug)** · Onobuta front→4 (-201478,1275,320762) / back→5 (-201620,1986,321025) · Omasao→7 (-193413,700,318686) · A_mori→8 (-200283,3696,321947) summit · Pjavdou→9 beach · Cave09→10 cliff · LinkUG→11 (-205596,580,316562) beside Grandma's. **(MESA CORRECTION, user-caught): KNOB03D = MESA'S DOOR** (Omasao's return spawn sits exactly on it) — doors.ini `[omasao]` now targets EXT_BG5; **the real forest entrance = the summit WALK-THROUGH passage** (no knob in WW) — new `[amori]` section at (-200243,3700,321929) r=300 with `knob=0 walkthrough=1`. **DATA AUTHORED THIS ENTRY:** ext_bg1 entry spawn → the door spawn (-255,0,1125); ALL manifests gain `return_pos`/`return_ry` (WW world, the Nintendo porch returns above) + `exit_door_rel` (the interior KNOB00 position — the exit knob goes HERE, ON the room's real door, not at the landing offset). **CURSOR (№56 engine, small): (a) exit knob spawns at `exit_door_rel` (kills the fake-door/real-door split — one door, functional, where the room's door IS); (b) exit warp targets `return_pos`/`return_ry` (replaces computed porch); (c) `walkthrough=1` support — crossing the radius triggers the transition, no A-press, both directions (enter at [amori] gate; A_mori-side exit zone at its return-adjacent edge); (d) Grandma's "spawns at wrong spot" is fixed by DATA (a rebuild-free manifest change) — entry now lands at her real door.** LOOSE END (logged): the UPPER mid-village door (-203060,675) has return spawn id 2 but NO interior SCLS references it — likely locked/decorative in WW; our [ojhous] mapping on it stays as an adaptation convenience, `? (unverified)`. Pjavdou note: A_mori's fairy fountain SCLS points at stage `Fairy04` (not Pjavdou) — Pjavdou is the beach-side fountain; W1 scope unchanged. |
| **№57 USER CANON CORRECTION (Ivan rule in action) + CAVE GATE:** | **USER CORRECTED №56's ladder claim: the Orca/Sturgeon ladder is EXTERIOR — it connects the two exterior DOORS on the island; the rooms are NOT internally connected.** Sturgeon's room exits at the UPPER door, full stop. **№56's "both rooms exit the lower door / inside ladder" reading is STRUCK.** Honest data note kept on record: the extracted interior SCLS bytes read `sea spawn=3` for BOTH Ojhous and Ojhous2 — either a genuine WW data quirk or a parse subtlety (re-verify someday against tww's dzx reader); irrelevant to the build because our engine consumes History's authored `return_pos`, which now follows user canon: **ext_bg3 (Ojhous/Sturgeon) `return_pos` → the UPPER door (-203060.4, 675, 318175) ry=-32768; ext_bg4 (Ojhous2/Orca) stays at the lower door.** Entry pairing already matched canon (upper→Sturgeon, lower→Orca) — the `? (unverified)` on that split is hereby CLEARED by the user. **CAVE GATE (user-confirmed walk-through class): doors.ini `[cave09]` authored** at the west-cliff mouth (-208619, 2350, 317732) r=300, `knob=0 walkthrough=1`, + **`ext_bg10.ini` manifest authored** (Cave09.arc, parked at (240000,2000,-240000), entry PLYR[0] (-800,400,0) flagged verify, return = Outset PLYR id 10). **Inert until Cursor mints the `EXT_BG10` socket — the №44 two-liner (1 enum line + 1 profile line); fold into the №56 engine pass.** Note: Cave09's own SCLS[0] points to `Cave09 room 1` — it's a two-room cave (internal hop) — v1 mounts room 0 only, interior hop queued. **USER CANON ADDENDUM (locks the Orca/Sturgeon geography for good): Sturgeon's room (Ojhous) is itself a TWO-FLOOR interior with its own INTERNAL ladder — but it does NOT connect to Orca's room. Matches the data: Ojhous's dzr carries a climb surface (`Akabe10` @ y=233) above its single ground-level KNOB00 door. Summary: two separate one-door interiors; the EXTERIOR ladder links their two exterior doors; Sturgeon's interior ladder links his own two floors. No change to authored data.** |
| **№58 STURGEON'S ROOM IS A MISSING ARC + the mash decoded (History; sweep logs + dzr census diff):** | **WINS ON RECORD: Grandma full loop ✓, Mesa ✓, Rose ✓, forest WALK-THROUGH CLEAN ✓, returns to own porches ✓ — the door system core is DONE. (A) STURGEON — the routing is INNOCENT (`req=EXT_BG3 resolved=Ojhous prop='ojhous'` = clean transit); the CONTENT is the bug: full dzr census diff proves `Ojhous.arc` AND `Ojhous2.arc` are BOTH ORCA'S HALL — same Ji1(Orca) at (0,0,-299), same pots/stools/tables/KNOB00; they differ only by the `Akabe10` climb-wall + shelf arrangement = two STATE VARIANTS of one room. STURGEON'S ROOM IS NOT IN THE LIBRARY (57 arcs audited). WW ships it as stage `Ojhous2` ROOM 1 (Outset's own SCLS says `room=1`); our extraction grabbed a room-0 variant twice. ACTION — USER: point History at the WW extract (or drop `Stage/Ojhous2/Room1.arc` — also grab `Ojhous/Room*.arc` siblings while there); History adapts + installs + rewires EXT_BG3 in minutes. Until then the upper door unavoidably shows Orca's hall.** **(B) THE "MASHING" = COLD-MOUNT TIMEOUT, decoded from the ledger:** first press → `ABORT reason=no_compleate after 180f` (parked interior resLoads cold, >3s), fail-safe returns Link, second press hits the now-cached arc and succeeds. **CURSOR FIX: WARM the interior mounts in the background right after island COMPLEATE** (they're permanent mounts; doors become instant, single-press) — plus while a resLoad is honestly in-flight, HOLD the fade instead of aborting at 180f (abort stays for true failures). **(C) ARCHITECTURE — user's read VERIFIED CORRECT:** our transition = PHYSICAL RELOCATION within one persistent world behind a cosmetic fade (pinned BG warp) — NOT a TP/WW scene change (no stage reload, no event-locked walk-through). This is №53's ratified design (TP's scene system cannot load WW stages). The remaining "clean one-press TP/WW feel" = (B) warm-up + finishing №53-A's demo lock (Link's open anim with control locked through the fade). **(D) DOOR-CLASS TAXONOMY (user directive, adopted): extra in-room doors are NOT all entrances — classes: `knob` / `walkthrough` / `crawlthrough` / `dropthrough` (+ladder), plus cutscene-spawn markers (PLYR id≥200). Interior hatches queued under this taxonomy: LinkRM→LinkUG (dropthrough, scls[9]), Onobuta back door (scls[5]), Cave09 internal room hop. Cookbook §4 updated.** |
| **№59 STURGEON INSTALLED + FURNITURE PASS (History — the extract location unblocked everything; ALL DATA, no engine work needed):** | **THE EXTRACT: `D:\XXXXXXX\Ex WW` (user-recorded in memory + here — NEVER guess this path again; WW stages keep rooms as separate `Room*.arc` files — a stage's room 0 is not the stage).** **(A) STURGEON'S ROOM: `Stage/Ojhous2/Room1.arc` census-verified — `Aj1` = STURGEON** (the room is FULL of `Paper` charts — the old navigator's study; two-floor with shelves at y=250–720; `Yw1` Sue-Belle visits in a later state) **and its own SCLS returns to `sea spawn=2` = THE UPPER DOOR — Nintendo confirms the user's canon exactly; the №57 "spawn-3 oddity" is fully RESOLVED (both library arcs were Orca-hall state variants, correctly returning to the lower door; no quirk, wrong arcs).** INSTALLED: adapted → `arcs/Ojhous2R1.arc`; `ext_bg3.ini` rewired (arc, spawn ON its interior KNOB00 (546,-20,327)@-32768, `exit_door_rel`, return = upper door); 59 census rows appended to `interior_placements.csv` as stage `Ojhous2R1`. **(B) FURNITURE PASS (the interiors' missing props, sourced from the extract's `res/Object/`):** `Otana.arc` (shelf) + `Table.arc` (`ytble` table + `qcfis` fish) + `Plant.arc` (`yrmwd`) + `Opaper.arc` (Sturgeon's charts) + `Lamp.arc` (BMD passthrough) — ALL ADAPTED into the library; manifests `npc_otana/otble/plant/opaper/lamp.ini` authored (HENNA0 args 50–54, static props); actor_map `[Otana]/[Otble]/[Plant]/[Paper]/[Lamp]` mapped. These fill holes in EVERY interior (Orca's stools/tables, Grandma's shelves, Sturgeon's charts). **HOLE kept honestly: `Ostool` — no stool-named arc in the extract's Object dir and not inside Table.arc; awaits a tww `d_a_obj` profile consult for its resource name. `swood` stays on the №42 skip list.** NOTE: `Aj1` was already actor-mapped (exterior) — Sturgeon spawns with head rules per manifest; his `unique=1` now spans exterior+interior instances — if the interior copy is suppressed by uniqueness, that's the first thing to check in the log (`[Spawn] src=census:Aj1`). NO REBUILD NEEDED for any of this — data lane only; the next boot picks it all up. |
| **№60 TRANSITION FEEL RULING — ★USER RATIFIED 2026-07-19 ("sounds right, especially for the long term goal") — STANDING SPEC★ + CLASS BEHAVIOR SPEC (the doc-of-record for hatch/gate work):** | **RULING: TP mechanism + WW content — because TP's and WW's door UX are the SAME seven-beat skeleton:** (1) prompt → (2) INSTANT control lock → (3) Link's door-open anim + door swing anim/SFX → (4) screen cover mid-anim → (5) placement inside facing in → (6) fade lifts WHILE Link auto-walks 2 steps + door-close behind → (7) release. **"Feels right" = beats 2/3/6 — the lock, the hand-on-door, the walk-in.** TP supplies: `procDoorOpen`/`procKnobDoorOpenInit` (already in the port's alink — Ordon's own flow), attention, camera settle, lock. WW supplies: door visual+swing BCK (live), Nintendo spawn/returns (authored), WW door SFX later via audio lane (purity: SILENCE beats a TP creak in a WW house). Pinned-warp backend stays underneath — proven, and no scene loader here can host WW stages. **PER-CLASS BEHAVIOR (not one flow with flags): knob** = full seven beats. **walkthrough** = no prompt/lock; plane-cross → quick fade, heading preserved (forest LIVE ✓; cave on EXT_BG10). **crawlthrough** = gate fires ONLY while crawling (hold-A crawl shipped); enter crawling, fade mid-crawl, emerge crawling — **first case: LinkUG is canonically a crawlthrough FROM OUTSIDE (return spawn 11 at ground level beside Grandma's; LinkRM's SCLS has NO basement link — the under-stilts crawlspace to the storage room is WW's design).** **dropthrough** = downward hatch interaction (Cave09 room hop); return climb = TP native ladders once WW dzb ladder attrs map (deferred research stands). **Cutscene spawns (PLYR event ids)** = markers, never transitions. Data keys: doors.ini knob/walkthrough flags today; `crawl=1`/`drop=1` when those gates are built. **BRIDGE RUN: observed only — History not in lane; standing obligation noted: keep all History extraction scripted/reproducible (cookbook + scratch scripts) since the bridge productizes this exact pipeline.** |
| **№61 DOOR CHAPTER: CORE COMPLETE — residuals named (History; user pass + logs 143020/143206/143345):** | **MILESTONE ON RECORD: STURGEON'S STUDY LIVE (Sturgeon + Sue-Belle spawned in it) — ALL house-specific doors on Outset deliver their own rooms; forest walkthrough clean. The door system's core is DONE.** Residuals, each named: **(1) MESA "WALKTHROUGH" MOMENT = History data bug, FIXED:** the `[omasao]` row inherited `radius=280` from the old forest-gate section — ledger shows the trigger armed at `dist=250` (intentional presses log 80–100) = door fired on approach. Radius normalized to the knob standard 240. **(2) TWO-PRESSES-SOMETIMES + WRONG-ROOM/VOID RACES:** the user's diagnosis is correct in mechanism ("the room doesn't load fast enough for the physical move — we need natural loading"). **№58-B (warm interiors + hold-fade + demo lock) IS in the newest build per the status row** — so if the residual persists on the NEXT pass, the remaining window is pressing a door BEFORE the background warm finishes (or a warm that evicted/never covered that mount). **USER: on the next double-press or void, ferry the surrounding `[Doors]`/`resLoad`/`COMPLEATE` lines — the hold-fade path should make a slow load LOOK like a long door (black held), never a void; a void sighting post-№58-B = the fade lifted early somewhere, and the log will name it.** **(3) CRAWL/DROP GATES UNVERIFIED (expected — not built yet):** user located **Mesa's interior crawl hole** (screenshot @ EXT_BG5 host — the wall gap) — first interior crawlthrough case alongside LinkUG's exterior crawlspace; both await the №60 crawl-gate build. **(4) "Jabun walkthrough" (the sealed cave behind Outset's waterfall) — user-deferred, logged for the quest-spine era.** |
| **№62 NATURAL LOADING — THE ARCHITECTURE PROPOSAL (user-driven; ends the physical-move era; needs Cursor feasibility before it becomes a work order):** | **The user's challenge is correct: the pinned physical move is the buggy foundation, and BOTH source games agree on the alternative.** TP Ordon: village stage + separate interior stage (one house per ROOM); every door = REAL scene transition (village unloads, room loads, lighting reinits, fade covers an actual load). WW Outset: sea room 44 + interiors as mini-STAGES via SCLS+spawn ids (the №56 graph) — same real load/teardown. **Neither game ever co-hosts exterior+interiors; our current build co-hosts EVERYTHING and teleports Link between mounts — the load races, offshore parking, swim-between-rooms, leave-shell guards, and warm-up band-aid are ALL symptoms of that one choice.** **PROPOSAL (primary): ROOM-PER-INTERIOR — assign each WW interior a room slot of the host stage and ride TP'S OWN ROOM STREAMING (the dungeon-door pattern: lock player → request next room load → engine waits natively → open). Mounts bind to their room's load/unload lifecycle instead of eternal residence. (Variant for big spaces (A_mori, future islands): stage-per-area via real changeScene into a host stage that auto-mounts its WW BG at create.)** Retires structurally: parking table, warm-up, AABB exit guards, fade-race class; beat 4 of №60's seven beats becomes a genuine load wait — the "natural loading" the user named. Terminology pin: TP LAYERS = story-variant sets (we already use those for census gating); TP ROOMS = the streaming/memory unit — this proposal is about ROOMS. **OPEN QUESTIONS FOR CURSOR (feasibility pass, no build yet): (1) adding room slots to the host stage's room table (level-editor/Aurora precedent says shipping-stage edits are unblocked); (2) hooking mount create/destroy into room load/unload callbacks; (3) door actors driving TP's native room-change request path (door-shutter pattern) with our doors.ini as the data source; (4) migration order (doors keep working during the transition — pinned warp stays as fallback lane until room lane proves on ONE exemplar interior, per one-exemplar vetting).** USER RATIFICATION + Cursor feasibility both required before Phase D. **ADDENDUM — "why not port WW's loader?" (user asked; settled): №62 IS WW's loading reused — as DATA/behavior (the №56 exit graph: stage+spawn+room+fade-type per doorway, executed verbatim); porting WW's loader CODE fails four ways: (1) systems-don't-port — WW's scene core entangles WW's globals/lighting/memory = a second engine core inside TP; (2) COVENANT — WW-derived code compiled into the receiver = WW content in the exe, poisoning distributability; (3) the BDL lesson at 100× — untested decomp scene code vs TP's every-second-battle-tested streaming; (4) the GREAT SEA endgame favors TP's lane — WW's sea streaming IS room streaming, and a ported WW loader would still need TP rewiring underneath. WW-only loader features (circle-wipe visuals, fade flavors) = cosmetic content, recreatable in WW spaces later; fade types are already per-exit data. №62 = №60's ratified ruling applied to loading: TP mechanism, WW content.** |
| **№63 LIGHTING + CUTSCENES — DIRECTION PRE-RESEARCH (cursory by user request; both follow the №60/№62 pattern: TP mechanism, WW content):** | **(A LIGHTING — never call WW's kankyo code; FEED on its data.** WW's look = per-stage environment PALETTES (ambient/light/fog per time-of-day, chunked in stage files), blended by WW's kankyo at runtime. The code is systems-don't-port + covenant-hostile; the palettes are pure extract data — and our WW draw branch already has the plug points (the per-mount fixed ambient + WREG trim IS the socket). **Upgrade ladder: fixed ambient (today) → Outset's authored DAY palette extracted from the sea stage's env chunks → full time-of-day blending driven by TP's clock.** Each rung = data + small engine. Future History task: parse EnvR/Colo/Pale from the extract's sea Stage.arc.) **(B CUTSCENES — the shape is decided by precedent: WW's `event_list.dat` interpreter does NOT port (same four-point rejection as the loader); instead TP DEMO PRIMITIVES (camera control, actor overrides, control lock, fades, №33 native dialogue, item grants, flags — all existing) consume HAND-AUTHORED BEAT SCRIPTS in the mod folder, with WW's event_list.dat entries EXTRACTED as the authoring bible per beat** (who's on stage, camera, timing — History extraction task when the era opens; dialogue from the 3,539-line pull). **One-exemplar vetting: first cutscene = a small interior beat — Grandma granting the shield (one room, two actors, dialogue, grant, one flag) — BEFORE flyovers/departures.** DuskScript (code-mods research) = the eventual scripting vehicle; beat scripts stay data until then. User's stated goal ("beat-for-beat via TP demo machinery") = exactly this lane. NO work spawned now — Cursor stays on №62 feasibility; this entry exists so the era opens with direction already settled. |
| **№64 EXEMPLAR VERDICT: THE ROOM LANE IS PROVEN — two wiring gaps, one root cause (History; user report on LinkRM→R02):** | **MILESTONE: the natural-loading transition WORKED on first exercise — №62's architecture is validated; the physical-move era's replacement is real.** Both failures share one root: **the R02 lane invented its own placement instead of consuming the authored manifest — the room lane gets NO new placement authority; every number it needs is already on disk and Nintendo's own: (1) DOOR/ENTRANCE IN MID-ROOM:** entry spawn MUST be `ext_bg1.ini spawn_rel=(-255,0,1125)` + `spawn_ry=-32768` (the interior KNOB00 door — PLYR id 1 co-located, №56-proven); exit interactible at `exit_door_rel=(-255,0,1125)`; return via `return_pos=(-205170,720,316800)`/`return_ry=20206`. **The manifest fields are lane-agnostic by design — pinned lane and room lane read the SAME keys. Any lane-local spawn constant is a bug by definition. (2) GRANDMA ABSENT: the population pass didn't run on room load** — №62's own design point: mounts (and therefore `ExtNpcPop` + the spawn ledger) bind to the room's load/unload lifecycle exactly as they bound to mount COMPLEATE in the pinned lane. Acceptance for the exemplar re-test: Link enters AT the room's real door facing in; Grandma (`[Spawn] src=census:Ba1`) + the room's furnishings present; exit door AT the same door returning to her porch; ledger lines identical in shape to the pinned lane's. **When these two land, the exemplar is DONE and the remaining interiors migrate by data alone (their manifests already carry everything).** |
| **№65 THE ROOM LANE IS HALF-MIGRATED — user's suspicion CONFIRMED IN CODE (History audit of `d_ext_npc_doors.cpp`, not of the ledger):** | **WIN FIRST: entry fade→Grandma's room→Grandma present = №64 CLOSED; the exemplar's acceptance trio is 2/3 (entry ✓, population ✓, exit ✗).** **THE AUDIT (evidence, line-cited): MEMORY lane migrated; TRANSPORT lane NOT — for BOTH directions. Entry (`commitEnter` :450-459): claims + `ensureRoomLaneLoaded(hostRoom)` = REAL TP room load ✓ … then still calls `requestBgWarpGuarded(proc, failSafe)` — Link is still TELEPORTED to the mount. Exit (`queueExit`→`tickPending` :544-550): pure `requestBgWarpTo("EXT_BG0", exitSpawn, facing)` + `ensureRoomLaneUnloaded` — 100% physical move, exactly as the user saw.** So today's build = TP room streaming for MEMORY (which is why the races died) wrapped around the OLD physical-move transport. **№62's remaining half: the player's transport must become TP's native room/stage change** — the scaffolding is already sitting there (`dComIfGp_setNextStage(dest.hostStage, 0, dest.hostRoom, dest.hostLayer)` :440) — engine changes room + places Link at the entry (our authored `spawn_rel`/`spawn_ry` as the entry point), instead of a warp request. Same for exit (native change back to the host room + `return_pos`/`return_ry`). **DIAGNOSTIC-HONESTY BUG (fix while in there): the exit ledger prints `lane=room` whenever a host room is claimed EVEN THOUGH the transport was pinned (:552-554) — label the TRANSPORT, not the claim, or the log will keep telling us the migration is done when it isn't.** **DEFERRED by user (noted, not acted): stray objects in Grandma's room — History's hypothesis to test later: the interior census carries its own `KNOB00` row (LinkRM ACTR @(-255,0,1125)) AND the engine spawns an exit knob at `exit_door_rel` (same spot) ⇒ duplicate/ghost doors, plus skip-list props (`swood`) if the room-load population pass doesn't honor the same skip rules as the pinned lane.** |
| **№66 №65 TRANSPORT LANDED — two regressions, both narrow (History; log 162543 + user screenshot):** | **CONFIRMED SHIPPED: the transport is now NATIVE both ways — `[ExtNpcMount] №65 room-lane ENTER transport 'EXT_BG1' room=2 (no BgWarp)` / `EXIT transport … → porch (no BgWarp)`; the physical-move calls are gone from the room lane. Entry cycles log clean: register room 2 → mount → BG COMPLEATE → population (3 actors, unique_folk=1) → `Link placed room-lane at spawn_rel → (-60255,2050,-238875)`.** **(A NO FADE = NOT INTENDED — it's a regression against the RATIFIED №60 spec:** beat 2 (instant control lock) and beat 4 (screen cover) are what make a transition read as a door instead of a teleport. The native path dropped `beginDoorFade` + the door-demo lock; **restore both AROUND the native room change** — cover the screen, change room, place, then lift. That alone kills the "magically moved there" feel; beat 6 (arrival walk-in) is the remaining polish. **(B THE VOID = A DRAW/VISIBILITY BOOKKEEPING BUG ON RE-ENTRY, NOT A LOAD FAILURE — evidence: the failing entry logs IDENTICALLY to the good ones (mount id issued, `BG COMPLEATE`, population spawned, Link placed), and the user's overlay reads `Room: 2` at the correct authored spawn — i.e. the engine believes Link is in a loaded room while NOTHING renders.** Suspicion to check first: the unload path (`№62 unload room2 keep0 rt=0`) clears room 2's table/visibility state, and `ensureRoomLaneLoaded` on re-entry re-creates the MOUNT without re-registering the ROOM's draw/switch state — so the actor exists and reports complete but its room is never marked visible. **Cursor: make load/unload symmetric — whatever room-table/switch/RTBL state the unload clears, the reload must restore; verify by re-entering twice and confirming the BG draws on entry #2+.** **(C STRANDING GUARD (safety, cheap): in the broken state the user was left in an unloaded room with NO door prop (`A-press — no doorAttention prop within 300u` ×12) — no way out but the menu. Extend the leave-shell/G-guard to the room lane: room claimed + no exit prop resolvable (or BG not drawable) ⇒ force the native return to `return_pos` and log it.** Link must never be stranded in a room he can't leave. |
| **№67 AUDIT F-3 RATIFIED — and the scope was one file too small (History re-ran the gate on EVERY preserved binary):** | **VERDICT: F-3 is CORRECT — `build/fps-good-backup-20260719-0827/dusklight.exe` (pre-Phase-M, 08:27) genuinely fails: `"NPC_MK — Ivan socket (Mk.arc; Plan R)"`, `"NPC_P2 — Medli socket (Plan R)"`, plus `mk.greet` + `display_name=` manifest-template text. Distribution risk, not commit risk (build/ is gitignored) — exactly as the auditor framed it.** **SCOPE CORRECTION (why gate-one-file isn't enough): FOUR `dusklight.exe` copies exist on disk. History gated all four — `fps-dirty-backup-20260719-1624` CLEAN of WW names; `windows-msvc` (June) CLEAN; `windows-msvc-relwithdebinfo` (CURRENT) CLEAN — its only `display_name=` hit is the neutral editor help text `display_name=RealName`, which independently CONFIRMS the №55 covenant edits are compiled into the live exe. So 1 of 4 fails, and the current build is the safe one to share.** **TRIAGE NUANCE (adopted into cookbook §0 so the gate stays credible): some backup hits are NOT leaks — `"(Makar crash class)"` and `"STUB entries (Makar/Medli/…) are roster shells"` are TP's OWN cut-actor roster vocabulary (TP ships Makar/Medli STUB RELs — our own TPHistory archaeology), the same exempt class as Bokoblin/Moblin in №55. A gate that reports TP vocabulary as leaks trains people to ignore it; a gate that misses `Ivan socket (Mk.arc)` is worthless. Triage, don't count.** **ACTION TAKEN (non-destructive): marker file `DO-NOT-SHARE-fails-covenant-gate.txt` written INTO the failing backup folder — NOT renamed/deleted, because `tools/_fps_ab3.bat` hard-codes that exact path (`set "CLEAN=…build\fps-good-backup-20260719-0827"`) and the FPS lane still needs the reference exe; the marker names the safe alternative.** **STANDING RULE RATIFIED (auditor's wording, adopted verbatim into cookbook §0): binaries outlive source fixes — any preserved exe passes the gate before it is shared with anyone.** |
| **№68 EXIT CRASH = TEARDOWN USE-AFTER-FREE + the TWIN-DOOR hypothesis CONFIRMED (History; crash log 173204):** | **№66-A LANDED (`№66 room-lane EXIT cover→unload` — the fade cover is in). Entry clean. Then: `unload room2 keep0 rt=0` → `room2 unload drop 'EXT_BG1'` → **EXCEPTION_ACCESS_VIOLATION, fault addr `0x66c`** — a tiny member offset = dereference of a **freed/NULL object + field**, i.e. something still touches room-2 actors AFTER the room dropped them. (Crash PC couldn't be symbolized — the exe was rebuilt since; the sequence is unambiguous without it.)** **CURSOR — ORDER THE TEARDOWN (the fix class, same family as №64's FIFO steal: registry/handles outliving their actors): before dropping the room, (1) FORGET every cached handle into it — the DoorDef's `exitKnobSpawned`/`hasExitKnobHost`/exit-knob actor id, doorAttention registration, any mount pointer the door poll or `playAnimNearest` walks; (2) make the poll/draw paths skip a room that is unloading (a single `s_roomUnloading` latch is enough); (3) THEN unload; (4) THEN place Link. Also `clearForBg` the room's population registry so the next entry re-spawns cleanly. Acceptance: enter → exit → re-enter → exit ×3 with no AV.** **TWIN DOORS — CONFIRMED, AND FIXED IN DATA (no rebuild): the log shows BOTH `[Spawn] src=census:KNOB00@(-255,0,1125) proc=NPC_KNOB` AND `[Doors] exit prop key=exit:linkrm pos=(-60255,2000,-238875)` — the same world spot (host+census). Every interior had TWO overlapping knobs: a decorative census one with NO door key + the functional exit one. That is almost certainly the DOUBLE-PRESS (press 1 lands on the dead twin) and it doubled the teardown surface. History unmapped `[KNOB00]`/`[KNOB00D]` in actor_map — interior census door rows are now holes BY DESIGN; exterior CSV has zero knob rows anyway. Doors now come from ONE source: `doors.ini` (exterior) + `exit_door_rel` (interior), and `exit_door_rel` was itself derived from these census rows, so nothing authored is lost.** |
| **№69 THE EXIT CRASH IS A COLLISION USE-AFTER-FREE (History; crash 174529 vs 173204 — identical fault, and №68 PROVES it isn't the door handles):** | **Evidence: SAME `EXCEPTION_ACCESS_VIOLATION` fault addr `0x66c`, SAME crash RVA `0x22f847`, across two different builds — one WITHOUT №68's handle-forgetting and one WITH it (`№68 forget handles` / `№68 forgot mount` / `№68 room2 unload (handles already forgotten)` all present, then crash anyway). A stable RVA across rebuilds = a big, unchanged ENGINE function, not the freshly-edited door code. History's twin-door data fix VERIFIED WORKING in this log: zero `src=census:KNOB00` spawns.** **LEADING DIAGNOSIS (high confidence, from the source): Link's collision attachment outlives the interior's collision object.** The BG mount registers its collision with the global system — `dComIfG_Bgsp().Regist(i_this->mpBgW, i_this)` (mount.cpp:3886) — and only releases it in `dExtNpcMount_delete` (mount.cpp:4002-4004). **On exit we RELEASE/free the interior's `dBgW` while Link is still standing ON it; his ground/attach state (dBgS_Acch-class) still points at that freed `dBgW`, and the very next ground update dereferences it → AV at a small member offset (`0x66c`) inside a big engine collision routine.** That is exactly the observed signature and explains why forgetting *door* handles changed nothing. **CURSOR FIX — ORDER, not new logic: (1) PLACE LINK ON THE ISLAND FIRST (native return to `return_pos`, so his ground probe re-attaches to EXT_BG0 collision); (2) THEN `Bgsp().Release(mpBgW)` + null it; (3) THEN free the room/heap. If any of that must happen in one frame, explicitly DETACH Link's ground/attach (force re-probe / clear the cached floor) before the Release. Never Release a `dBgW` the player is currently standing on. Same rule for the exit-knob and any actor mid-teardown: unregister from global systems BEFORE the heap dies, and defer the free to end-of-frame if in doubt.** **SYMBOLIZATION UNAVAILABLE ON THIS MACHINE (recorded so nobody re-burns the hour): `llvm-symbolizer` cannot read MSVC PDBs (returns `??:0:0` — the repo's older `tools/_symcrash.out.txt` shows the same failure), and Windows Debugging Tools (`cdb.exe`) are NOT installed. To CONFIRM the function: Cursor should link with `/MAP` (then RVA→symbol is a text lookup) or reproduce once under the VS debugger — one repro names it instantly.** |
| **№70 HOW TP/WW ACTUALLY DO IT — the №69 fix is not a hack, it RESTORES the native invariant (History; verified in this repo's own TP source):** | **TP's pattern, quoted: every room owns a collision actor** — `d_s_room.cpp:281` creates one per room (`fopAcM_create(fpcNm_BG_e, roomNo, …)`); `daBg_c` **registers** its collision on create (`d_a_bg.cpp:599` `dComIfG_Bgsp().Regist(mpBgW, this)`) and **releases it in its DESTRUCTOR** (`:262-265` `Bgsp().Release(mpBgW)` + `dStage_roomControl_c::setBgW(roomNo, NULL)`, plus `bgp->releaseBg()`, room grass/flower cleanup, and `offStatusFlag(roomNo, 0x10)`). **So in TP, collision dies exactly when the ROOM SCENE dies — and TP never tears down the room the player is standing in: either he has already crossed into the next room (room streaming), or he is being RE-CREATED in a fresh scene (door = stage change). WW is the same shape — our own №56 extraction shows stage + spawn-id + room per exit, with the player respawned at the authored PLYR. NEITHER GAME EVER FREES COLLISION UNDER A PERSISTENT PLAYER — that state simply cannot arise there.** **Our exemplar is the one configuration both engines avoid: Link stays alive AND we free the room he is standing in, then move him. Hence the №69 use-after-free. The ordering fix (place → release → free) is precisely how TP's room streaming behaves, so it is a RESTORATION of the native invariant, not a workaround.** **STRUCTURAL RECOMMENDATION (covers №69 AND №66-B in one move): model our BG mount on `daBg_c` — one collision-owning actor per room slot that registers on create and releases in its DESTRUCTOR, with the room bookkeeping mirrored symmetrically (`setBgW(roomNo, …)` on load / `NULL` on unload; the room status flag ON at load / OFF at unload). Note `daBg_c`'s teardown clears exactly the room state whose absence causes our re-entry draw failure (№66-B) — i.e. TP already documents, in code, the symmetric pair we were missing. Route the mount through that shape and the engine's own room lifecycle drives load, draw-state and release together.** **LADDER (record for the road ahead): (1) NOW — ordering fix, cheap, unblocks the exemplar. (2) NEXT — mount-as-room-BG (above): engine owns the lifecycle. (3) ENDPOINT — stage-per-interior (what TP/WW literally do for houses: the exterior unloads and Link is re-created at the spawn), which makes this entire bug class structurally impossible; it is the №62 "stage-per-area variant" and the honest destination when interiors multiply across islands.** |
| **№71 USER CALL: "each interior its own stage" — HISTORY AGREES (direction ratified pending Cursor feasibility):** | **The user asked whether the crash proves interiors must be stages. Answer: essentially YES.** Tally of everything this lane has cost us — offshore parking table, warm-up hack, leave-shell AABB guards, swim-between-rooms, fade races, stuck fade, twin doors, collision use-after-free (№69), re-entry draw failure (№66-B). **NOT ONE is a door bug; every one is a lifetime/ownership bug, and they exist only because we invented a configuration NEITHER SOURCE GAME HAS: everything co-resident + the player moved by hand. TP and WW both make a house door a STAGE CHANGE — exterior unloads, player RE-CREATED at an authored spawn (№70, verified in-source). In that model the player cannot hold a pointer into a freed room; there is nothing to keep resident; no parking, no guards. The class stops being expressible rather than being fixed.** **PRECISION (do not lose this): "interior = its own stage" means A TP HOST STAGE PER INTERIOR THAT MOUNTS THE WW ROOM AT SCENE CREATE — not TP loading WW's stage format (still impossible; that constraint is why the pinned-warp backend exists). The mount side is already proven by the room-lane exemplar; the open engine question is STAGE-TABLE entries vs the room slots Cursor already added (F_SP115 R02).** **HONEST CAVEAT: rooms are not inherently wrong — TP streams dungeon rooms within a stage. Our room lane broke because the implementation diverged from the engine's ownership model (№70 `daBg_c`), not because rooms are the wrong unit. Deciding factor is SCALE: a few small interiors ⇒ proper room slots are cheaper; the WHOLE GAME (many islands, many interiors, the Great Sea) ⇒ stage-per-area is what both source games do and what scales. Given the project's ambition, stage-per-interior is the endpoint.** **SEQUENCING (History recommendation, one-exemplar doctrine): land №69's ordering fix FIRST — small, unblocks the exemplar, validates the rest of the room lane — THEN pivot with Cursor's feasibility read in hand. Pivoting mid-crash leaves no working lane and no evidence; a working exemplar makes the stage migration a comparison rather than a leap. CURSOR: feasibility on stage-per-interior (host-stage table entries, scene create → mount WW BG, save/Saved-Stage bookkeeping, exterior reload cost on return).** |
| **№72 CORRECTION (user caught it): multi-stage hosting is NOT impossible — TP's OWN `R_SP01` is the blueprint:** | **History conflated two claims and must correct the record: (TRUE) TP's scene loader cannot parse WW stage archives — hence the mount/BG backend. (FALSE, and implied by №62/№71 phrasing) that we must therefore keep everything inside ONE host stage. Nothing forces that.** **TP PRECEDENT, verified in-source: `R_SP01` = "Ordon Interiors" — a SEPARATE STAGE whose ROOMS are the individual houses** (`d_a_npc_seira.cpp:363` comment "R_SP01-R01 Ordon Interiors - Sera's Sundries"; more houses at rooms 4 and 7 per `d_a_alink.cpp:11799`, `d_a_tbox.cpp:539`). **Ordon Village itself is a DIFFERENT stage (F_SP103). TP separates worlds by STAGE and subdivides each world by ROOM.** **THIS RESOLVES THE ROOMS-vs-STAGES TENSION: it is BOTH, in TP's arrangement. Rooms = the unit within a world; stages = the separation between worlds. Our actual error was not "using rooms" — it was using rooms OF THE ISLAND'S STAGE for interiors, which is exactly what places them in the island's world space (the offshore parking table).** **USER'S SCALING POINT IS DECISIVE AND THE PARKING HAS A HARD EXPIRY: interiors currently occupy world coordinates the GREAT SEA will later fill — players would sail into interiors. Multiply by every island's houses/shops, then dungeons (many rooms each), and single-stage parking collapses.** **PROPOSED MAPPING (mirrors both source games, economical on stage entries): island exterior = its own host stage; that island's interiors = ONE interiors host stage with a ROOM PER INTERIOR (the R_SP01 pattern); each dungeon = one host stage whose rooms are its rooms (TP's dungeon model, and WW's). No world-space parking anywhere; nothing co-resident that shouldn't be.** **Cursor feasibility (folds into №71's list, sharpened): how host-stage entries are added/registered; scene-create → mount WW BG + population per room; save/`Saved Stage` bookkeeping; return plumbing; whether existing room-lane code becomes the per-room mount path unchanged (it should). SEQUENCING UNCHANGED: №69 crash fix first — user agrees.** |
| **№73 №69 FIXED (3 clean loops) — new bug: RE-ENTRY MESH CORRUPTION = stale J3D cache across arc release (History; log 181136 + source):** | **WIN: enter→exit ×3 with NO crash — the №69 teardown ORDER fix works (`place-first` → `Released interior BgW` → `forgot mount` → unload, all logged).** **NEW BUG, diagnosed: on re-entry the room draws GARBAGE geometry while COLLISION IS FINE — the exact signature of drawing from freed memory. Proof in the log: the 2nd entry binds models (`BG model[0] mats=19 …`) with NO `model-data cache +` line = a CACHE HIT, while the unload path had already called `dComIfG_resDelete(arc)` (mount.cpp:4041/4050). `s_modelDataCache` (mount.cpp:70, keyed `bg:<Arc>/<model>.bdl`) is SESSION-LIVED and its own comment states the assumption the room lane silently broke — mount.cpp:2053: "Keep s_modelDataCache — session-lived; arcs are stable while the mod folder is." TRUE in the pinned lane (mounts permanent, arcs never released); FALSE now. Collision survives because the dzb is re-fetched and a FRESH dBgW built each entry — hence "everything beyond collision is garbage".** **ROOT LAW (user made the cross-chat connection — it is 1:1): J3D parsed data is POINTER-FIXED INTO ITS ARCHIVE BUFFER. Two fatal directions, both now proven here: (a) re-parsing an already-fixed buffer = the sumo/`body_bmt` crash (№50-C; fixed by pristine-raw stash + parse-from-copy); (b) freeing the buffer while parsed data lives = THIS bug. Same law, opposite directions.** **CURSOR FIX: purge `s_modelDataCache` entries for an arc whenever that arc is released (arc-scoped key sweep in `dExtNpcMount_delete` / the room-unload path), and UPDATE the now-false comment at :2053. Re-parse on next entry is cheap and the fade covers it. **WATCH SHARED ARCS: `Knob.arc` is used by 5+ door props + exit knobs — one owner's release must not orphan another's parsed data; make the purge arc-scoped with refcounting (or pin shared arcs).** Per №70, the `daBg_c` shape puts this purge in the destructor alongside collision release — one symmetric teardown.** **PATTERN TO NAME (three in a row): the room lane keeps invalidating pinned-lane invariants — stale door handles (№68), collision released under the player (№69), now a session-lived model cache (№73). CURSOR: sweep the remaining session-lived state for "mounts/arcs are permanent" assumptions BEFORE the stage pivot, or they resurface there.** |
| **№74 AUTO-WARP-OUT = HISTORY'S OWN №66-C GUARD FIRING FALSELY (History owns this one; log 182335):** | **STABILITY CONFIRMED FIRST: 4+ enter/exit loops, no crash, no corruption — №69 + №73 both hold.** **The auto-warp is named explicitly in the log: `[Doors] №66-C stranding G-guard — 'EXT_BG1' drawable=1 exit=0 → return_pos`. Fired on entries 3 and 4; entries 1-2 exited normally by the door (no warning). That matches the user's account exactly — the first loops were quick in-and-out NEAR the door; the roll-around session moved Link AWAY from the door, and then the guard fired.** **DIAGNOSIS: the guard's "is there an exit?" test is evidently PLAYER-PROXIMITY based (`exit=0` while the log shows the prop WAS spawned that same entry: `exit prop key=exit:linkrm pos=(-60255,2000,-238875)`), so walking/rolling out of the door's radius reads as "no way out" and force-returns Link. `drawable=1` means the room was perfectly healthy — a stranding guard must NEVER actuate in that state.** **HISTORY ACCOUNTABILITY: №66-C was my recommendation, written to prevent the unloaded-room strand. The intent was right, the predicate is wrong — it tests where the player is standing instead of whether the room HAS an exit.** **CURSOR FIX: (1) existence test = the room's exit-prop REGISTRY/spawn record (id/handle), never distance-to-player; (2) actuate ONLY when Link genuinely cannot leave — exit prop failed to spawn at all, OR Link is outside the room AABB with no transport pending — AND require the condition to persist ~30+ frames (no transient firing); (3) log the evidence (prop id, player pos, AABB, drawable) so any future false positive is self-diagnosing; (4) RECOMMENDED for this build: demote the guard to WARN-ONLY (log, do not warp) — we are stable, and a guard that misfires is worse than the strand it prevents. Re-arm it once the stage lane lands.** **USER QUESTION ("time to rework interiors?"): YES — this is the right moment. The crash class (№69) and the cache class (№73) are FIXED and understood, the exemplar is stable, and the only open item is a guard predicate. Pivoting now means pivoting FROM a working, explained baseline (one-exemplar doctrine satisfied) rather than mid-bug. Proceed to №71/№72 stage feasibility as the next major work.** |
| **№75 PRE-PIVOT DESIGN MATRIX (user asked 5 questions before the interiors rework; History answers with recommendations):** | **(Q0 "can we still fix misplaced models/textures/joints/meshes?") YES — unchanged. Those are ASSET/DATA problems (arc adaptation, manifests, actor_map, head/companion joint maps, BMT) and are independent of WHICH container hosts the space. The mount pipeline, the adaptation tool, the census CSVs and the in-game editor all keep working exactly as now; only the container changes. Nothing in the pivot re-opens fixed art bugs.** **(Q1 stay on the Hylia map?) NO — long-term. Hylia (F_SP115) was the bootstrap host. With stage-per-area each WW space gets its OWN host stage and can carry WW'S OWN COORDINATES (anchor = identity), which removes the offshore parking, removes any chance of sailing into an interior, and keeps float precision sane. **AND THE COST OF MOVING IS LOW BY CONSTRUCTION: the №22 anchor/host split means positions are anchor-relative — moving a space is a MANIFEST EDIT, not a re-extraction.** For the Great Sea: WW itself ships the sea as ONE stage whose ROOMS are the sea sectors — map that literally (sea = host stage, sector = room); islands then live in their own stages or as sector rooms, decided when W3 starts.** **(Q2 rooms / layers / areas?) The only CONTAINERS in TP are STAGE and ROOM. "Layer" is a story-VARIANT of a room's actor set (we already use layers for census gating: ACT0/ACT4…), NOT a memory unit; TP has no formal "area" container (area = save/region flags). **Therefore: interiors = ROOMS of an interiors STAGE (the verified `R_SP01` pattern). Dungeon = ONE STAGE whose rooms are its rooms — the user's instinct is exactly TP's own design.** **(Q3 multiple interiors per room?) NO — one interior = one ROOM. Sharing a room re-creates the co-residency we are escaping (shared collision + actor set). **AND THE DATA ALREADY AGREES: WW ships interiors as `Room0.arc`/`Room1.arc` (Ojhous2 = Orca in Room0, Sturgeon in Room1 — №59). So the mapping is LITERAL: WW stage → TP host stage; WW `RoomN.arc` → TP room N. The per-room censuses History already extracted line up 1:1.** **(Q4 house WW spaces inside thematically-similar TP stages — WW fire dungeon in Goron Mines etc.?) RECOMMEND NO, for two hard reasons: (a) PURITY (№31) — a TP dungeon stage carries TP's own rooms/collision/events/lighting and its save-flag identity; (b) **STAGE-NAME BRANCHING IS REAL AND EVERYWHERE — TP actor code literally branches on stage name (`checkStageName("R_SP01")` in `d_a_alink.cpp:11799`; `strcmp(…, "R_SP01")` in `d_a_npc_seira.cpp`, `d_a_tbox.cpp`, …), so hosting WW content inside a TP dungeon stage inherits that stage's special-case behaviour in both directions.** USE NEUTRAL host stages (unused/newly registered) so no TP scene logic fires. **The user's underlying instinct is still RIGHT and keeps its value in a different form: reuse TP's equivalent MECHANISMS (Goron-Mines-style systems, Forest-Temple-style puzzles) inside neutral WW stages — mechanism reuse, not space co-habitation.** (Shared TP/WW engine lineage is real — our TPHistory research found WW data inside TP — but that is ENGINE code, not stage content.)** **(Q5 what is NOT being considered — History's list): (1) SAVE DATA: stages/rooms are save-coupled (`Saved Stage`, area flags, room switches); adding stages needs a bookkeeping plan and a compatibility check against existing saves. (2) STAGE-TABLE capacity/registration (Cursor feasibility). (3) MAP/MINIMAP: Fmap is per stage/room — WW spaces will show wrong or empty maps until addressed (sea chart later). (4) BGM/AUDIO: music is stage/room-assigned; WW spaces need WW music via the audio lane (twin-size guard rules apply). (5) LIGHTING: №63's palette ladder gets EASIER with stage-per-area (WW palettes per WW stage, zero TP pollution). (6) CAMERA: WW rooms carry RCAM data we already extract — room-based camera setups can be honoured. (7) DEATH/RESPAWN: dying in a WW space must respawn correctly, and it interacts with the Stargazer/Shade Refuge system. (8) FAST-TRAVEL/WARP UI: our warp menu is stage-driven — needs entries per WW stage. (9) LEVEL EDITOR: works per stage; new stages need editor awareness (separate chat owns it). (10) DEMO/CUTSCENE system also branches on stage names (№63 lane). (11) EXIT RELOAD COST: stage change reloads the exterior on return — both source games pay it; confirm acceptable. (12) MIGRATION ORDER: one island first (Outset), pinned lane stays as fallback until the stage lane proves. (13) BRIDGE TOOL: stage/room assignment should be EMITTED AS DATA in manifests so the bridge can regenerate it (keeps reproducibility, per that lane's goals).** |
| **№76 PRE-PIVOT ANSWERS v2 (user corrected Q1/Q2 and challenged Q3/Q4; History re-answers WITH EVIDENCE — WW extract census + TP's own warp table):** | **(Q1 EXTERIORS + GREAT SEA — evidence settles it): the WW extract census shows `sea` = ONE STAGE WITH 50 ROOMS (the sea sectors); dungeons are one stage each with many rooms (`Siren` 24, `M_Dai` 21, `kaze` 17, `M_NewD2` 17, caves `Cave09/10/11` 21 each). **So WW's own file layout IS the answer: Great Sea = one host stage whose ROOMS are the sectors — and TP room streaming (load/unload by proximity) is exactly the "cull islands as they drift behind, draw as they approach" behaviour the user describes. Better still, Nintendo already numbered it for us: Outset's own SCLS returns read `sea … room=44` — Outset IS sector room 44.** Hylia (F_SP115) was the bootstrap host only; the exteriors move to their own stage, keep WW world coordinates (anchor = identity), and the parking table dies.** **(Q2 WARP MENU — corrected reading): Dusklight's warp UI is data-driven from `include/dusk/map_loader_definitions.h`: `RegionEntry` → `MapEntry(name, stageFile, rooms)` → `RoomEntry(roomNo, points[])`. So its hierarchy is REGION → MAP(stage) → ROOM → POINT, and "points" are spawn ids. **Two dividends: (a) adding WW stages/rooms there gives us warp-menu entries FREE (e.g. Region "Great Sea" → Map "Outset Island" → room 44 → points); (b) the POINTS are exactly the Nintendo PLYR spawn ids History already extracted (№56) — the menu can offer real WW entrances, not invented ones.** **(Q3 MULTIPLE INTERIORS PER SPACE — user asked whether TP/WW do this; ANSWER: YES, and it is the norm — but per ROOM, not per room-share. TP's own table proves it: `R_SP01` holds SIX Ordon interiors (Bo r0, Sera r1, Jaggle r2, Link r4+r7, Rusl r5); `R_SP109` holds SEVEN Kakariko interiors (Renado 0, Barnes 1, Elde Inn 2, Malo Mart 3, Lookout 4, Bomb Warehouse 5, Abandoned House 6); `R_SP160` holds SIX Castle Town interiors. WW does the same (`Obshop` 6 rooms, `ma*room` 5 each, and our own `Ojhous`/`Ojhous2` = Room0/Room1).** ⇒ **RECOMMENDATION CONFIRMED AND SHARPENED: one INTERIORS STAGE PER SETTLEMENT, one ROOM PER INTERIOR. That is literally TP's pattern, and it answers the user's co-residency question: grouping is fine and normal at STAGE level; the isolation that matters is at ROOM level.** **(Q4 THEMATIC TP HOSTS — user rightly called History's "reuse TP lava/magnet actors" CROSS-POLLINATION; CONCEDED and struck. Correct statement: WW spaces get WW actors (content); only ENGINE systems (collision attributes, physics, demo/camera machinery) are shared. **The user still wants thematic hosts explored later WITH GUARDS (two rule-routes: vanilla TP vs WW-room rules for chests/hearts/etc.). History's honest assessment: the blocker is not theming, it is STAGE-NAME BRANCHING — TP actor code checks stage NAMES (`checkStageName("R_SP01")`, `strcmp(…,"R_SP01")` in alink/seira/tbox), so a TP dungeon host injects its own special cases. IF pursued, the guard must be a per-ROOM "space owner" flag (TP|WW) consumed by: spawn tables, chest/heart logic, save bits, lighting, BGM, and any stage-name branch (which would need room-aware overrides). That is real surface — viable, but strictly more work than neutral hosts. KEPT OPEN as a later experiment, not the W-phase path.** **(Q5 "are there truly neutral TP stages/rooms?" — partially): the table shows candidates that carry little/no gameplay: `F_SP200` (Wolf Howling cutscene map), `R_SP300`/`R_SP301` (cutscene maps), `S_MV000` (title movie map), plus BETA rooms (`F_SP118` r2 "Bulblin Camp Beta Room", `D_MN11B` r49 "Darkhammer Beta Arena") and the tiny grottos `D_SB05-09`. **But none are "large empty swaths" — TP stages are fixed authored worlds. The better route is what Cursor has already proven twice: MINT NEW ENTRIES (new proc ids for EXT_BG10; a new ROOM R02 in F_SP115) — i.e. register NEW stages rather than scavenge old ones, giving genuinely neutral hosts with zero TP content, zero stage-name branches, and free save/flag space.** **AND THE USER IS RIGHT that map/minimap/lighting/BGM/save-bit competition exists in ANY TP stage — so per-room overrides are required REGARDLESS of host choice; stage-per-area does not avoid that work, it MINIMISES it (a neutral stage has nothing to fight).** |
| **№77 DO NEW STAGES COMPOUND THE "FREE BIT" PROBLEM? (user's question; History checked `d_save.h` — answer: NO if save-neutral, and the REAL ceiling is elsewhere):** | **EVIDENCE: TP's persistent per-stage save table is FIXED — `dSv_save_c::STAGE_MAX = 32` (+ `STAGE2_MAX = 64`), `dSv_memory_c mSave[32]`. Stages do NOT get a slot by existing: each stage's STAG info DECLARES its save index (`dStage_stagInfo_GetSaveTbl`, used by `dComIfGs_isStageTbox/onStageSwitch/…` in `d_com_inf_game.cpp:1553-1581`). That is how TP runs ~100 stages on 32 slots — sharing + no-save stages.** **⇒ RULE TO LOCK IN: WW host stages are SAVE-NEUTRAL — declare no save table (or a reserved unused index). Then N new stages cost ZERO save bits. A stage that declares a vanilla index would write into vanilla chest/switch bits = exactly the free-bit failure class (sumo bit 700 / mod-flag v1→v2 reset family).** **TRAP NAMED: TP's NATIVE chest actor (tbox) writes to the stage's save-table bits — so wiring WW chests through it would DEMAND a slot. WW chests/hearts must grant through OUR mod-flag store instead (`dExtModFlags` in `dSv_reserve_c`, namespaced per mod folder).** **THE ACTUAL COMPOUNDING RISK IS OUR OWN STORE, NOT STAGES: the mod-flag KV is ~18 entries inside `dSv_reserve_c`'s 80 bytes (the run doc flagged "critique capacity (18 flags)" when it shipped). Fine for Outset's beats; nowhere near a full WW restoration (every island's chests, heart pieces, quest beats, NPC states). A HASHED store at capacity is precisely how silent bit COLLISIONS appear — the same symptom class the user remembers. ⇒ **FORWARD ITEM (before W2/W3 scale, not now): re-size + namespace the mod-flag store — versioned, per-mod namespaced, explicit allocation scheme, capacity headroom, and a migration path (v2→v3) that does not reset player saves silently.** **VERDICT ON THE USER'S PLAN: "new neutral stages feeding WW entries to the warp menu" is the IDEAL PATH and does NOT inherit the sumo bit problem — conditional on (a) save-neutral stage declarations, (b) WW state living in our store, (c) that store being grown deliberately before scale.** |
| **№78 PROGRESSION STORAGE — History CORRECTS №77's emphasis (user challenged it and was right):** | **№77 said "WW host stages are save-neutral." That is RIGHT for houses, WRONG as a blanket rule — because a TP save slot IS the progression vocabulary. `dSv_memBit_c` (0x20 per stage) contains exactly what the user asked about: `mTbox[2]` = 64 CHEST bits · `mSwitch[4]` = 128 switch bits · `mItem[1]` = 32 item bits · `mKeyNum` = SMALL-KEY COUNT · `mDungeonItem` bits = MAP / COMPASS / BOSS_KEY / STAGE_BOSS_ENEMY / STAGE_BOSS_DEMO / STAGE_LIFE / STAGE_BOSS_ENEMY_2 (= boss started/defeated). Quest/story progression lives separately in `dSv_event_c mEvent[256]` = 2,048 GLOBAL EVENT BITS. Dungeon-scoped runtime state also exists in `dSv_danBit_c` (switches+items+key bookkeeping).** **⇒ CORRECTED RULE: WW DUNGEONS SHOULD CLAIM THEIR OWN SAVE SLOTS — that is how keys/chests/boss-cleared work natively. The prohibition is narrower and unchanged in spirit: NEVER declare an index a vanilla stage already uses (sharing = the corruption/free-bit class). Houses/interiors with no persistent state still declare none.** **TWO UNKNOWNS TO SETTLE BEFORE THE PIVOT COMMITS: (1) HOW MANY OF THE 32 SLOTS ARE FREE? Assignment is per-stage via STAG (`dStage_stagInfo_GetSaveTbl`); many stages share or claim none, but nobody has counted. CURSOR: dump the used indices across vanilla stages — that number is the WW dungeon budget. (2) IS 32 ENOUGH FOR THE FULL GAME? WW brings its own dungeons plus islands/caves with chests. If the free count is small, the honest PC-fork answer is to EXTEND THE SAVE with a Dusklight WW block — WW-stage entries mirroring `dSv_memBit_c` semantics + a WW event bitfield sized like TP's 2,048 — rather than squeezing vanilla. Precedent exists: the fork already extended saves once (mod flags, v1→v2 migration).** **CLARIFICATION THAT CAUSED THE CONFUSION: the ~18-entry mod-flag KV in `dSv_reserve_c` is for MOD TOGGLES, never for game progression. Chests/keys/bosses/quests need real slots or the extension block. Design decision forced NOW (correctly) rather than discovered at Windfall.** |
| **№79 SAVE-INDEX COUNT ANSWERED + critique of Cursor's stage plan (History; `d_stage.h` enum):** | **COUNT #1 IS CLOSED FROM THE SOURCE — `dStage_SaveTbl` enumerates vanilla's claims: 0 ORDON · 1 PRISON · 2 FARON · 3 ELDIN · 4 LANAYRU · 6 FIELD · 7 GROVE · 8 SNOWPEAK · 9 CASTLE_TOWN · 10 DESERT · 11 FISHING_POND · 16-24 LV1-LV9 · 25 CAVE1 · 26 CAVE2 · 27 GROTTO = 23 used. The field is 5 bits (`field_0x09 >> 1 & 0x1f`) so the space is 0-31. ⇒ **FREE INDICES: 5, 12, 13, 14, 15, 28, 29, 30, 31 — NINE.** (Cursor: confirm by dumping actual STAG values, but the enum is the game's own vocabulary and unnamed = unclaimed.)** **CRITICAL CORRECTION TO "SAVE-NEUTRAL": DO NOT DECLARE 0 — 0 is ORDON. A WW stage declaring 0 would write into Ordon's chest/switch bits (the exact free-bit corruption class). ⇒ "Save-neutral" must mean DECLARE A FREE INDEX (e.g. 15 as the WW catch-all), never 0.** **BUDGET PLAN (History proposal): reserve 15 = WW interiors/misc catch-all; reserve the remaining eight (5, 12, 13, 14, 28, 29, 30, 31) for WW DUNGEONS. WW has ~7 majors (Dragon Roost, Forbidden Woods, Tower of the Gods, Forsaken Fortress, Earth Temple, Wind Temple, Ganon's Tower) ⇒ **tight but sufficient for the majors; the long tail (island chests, caves, submarines, minor puzzles) is what justifies the №78 save-extension block.** Decide the block before W3, not before the exemplar.** **NAMING CRITIQUE (covenant, and it matters): Cursor proposed `R_WW01`. **REJECT the "WW" token — stage folders live in `/res/Stage/` = THE GAME TREE, and the receiver covenant forbids WW names there.** Recommend either TP-native-looking unused numbers (`R_SP90`, `R_SP91`, …) or a neutral fork prefix (`R_DL01`); ≤7 chars either way. **BETTER QUESTION FOR CURSOR: can stage shells live MOD-SIDE and load through the existing custom-asset redirect (the `Toripost` R2 arc-mount precedent)? If yes, the game tree stays untouched entirely and the covenant is structurally satisfied rather than merely name-dodged.** **ENDORSEMENTS: (a) exit must regain `setNextStage` — Cursor's own catch, and it is the SYMMETRY INVARIANT: enter and exit must both be native stage changes, or we rebuild the asymmetry that produced №66/№69/№73. (b) warn-only strand guard with `exitKnobSpawned` existence = exactly right. (c) room-lane reuse (`onRoomObjectsReady`/registerRoomLane) stage-gated = right.** **SEQUENCING INSIGHT: interiors are save-neutral, so THE INTERIORS PIVOT IS NOT BLOCKED by the dungeon budget — only dungeons wait. Exemplar can proceed immediately.** **EXEMPLAR ANSWER: use ROOM 0 for LinkRM in the new interiors stage (fresh native numbering; R02 was an F_SP115 workaround). History will author the room-assignment table (one room per interior), the warp-menu entries, and the per-room manifests on GO.** |
| **№80 ROOMS vs SAVE SLOTS — the scarcity is ONLY the save table; and the 9 are safe BECAUSE the escape hatch is layout-compatible (user question):** | **SEPARATION OF RESOURCES: (a) STAGES = unlimited (data-driven `/res/Stage/<name>/`, no registry — Cursor). (b) ROOMS = ~64 per stage (`dSv_memory2_c mVisitedRoom[2]` = 64 bits; WW `sea` has 50 → fits). (c) SAVE SLOTS = the ONLY fixed resource (32 total, 23 vanilla-claimed, 9 free). ⇒ We can always make new rooms/stages; only progression slots are budgeted.** **A SLOT IS PER-STAGE, NOT PER-ROOM — and that is the whole budget insight: one `dSv_memBit_c` = 64 chest bits + 128 switch bits + 32 item bits + key count + MAP/COMPASS/BOSS_KEY/BOSS_ENEMY/BOSS_DEMO bits, SHARED BY ALL THAT STAGE'S ROOMS. A 21-room dungeon consumes ONE slot for its entire chest+puzzle state. ⇒ 9 slots ≈ 9 dungeons of native progression.** **PINCH POINT TO WATCH (flagged early): the Great Sea as ONE stage = ONE slot = 64 chest bits for all 49 sectors. If WW's island chest count exceeds that, split the sea into a few sector-group stages or move it to the extension. HISTORY ACTION when W3 nears: count WW's chest placements per sector from the extract.** **RECOMMENDATION (sequenced, not either/or): (1) USE THE 9 FREE INDICES NOW — zero engine work; native chests/keys/boss flags work out of the box; exemplar + first dungeons unblocked. (2) DESIGN THE EXTENSION AS A VIRTUAL INDEX SPACE, build it when the 9 run short: indices ≥32 route to a Dusklight block using the IDENTICAL `dSv_memBit_c` LAYOUT, so native call sites (`dComIfGs_isStageTbox`, switch/key/boss accessors) are unchanged and a slot→virtual migration is a COPY, not a rewrite. **Because the layout matches, starting with the 9 costs nothing later — that is why the 9 are not a trap. The actual trap would be inventing bespoke WW storage now, which forks the accessors and loses native chest/key/boss behaviour.** **CAVEAT TO CLOSE BEFORE ALLOCATION: the 9 are currently INFERRED from unnamed enum values. CURSOR: dump actual STAG save-table values across vanilla stages to convert inference → verified fact before any dungeon claims one.** |
| **№81 EXTENSION-FIRST (user call, ACCEPTED — History updates its own recommendation) + bridge-test triage:** | **USER'S DECISIVE CATCH: Outset is hosted in `F_SP115`, which declares `dStage_SaveTbl_LANAYRU` (4) — so ANY native save write from WW content TODAY lands in LANAYRU'S chest/switch bits. Nothing is corrupted only because no WW chest/key/switch is wired yet. The exposure is live but unexercised.** **⇒ HISTORY REVERSES №80's "use the 9 first": EXTENSION-FIRST IS CORRECT. Reason the user is right: ZERO WW progression is wired, so this is the CHEAPEST MOMENT IN THE PROJECT'S LIFE to build the real store — every later day means migrating real player saves instead of empty ones. **New allocation: ALL WW progression lives in the Dusklight extension from day one; the 9 free vanilla indices stay as HEADROOM FOR FORK-SIDE TP FEATURES (ALBW content, Shade Refuge, etc.) rather than being consumed by WW.** Layout-compatibility still holds (mirror `dSv_memBit_c` so native chest/key/boss accessors work unchanged) — it is now the PRIMARY home, not the escape hatch.** **IMMEDIATE GUARD (before the pivot even lands): WW content must NEVER call native save writers while hosted in a vanilla stage — today that writes into Lanayru. Cursor: assert/refuse + log if a WW mount attempts a native save write outside a WW stage.** **BRIDGE-LANE TRIAGE (user asked whether POP/M5 still make sense): (1) `[POP!]` BADGE TEST (§16 step 2) — VALID NOW, unaffected by the pivot (tests schema handshake + mod-row badge; independent of hosting/slots). Only scheduling note: it breaks population for one boot, so not mid-door-playtest. Step 1 (legacy path) already confirmed in the wild. (2) M5b FOLDER-ABSENT SMOKE — NOT DEFUNCT; the pivot EXPANDS it ⇒ RUN TWICE: now as a cheap baseline (it has still never validly run — rename≠disable per F-2; valid procedure = move folder out / Mods-UI off), then AGAIN after the pivot with new checks whose shape depends on Cursor's mod-side-shell answer — game-tree shells ⇒ verify vanilla boots/warps normally with foreign stage folders present; mod-side shells ⇒ verify the warp menu degrades gracefully when target stages vanish. (3) §12 LIVE CENSUS REFRESH (recovered `ry` rotations) — **CONTENT LANE = HISTORY DEBT, and it should land BEFORE I author stage-era data** so room manifests are built on correct facings instead of being redone. Live CSVs have ZEROED rotations (decomp-validated data loss) — very likely part of the "confused objects" the user saw in LinkRM. Cautions I own: my hand-appended rows (Ojhous2R1, furniture arcs) must survive the refresh, and the bridge noted TWO FIELDS its CSVs never capture (setID / SCOB scale) — check relevance to mounts before adopting wholesale.** |
| **№82 STAGE SHELL FAILURE DIAGNOSED + FIXED IN DATA (History; log 200033 + TP extract):** | **SYMPTOMS: Grandma's door → black screen, Link still on F_SP115 at the porch (-65090,4720,-73226); warp to the new stage → debug overlay ALL `?` (Stage `?`, position `?`) = the SCENE NEVER CAME UP.** **LOG EVIDENCE: `Loading Resource: shell.txt (Size: 28)` then `[ExtNpcMount] №62 room0 create FAILED 'EXT_BG1'`. Cursor DID take the naming advice (stage = **`R_DL01`**, neutral) and put it MOD-SIDE (`files/res/Stage/R_DL01/`) — covenant-clean ✓. `STG_00.arc` is credible (stage.dzs with STAG/RTBL/MULT/RCAM/RARO/EVLY + Env/Col/PAL, room0.dzs with FILI, pol_* tables). **THE FAULT WAS `R00_00.arc`: it contained ONLY `shell.txt` — no `room.dzr`. TP's room scene loads its data from the room arc's `room.dzr`; with none, the room scene cannot come up, the mount create fails, and the debug overlay has no stage to report (the `?` screen).** **HISTORY FIXED IT IN DATA (my lane — a DZR is a VALUE), authored from TP's OWN format, not guessed: templates read from the TP extract at `D:\XXXXXXX\Ex TPiles
es\Stage` — `D_SB05/R00_00.arc` and `R_SP01/R00_00.arc` (Bo's House, the closest interior analogue). Minimal valid room.dzr = 68 B: chunk count 2 · `FILI` (1 entry, TP interior bytes `1fec7f90 00000000` verbatim) · `PLYR` (1 entry, 0x20): name `Link`, params `ff00a03f` (TP interior spawn params), **pos (-255, 0, 1125) = Nintendo's own LinkRM door spawn in room-local coords**, rx 0, **ry -32768 (Nintendo's authored facing)**, **spawn id 0**, tail `ffff`. **PLYR spawn-id offset re-confirmed against TP: the u16 at 0x1C (D_SB05 shows ids 0 and 1 in consecutive entries) — same decode as №56.** Packed into `R00_00.arc` replacing `shell.txt` (original preserved as `R00_00.arc.shell-bak`); output re-parsed and verified: `room.dzr 68B · FILI n=1 · PLYR n=1 · pos (-255,0,1125) ry -32768 id 0`.** **CURSOR — NO REBUILD NEEDED FOR THIS, it is mod data; just re-run. If the room now comes up but Link lands oddly, the remaining knobs are the mount's host/anchor for R_DL01 (should be anchor=identity, host=(0,0,0) so WW room-local coords ARE the stage coords — that is why the PLYR sits at WW's own (-255,0,1125)). If MORE rooms are wanted, each needs its own `RNN_00.arc` with the same 68-byte skeleton (History will author the table on GO) plus a matching `roomN.dzs` FILI entry inside `STG_00.arc` and an `RTBL`/`MULT` row — Cursor owns those stage-side tables.** **NEW REFERENCE RECORDED: TP extract lives at `D:\XXXXXXX\Ex TP` (alongside the WW extract at `D:\XXXXXXX\Ex WW`) — the source of truth for TP-format templates (stage/room skeletons, FILI/PLYR bytes). Use it instead of inventing TP structures.** |
| **№83 SHELL NOW LOADS — failure moved from SCENE to MOUNT CREATE (History; log 202053):** | **PROGRESS (even though the screen looks the same): History's `room.dzr` IS loading (`Loading Resource: room.dzr (Size: 68)`), and the stage now comes up — stage.dzs/room0.dzs/pol_* load, `fpcNm_ROOM_SCENE_e` created OK, TP's own `fpcNm_BG_e (732)` created OK, and `fapGm_Execute` frames run inside R_DL01. **The scene-never-came-up blocker (№82) is CLOSED.** **SOLE REMAINING FAILURE: `[ExtNpcMount] №62 room0 create FAILED 'EXT_BG1'` ⇒ the room is EMPTY (no WW model, no WW collision) — which is why the visible result is unchanged (black, nothing to stand on).** **DATA IS CONFIRMED CORRECT — not the cause: `ext_bg1.ini` already carries `host_stage=R_DL01`, `host_room=0`, `host_pos=0,0,0`, `anchor=0,0,0`, `spawn_rel=-255,0,1125`, `spawn_ry=-32768` (identity anchor exactly as №82 recommended; Cursor set this up right). All other manifests still point at F_SP115, correctly, until they migrate.** **LEADING CAUSE (ordering, from the log sequence): the mount create is attempted IMMEDIATELY AFTER `ROOM_SCENE` create and BEFORE TP's `BG_e` room actor exists — i.e. before the room's actor infrastructure is ready. Cursor's own feasibility note lists an `onRoomObjectsReady` hook; the mount should be created FROM THAT HOOK, not at scene-create time. Secondary candidate: the room-lane registration is made at BOOT (`№62 room-lane register 'EXT_BG1' → room 0` at log line 801, during the F_SP115 session) and may be stale/mis-gated when the actual stage becomes R_DL01.** **ASK: log the create failure REASON (fopAcM_create return + actorId/roomNo/layer/proc) — we have inferred two failures in a row where one printed value would have named it outright.** |
| **№84 ROOT CAUSE NAMED: the pivot is doing a ROOM LOAD where it must do a STAGE CHANGE (History; Cursor's new reason-log + TP source):** | **Cursor's added diagnostic paid for itself immediately: `№83 room0 create FAILED 'EXT_BG1' reason=no_play_scene actorId=578 roomNo=0 host=(0,0,0) stage='R_DL01' playScene=0`. **NO PLAY SCENE EXISTS** — and the log confirms the consequence: no `fpcNm_ALINK_e` create for R_DL01 (the ALINK creates at lines 610/1295 belong to the earlier loads), hence no Link, no camera, and a debug overlay with nothing to report (`Stage: ?`). A ROOM scene alone is not a playable stage.** **WHY (TP source): there are TWO DIFFERENT MECHANISMS and we are using the wrong one. (a) WITHIN-STAGE ROOM LOAD — `d_stage.cpp:287` `fopScnM_CreateReq(fpcNm_ROOM_SCENE_e, …)`; this is what our room lane calls, and it is correct ONLY for rooms of the CURRENT stage (it is why the F_SP115 R02 exemplar worked). (b) REAL STAGE CHANGE — the PLAY SCENE re-requests ITSELF: `d_s_play.cpp:608` `fopScnM_ChangeReq(i_this, fpcNm_PLAY_SCENE_e, l_wipeType[wipe], 5)` after `dComIfGp_setNextStage(...)`. **The play scene is what creates Link, the camera and the room scenes. Our R_DL01 entry calls (a), so a foreign stage's room data is loaded with no play scene around it — a hybrid TP never produces.** The log signature is exact: `fopScnM_CreateReq: procName=18` (ROOM_SCENE) and never a PLAY_SCENE change.** **CURSOR FIX: when `hostStage` differs from the current stage, the enter path must be a NATIVE STAGE CHANGE — `dComIfGp_setNextStage(hostStage, point, roomNo, layer)` and let the play scene's own change path run (`fopScnM_ChangeReq(PLAY_SCENE, wipe, 5)`); do NOT call `ensureRoomLaneLoaded`/room-create for a foreign stage. The room-lane code stays valid for SAME-STAGE rooms (and becomes the per-room mount path once we are inside the interiors stage). Exit is the mirror image (this is the same symmetry invariant as №65/№83).** **PREDICTION TO VERIFY: once entry goes through the play scene, `fpcNm_ALINK_e` will be created for R_DL01, the overlay will report `Stage: R_DL01`, Link will spawn at the `PLYR` History authored (-255,0,1125 @ ry -32768, spawn id 0), and the WW mount create will then succeed because a play scene exists (`playScene=1`). If the mount still fails after that, the next suspect is create-before-room-ready (№83).** **PROCESS NOTE (worth keeping): two prior failures were diagnosed by inference from log ORDER; this one was named in a single line the moment Cursor logged the reason. Standing rule going forward — every refusal/abort path prints its cause and the values it judged.** |
| **№85 PLAY SCENE IS CREATED — the player is not; `dStage_playerInit` bails at its GUARD (History; log 203939 + `d_stage.cpp:1651`):** | **№84 LANDED: `fpcNm_PLAY_SCENE_e (11)` IS created for R_DL01 (pid 296, log 2489), followed by CAMERA, ROOM_SCENE, `room.dzr`, `BG_e`, and `fapGm_Execute frame=600` — frames RUN. So the stage change now happens; the game is NOT hung. What is missing is the PLAYER (no `fpcNm_ALINK_e` create for R_DL01) — hence overlay `Stage: ?` and nothing to stand on.** **ENGINE RULE READ FROM SOURCE (`dStage_playerInit`, d_stage.cpp:1651-1700): (a) SPAWN-POINT MATCH IS `(u8)player_data->base.angle.z == point` — the point id lives in PLYR **angle.z**, not a separate id field. **HISTORY DATA VERIFIED CORRECT: the authored PLYR has angle.z = 0 and Cursor requests point 0; TP's own D_SB05 entries use the same pattern (0 and 1).** (b) ON NO MATCH the engine prints `DuskLog.fatal("Failed to find player start point for next stage! Requested point: …, Valid points: […]")` — **THAT LINE IS ABSENT FROM THE LOG**, which proves the match was never attempted. (c) THEREFORE THE EARLY-RETURN FIRED: `if (dComIfGp_getPlayer(0) != NULL || dComIfGp_getStartStageRoomNo() != i_stage->getRoomNo()) return 1;`** **⇒ TWO CANDIDATES, both engine-side and both testable in one line of logging: (1) **A PLAYER ALREADY EXISTS** — the old Link from F_SP115 was never destroyed, so the new stage refuses to create one (this also fits every other symptom: a half-torn-down old scene, `playScene=0` at mount-check time, `Stage: ?`). (2) **ROOM-NO MISMATCH** — `dComIfGp_getStartStageRoomNo()` ≠ 0 while our room is 0 (check what `setNextStage`'s room argument actually lands in).** **CURSOR ASK: log both values at the transition (`getPlayer(0)!=NULL`, `getStartStageRoomNo()`, `getStartStagePoint()`, and the room being parsed) — the same reason-logging discipline that named №84 in one line. If candidate (1) holds, the fix is to let the play-scene CHANGE tear down the old scene/player properly (TP deletes and re-creates Link across a stage change — that is the whole point of the play-scene path) rather than any manual carry-over.** **HISTORY STATUS: room.dzr, PLYR/angle.z, FILI, manifest (host_stage/anchor/spawn) ALL VERIFIED AGAINST TP SOURCE OR TP DATA. No data debt on this failure.** |
| **№86 CURSOR SHIP (log 205514 + 203939):** | **DOOR PATH STUCK (205514):** `transport=stage` → OVERLAP0 created → **no new PLAY_SCENE** — frames stay on F_SP115 (900/1200/1500). Double enter while ChangeReq live. Cause: `beginDoorDemoLock` + `beginDoorFade` before native wipe fight OVERLAP completion. **WARP PATH (203939):** PLAY_SCENE+room.dzr OK, no ALINK; mount `reason=no_play_scene` mid-create. **FIXES:** cross-stage enter/exit arm `setNextStage` immediately with `endDoorDemoLock`+`offFade`, debounce if next already armed; `playerInit` SKIP log + clear stale player on WW host when rooms match; room-lane create stays on current layer when SearchByName misses. |
| **№86b CURSOR RECONCILE (History №86 ask):** | **Root values:** authored PLYR params `ff00a03f` → room bits **63** (TP interior copy); `playerInit` post-create `set(startRoom = params&0x3F)` would stamp 63; guard compares startRoom vs `roomDt.getRoomNo()`. **Fixes:** (1) `dStage_dt_c_roomLoader` `setRoomNo(param_2)` after `init()` before PLYR decode. (2) WW-host: if no player and rooms disagree → `setRoomNo(startRoom)` + continue. (3) WW-host: force `parameters` room bits to `getStartStageRoomNo()` before create/`set`. (4) Mod patch `R00_00.arc` PLYR `ff00a03f`→`ff00a000` (bak `.plyr-room63-bak`). Logs: `playerInit enter` / `№86 reconcile` / `CREATE`. |
| **№86 WW ROOM NOW LIVE INSIDE R_DL01 — only the PLAYER is missing, and the guard branch is now DEDUCED (History; log 211728):** | **BIG WIN: the pivot's core proposition is PROVEN. `[ExtNpcMount] №83 room0 mount 'EXT_BG1' id=00000134 stage='R_DL01'` (mount create now SUCCEEDS) → `[Spawn] src=room proc=EXT_BG1` → all three models load (`model.bdl mats=19`, model1, model3) → `№73 retain arc 'LinkRM' live=1` → **`BG COMPLEATE EXT_BG1 — dzbT=(0,0,0) model_space=local host=(0,0,0) anchor=(0,0,0)`**. Grandma's house geometry AND collision are mounted inside a real TP stage. Play scene created (pid 296), camera, room scene, TP `BG_e`, frames running.** **SOLE REMAINING FAILURE: no `fpcNm_ALINK_e` create for R_DL01 ⇒ NO PLAYER ⇒ blank overlay. Everything else is up.** **DEDUCTION THAT NARROWS IT TO ONE VALUE (no new logging strictly needed): `dStage_playerInit`'s guard is `if (dComIfGp_getPlayer(0) != NULL || dComIfGp_getStartStageRoomNo() != i_stage->getRoomNo()) return 1;`. **The overlay reports Link's position as `?` ⇒ NO player exists ⇒ `getPlayer(0) == NULL` ⇒ the FIRST condition is FALSE. Therefore the guard fired on the SECOND: `getStartStageRoomNo() != i_stage->getRoomNo()`.** (The `Failed to find player start point` fatal is still absent, confirming the match is never reached.)** **CURSOR: compare those two numbers at the transition — `dComIfGp_getStartStageRoomNo()` vs the `getRoomNo()` of the room whose dzr is being parsed. `setNextStage` is passed `static_cast<s8>(dest.hostRoom)` = 0, so if start-room is 0 the mismatch means the PARSED room is not numbered 0 (stage-side `RTBL`/`MULT`/`roomN.dzs` numbering, or the dzr being attributed to the stage rather than room 0). Candidate fixes: ensure the room scene is created for room 0 with matching numbering, or that `getStartStageRoomNo()` is actually set by our change path (it may still hold the previous stage's room).** **USER HYPOTHESIS ANSWERED (door animation / entrance cutscene): NOT the cause. Warping bypasses doors entirely and still yields no player, and player creation is stage-load-time from PLYR data — independent of door demos. The door path can stay unwired while this is fixed.** **HISTORY DATA: unchanged and verified (room.dzr FILI+PLYR with angle.z=0, manifest identity anchor). The WW side of this transition is now demonstrably correct — the room loads and completes.** |
| **№87b CURSOR SHIP (stale RARC name_hash):** | **Not WW `room.dzr` collision.** `R00_00.arc` string=`room.dzr` but hash still **`0xaee2`=`shell.txt`** (need **`0xa015`**). Index load logged Size 68; `findNameResource` miss → `roomInfo=NULL` → no `playerInit`. **Patched mod hash** (bak `.hash-shell-bak`); **PC `isSameName` strcmp fallback** on hash mismatch; **`[dScnRoom] phase_2` roomInfo log**. |
| **№87 THE BLOCKER: `dStage_playerInit` is NEVER CALLED — room-data lookup returns NULL (History; log 213054 + d_s_room/d_stage source):** | **Cursor's instrumentation did its job and DISPROVED History's №86 deduction — good. `[dStage] playerInit enter …` appears for `F_SP102` (point=100), `F_SP122` (point=0, room 8) and `F_SP115` (point=0, room 0), each followed by an `ALINK` create. **It NEVER appears for R_DL01.** So the guard is not the issue: the function is not reached at all.** **CALL CHAIN (source-verified): `dScnRoom_Create` → `arcName = dComIfG_getRoomArcName(roomNo)` (`d_com_inf_game.cpp:2903` → `"R%02d_00"` ⇒ **"R00_00"**, matching Cursor's file) → **`i_this->roomInfo = dComIfG_getStageRes(arcName, "room.dzr")` (`d_s_room.cpp:598`)** → **only if non-NULL** → `dStage_dt_c_roomLoader` (`d_stage.cpp:2751`, whose table maps **PLYR → dStage_playerInit**) → decode → player created. **⇒ `roomInfo` IS NULL. That is the blocker.** **HISTORY DATA RE-VERIFIED AGAINST THE ENGINE'S OWN DECODER (not my parser): `dStage_dt_c_decode` reads `dStage_fileHeader{ m_chunkCount; m_nodes[] }` with nodes `{tag, entryNum, offset}` and `dStage_dt_c_offsetToPtr` rebases each node offset. **My authored room.dzr matches that layout byte-for-byte (count=2; FILI@0x1C n=1; PLYR@0x24 n=1). The file format is NOT the fault.** **PRIME SUSPECT — A NAME-KEYED COLLISION (same CLASS as №73's model cache): this run loads TWO different resources both named `room.dzr` — ours (68 B, the R00_00 stage room) and **WW's own (1088 B, from `LinkRM.arc`, pulled by the mount)**. If `getStageRes` resolution is name-keyed rather than strictly arc-scoped — or if the arc is registered under a key other than `R00_00` — the room scene's fetch fails or resolves to the wrong buffer. Secondary candidates: the arc is not yet synced when `dScnRoom_Create` runs (`dComIfG_syncStageRes(arcName)` phase), or the overlay registers the arc under a lowercased/pathed key.** **CURSOR ASK — ONE LINE AT `d_s_room.cpp:598`: log `arcName`, `roomInfo == NULL`, and (if NULL) whether `dComIfG_syncStageRes(arcName)` had completed. That single line separates all three candidates. If it is the name collision, the fix is arc-scoped resolution (and it is worth auditing every other name-keyed lookup for the same trap, per №73).** |
| **№88 ★LINK IS IN R_DL01★ — the interiors pivot is PROVEN; residual = a never-ending EVENT (History; log 214221 + user screenshot):** | **MILESTONE: overlay reads `Stage: R_DL01`, `Room: 0`, `Entry: 0`, **`Position: -255.0, 0.0, 1125.0`, `Angle: -32768`** — EXACTLY the PLYR values History authored into `room.dzr` — and Grandma's house renders around him. **The whole chain is verified end-to-end: neutral mod-side stage → TP room skeleton → Nintendo spawn → WW room mounted → player created there.** The №62/№71/№72 pivot is no longer theory.** **RESIDUAL (user diagnosed it correctly): Link is frozen and the editor reports "Cannot enter fly mode while event is active". LOG PROOF: at the entry, `frameInterp: onTypeChange 41 -> 40 (event_type_id=40, … entering_event=true)` **with NO matching `leaving_event=true` afterwards**, while frames continue — compare the HEALTHY pair earlier in the SAME run (enter at 4268 → leave at 4304). An event starts on entry and never ends.** **HISTORY DATA FIX APPLIED (my lane): our shell's `stage.dzs` carried an **`EVLY` (event-layer) chunk that TP's own Ordon-interiors stage `R_SP01` does NOT have** — contents were a stub (`00000000 00000000 08000000 0000 00ff ffff…`). A stage that declares an event layer with nothing behind it is a textbook never-completing entry event. **Neutralised minimally and reversibly by retagging `EVLY` → `xVLY` (unknown tags are simply skipped by `dStage_dt_c_decode`), avoiding an offset rebuild; backup at `STG_00.arc.evly-bak`. No rebuild needed — re-run.** **IF THE FREEZE PERSISTS, THE OTHER CANDIDATE IS ENGINE-SIDE AND LIKELY: the №53-A door demo lock (`DEMO_DOOR_OPEN`) is begun in the OLD stage on the A-press and released "on hold complete / abort" — but a NATIVE STAGE CHANGE tears that context down, so nothing ends the lock in R_DL01. **Cursor: either end/cut the demo on the new stage's first frame after a stage-change transport, or do not begin the door demo at all when the transport is a stage change (the fade + load already cover the transition; TP's own door demo belongs to same-stage doors).** Note this is the same family as №65/№69/№73/№83: state begun in the old context outliving the transition.** **COMPARISON DATA FOR FUTURE SHELLS: TP `R_SP01/stage.dzs` = STAG · RTBL(8) · MULT(6) · RCAM · RARO · SCOB(2) · VRB0(5) · Env0(8) · Col0(7) · PAL0(16) — no EVLY, single env layer. Ours = same core + EVLY(now xVLY) + a second env layer set (VRB8/Env8/Col8/PAL8, harmless). When the interiors stage goes multi-room, RTBL/MULT counts must grow to match the room count (TP's 6-interior stage uses RTBL n=8, MULT n=6).** |
| **№94 CURSOR SHIP (world-gen latches + self-heal):** | `dExtNpcWorld_bump` on `onStageReady` + `restartRoom`; clears door/pop latches. Spawn keyed to gen. Self-heal if latched this gen + zero live outdoor knobs / census. |
| **№90 CURSOR SHIP (pop + exit + lane-agnostic G-guard + warp point):** | COMPLEATE on WW host arms `s_interiorBootstrapProc` → poll drains when player exists → `spawnForBg` + `onInteriorBgReady` (exit knob) + `lastBgProc`. Doors poll accepts `dExtWwSave_isWwHostStage`. `armArrivalGuard` (no demo) from `onStageReady` + warp foreign/region. WW `setNextStage` point clamped to 0. |
| **№89 CURSOR SHIP (destination-owned arrival + G-guard):** | **Root (engine):** `doorCheck` set `mMode=DEMO`, `tryNativeWarp`→`setNextStage`, `reset()` only sets flag 8; `endProc` blocked while `isEnableNextStage` → hollow event. **Fixes:** (1) stage-change: clear INDOOR cmds, `mMode=WAIT`, **return 0** (no source DEMO). (2) arm arrival on cross enter/exit; dest: residual force-end → 18f `DEMO_DOOR_OPEN` → end same context → 120f G-guard. (3) `forceEndDoorEvent` = cancel demo + `cancelStaff` + `remove()`. |
| **№89 FREEZE = THE DOOR DEMO LOCK OUTLIVING THE STAGE CHANGE — hand the demo to the DESTINATION (user call, ratified):** | **RESULT OF THE CONTROL TEST: freeze PERSISTS after History neutralised the shell's `EVLY` chunk ⇒ **EVLY was NOT the cause** (History's candidate was wrong; stated plainly). The patch is RETAINED as correct hygiene — TP's own `R_SP01` interiors stage has no event layer either — with `STG_00.arc.evly-bak` available if we ever want it back. **The cause is the remaining candidate: the №53-A door demo lock (`DEMO_DOOR_OPEN`) begun in the OLD stage on the A-press, whose release path dies with that stage; nothing in R_DL01 ever ends it ⇒ event stays open ⇒ Link frozen, editor refuses fly mode.** **USER CALL (ratified): hand the demo to the destination room — which is ALSO beat 6 of the №60 seven-beat spec (arrival walk-in), so the fix and the polish are the same work.** **THE INVARIANT TO ENCODE (this is the SIXTH bug of the family — №65 transport, №69 collision, №73 cache, №83 mount, №88 event): WHATEVER BEGINS A DEMO/LOCK/REGISTRATION MUST BE THE THING THAT ENDS IT. State begun in the old context must never be expected to close in the new one.** **CURSOR SPEC (№89): (1) For STAGE-CHANGE transports, do NOT begin a door demo lock in the source stage — the fade + load already cover the transition. (2) On the DESTINATION's first frame after transport completes, if the arrival came from a door-class entry (the door key is already carried), the destination BEGINS a short arrival demo: control locked → Link's door-open/step-in animation at the authored spawn (facing `spawn_ry`, which is Nintendo's own) → **then EXPLICITLY ENDS the demo/event and releases control**. Begin and end in the SAME context. (3) SAFETY G-GUARD (mandatory, same spirit as the warp guards): if an event is still active N frames (~120) after arrival with no demo owner, FORCE-END it, release control, and log — **the player must never be left frozen**; a missing animation is always preferable to a soft-lock. (4) Same treatment on EXIT (the mirror), so the porch arrival gets its own owned demo.** **NOTE FOR THE RECORD: the pending event is TP's own, not WW's — no WW event data is involved. The mental model "WW wants an event and TP will not answer" is incorrect; WE opened an event and nothing closed it.** |
| **№90 ★DOOR PATH FULLY WORKS★ — three residuals, each evidenced (History; log 220615):** | **MILESTONE: Link enters Grandma's house through the Outset door, into a stage of our own, AND MOVES. №89 confirmed working: `№89 arrival demo BEGIN … frames=18` → `force-end event (arrival-end)` → `arrival demo END … G-guard 120f` → `event G-guard clear — control free`. Also note Cursor's added `dScnRoom` probe now prints `phase_2 arc='R00_00' roomNo=0 roomInfo=1 syncRt=0` — **roomInfo=1, the №87 NULL is gone.** **RESIDUAL 1 — NOTHING BUT THE ROOM SPAWNS (no Grandma, no furniture, NO EXIT DOOR ⇒ no way back). Evidence: `[Spawn] src=room proc=EXT_BG1` appears (the BG mount) but there are ZERO `src=census:` lines and no `[Doors] exit prop key=…` in the R_DL01 section. In the old lane these came from the population pass on BG COMPLEATE and from the doors module; both are still gated on the old host stage / retired room-lane path. **CURSOR: run `dExtNpcPopulation_spawnForBg` and the exit-knob spawn for a BG mounted in a WW HOST STAGE, keyed off the mount's own COMPLEATE — not off F_SP115 or the room-lane.** (Population data is already correct and stage-agnostic: `population=interior_placements.csv`, `population_stage=LinkRM`.)** **RESIDUAL 2 — WARP LANE REQUESTS `point=-1`. Evidence, same log: door path logs `playerInit CREATE point=0` (History's authored spawn) while the warp path logs `playerInit CREATE point=-1`. In `dStage_playerInit`, **-1 means "use the saved RESTART position"**, not a spawn point — so the menu warp never asks for our entrance. Root: R_DL01 has no warp-menu entry, so no valid point is supplied.** **RESIDUAL 3 — THE EVENT G-GUARD IS DOOR-LANE-ONLY. It runs as part of the arrival demo, so a WARP arrival (which begins no demo) gets no watchdog ⇒ the freeze survives on that lane. **CURSOR: make the guard LANE-AGNOSTIC — any arrival into a WW host stage arms it; event still active ~120f with no owner ⇒ force-end + release + log.** (This is the same "never leave the player frozen" rule as the warp/black-void guards.)** **HISTORY PROPOSAL — WARP ENTRIES SHOULD BE DATA, NOT COMPILE-TIME: `map_loader_definitions.h` is a compile-time table, so every new WW interior would need an exe edit AND would put WW-side labels in the binary. **Better: have the warp menu ENUMERATE WW stages from the mod manifests — the fields already exist (`host_stage`, `host_room`, `warp_label`, and points from the authored PLYR ids).** Covenant-clean (no WW naming compiled in), and every future island/interior appears automatically as data. History will supply the per-stage point lists; the current exemplar needs only: stage `R_DL01`, room 0, point 0, label from `ext_bg1.ini warp_label`.** |
| **№91 FULL DOOR ANIMATION — WW and TP use the SAME pattern, so NOTHING is ported (History research; both sources):** | **USER ASK: real WW door behaviour — Link opens, steps through, door closes behind, and the reverse on the interior side. ANSWER: TP already implements WW's exact mechanism, in `d_a_door_knob00.cpp` (`daKnob20_c`) — the direct analogue of WW's `d_a_knob00`.** **TP's mechanism (quoted): (1) `attention_info.flags = fopAc_AttnFlag_DOOR_e` ⇒ the door-class "Open" prompt. (2) `eventInfo.setArchiveName(getEvArcName())` + `setEventId()` registering NAMED events: **`DEFAULT_KNOB_DOOR_F_OPEN`, `DEFAULT_KNOB_DOOR_B_OPEN`** (+ TALK variants). (3) `setEventPrm()` orders it: `eventInfo.setEventId(…)`, `setMapToolId(…)`, `onCondition(4)`. (4) `demoProc()` switches on `getDemoAction()`; the open actions call `openProc(...)` and, when the swing completes, **`dComIfGp_evmng_cutEnd(...)` — THE DOOR ENDS THE CUT** (plus `dComIfGp_event_offHindFlag(1)`). (5) SFX `Z2SE_OBJ_KNOB_DOOR_CLOSE` / `Z2SE_OBJ_IMPAL_DOOR_OP`.** **WW's `d_a_knob00` is the SAME SHAPE: front/back check (`mFrontCheck` → `m2C6` = 7 or 8) selecting `mEventIdx[...]`, then `eventInfo.setEventId(...)`. ⇒ **door orders a named event → the event drives Link → the door ends the cut**, in BOTH games. Front/back selection maps 1:1.** **IMPLEMENTATION (pure №60 doctrine — TP mechanism, WW content): our WW Knob prop registers `fopAc_AttnFlag_DOOR_e` and orders TP's `DEFAULT_KNOB_DOOR_F_OPEN` / `_B_OPEN` (front/back by facing, which we already compute), while WW's own `dooropenadoor.bck` plays on the `DoorDummy` joint (already working). Link's open/step-through animation comes FREE from TP's event. On completion the door calls `cutEnd`, and THEN the stage change fires. Arrival (beat 6) is the mirror: the destination's door orders the back-open/step-in cut, the WW door closes behind, `cutEnd`, control released.** **STRUCTURAL BONUS (matters after this week): in TP's pattern THE DOOR OWNS `cutEnd` — begin and end live in the same actor. Adopting it makes the ownership invariant automatic and RETIRES our ad-hoc `playAnimNearest` + manual demo lock (the №88/№89 failure class).** **PURITY NOTE (№31): TP's door SFX (`Z2SE_OBJ_KNOB_DOOR_*`) must NOT play inside WW spaces — use WW door sounds via the audio lane, or SILENCE until they are adapted. Animation/event machinery is a SYSTEM (fine); sound is CONTENT (not fine).** **SEQUENCING: land №90 first (population + exit knob + lane-agnostic guard + warp point) so the loop is complete; then this becomes the polish pass that also simplifies the code.** |
| **№92 ROADMAP AFTER THE EXEMPLAR — interiors rollout → hygiene → quest spine → CUTSCENES (user asked when cutscene work begins):** | **(A "same room?" — NO: same STAGE, ONE ROOM EACH (TP `R_SP01` pattern, №76). Sharing a room re-creates the co-residency we just escaped; separate rooms give each interior its own collision/actors/lifecycle. **`R_DL01` room map (History-authored on GO): 0 = LinkRM (Grandma/Link) · 1 = Ojhous2 (Orca) · 2 = Ojhous2R1 (Sturgeon) · 3 = Omasao (Mesa) · 4 = Onobuta (Abe & Rose) · 5 = LinkUG (basement).** NOT in this stage: **A_mori (outdoor forest), Cave09, Pjavdou** — natural/outdoor spaces get their own hosts, exactly as TP keeps caves/grottos separate from `R_SP01`. Each room needs its `RNN_00.arc` (History, 68-byte skeleton proven in №82/№88) + stage-side `roomN.dzs` FILI and grown `RTBL`/`MULT` (Cursor; TP's 6-interior stage uses RTBL 8 / MULT 6).** **(B HYGIENE, after the rollout — not before: `[POP!]` badge test is independent and can run any time; **M5b's SCOPE CHANGED with the pivot** (№75) — with stage shells existing it must ALSO verify vanilla boots/warps cleanly with our stage folders present, and that the warp menu degrades gracefully when target stages vanish. Running it after the interiors exist tests the real shape instead of a state we are about to abandon.** **(C QUEST SPINE = THE ACTUAL GATE FOR CUTSCENES. Reasoning: cutscenes are the TRANSITIONS BETWEEN BEATS — they set flags, shift the population between layers, grant items. With no beats, a cutscene has nothing to advance and would be rebuilt when the spine lands. The spine is mostly History work and fast (layers→flags already designed in №35-H5).** **(D CUTSCENE START CONDITIONS (all three): every Outset space enterable · day-1 population present + animated + dialogue working · quest flags exist to be set. **FIRST CUTSCENE = a SMALL INTERIOR ONE: Grandma handing over the shield (one room, two actors, dialogue, item grant, one flag)** — per №63's one-exemplar rule, before the pirate ship / Helmaroc / departure.** **(E PARALLEL WORK HISTORY CAN DO ANY TIME, BLOCKING NOTHING: extract WW's Outset `event_list.dat` entries as the CUTSCENE AUTHORING BIBLE (who is on stage, camera moves, timings per beat) — pure disc extraction, runs during any Cursor build cycle, waiting when the era opens. Awaiting user go.** |
| **№93 DIALOGUE RESEARCH + REAL MESSAGE ARCHIVE EXTRACTED (user-directed; №33 finally actionable):** | **(A THE BOX — the mount is NOT using the wrong class.** Compared against the known-good rental postman: both create `dALBWDialogue_c` then `tryCreate()` + `showWithText()`, and that class already renders TP's native window (frame BLO + text BLO) **with word-wrap (`dALBW_wrapMesgWords`) and pagination (`buildPages`, `mMaxLinesPerPage`, `kMaxPages`) built in.** **What differs is the TALK FLOW around it: the postman shows text from inside a real TP talk event (`evtTalk`), so the game enters a genuine talk state (camera, control lock, prompt, clean exit); the mount instead sets its own `mTalking` flag and shows the window.** Shade Watcher goes further still — full native `initTalk(msgId)` / `talkProc` with `dMsgObject_setSelectWord` for choices. **⇒ CURSOR (№33): wrap mount dialogue in the native talk event (postman pattern). Do not replace the window class.** **(B THE REAL SOURCE — EXTRACTED. Our `ww_dialogue_full.txt` was a flat dump: no ids, and raw escape residue reaching the font (the `·····` junk). The actual archive is `Msg/bmgres.arc → zel_00.bmg`: **INF1 index (4,411 entries, 24 B) + DAT1 pool**. **NEW TOOL (repo, contains no game text): `tools/ww_crew_restoration_skeleton/extract_bmg.py`** — parses RARC→BMG, walks INF1, decodes `0x1A <len>` escapes (colour/pause/furigana dropped, `{player}` preserved), emits **id-keyed TSV**. **Output `dialogue/ww_messages.tsv` (4,411 rows) written MOD-SIDE only — game text never enters the repo.** **(C ARYLL — DONE. Her block is CONTIGUOUS at msg_id 3001-3036 (NPCs own id ranges — a general finding that makes every other villager cheap). Full day-1 script recovered IN AUTHORED ORDER: 3001 greeting **"Ah! 'Hoy, Big Brother! Did Grandma make that outfit for you?"** · 3002-3003 outfit · 3004-3008 the telescope gift + how-to · 3009-3010 postbox/postman · 3011-3017 the sky, the falling girl, "too dangerous without something to defend yourself" · 3018-3021 zoom hints · 3022-3033 **Aryll's Lookout birthday scene** · 3034-3036 · 3113 · 3553 · 4424 (her letter from the pirate ship). **AUTHORED `dialogue/folk_ls.txt`** (manifest already says `dialogue=folk.ls`): working greet → outfit chain → repeat-visit nudge, with **every remaining beat id listed inline as the cutscene-era reference so that pass never re-derives them.** Adaptations declared in-file: `{player}` dropped, button-glyph escapes generalised, **nothing else reworded — the text is Nintendo's.** **(D NEXT RESEARCH STEP (not blocking): msg-id → NPC mapping for the REST of the cast. Aryll was found by content; the general map comes from WW actor sources (each NPC references its id range), the same decomp lookup that gave us heads and door tables. With the TSV in hand this is now a lookup, not a hunt.** |
| **№94 ⚠ EMPTY-WORLD BUG — spawn-once latches survive a world reload (History; source-named, no log needed):** | **USER REPORT: Link fell through the floor near a wall → respawned at the door with NO actors and NO door → walking out dropped him into void → second attempt warped him back to Outset **with every door and actor GONE**. **This is the SEVENTH instance of the state-outlives-a-transition family (№65/69/73/83/88/89) and the most dangerous: it leaves the player in an empty world with no way to interact.** **ROOT CAUSE (both latches read from source): (1) DOORS — `s_knobsSpawned` (`d_ext_npc_doors.cpp:74`) is set true after props spawn and is reset ONLY inside `ensureLoaded()` when the MOD FOLDER changes (`if (s_loaded && strcmp(s_loadedMod, island.modFolder)==0) return;` early-returns for the same mod). ⇒ once doors spawn in a session the flag never clears; any world reload skips door spawning. (2) POPULATION — `s_spawnedBgProcs` (`d_ext_npc_population.cpp:39`) is session-lived, cleared only via `dExtNpcPopulation_clearForBg` from mount-delete paths; a respawn that does not run those deletes leaves the proc marked spawned ⇒ census never re-runs.** **CURSOR FIX (three parts): (a) KEY SPAWNING TO A WORLD GENERATION, not a session — one counter bumped on every stage change, room load, and player respawn/restart; spawn logic stores the generation it ran for and re-runs on mismatch. (b) EXPLICIT CLEARS on the restart path (death / void-fall / `getRestartRoom*`), both latches. (c) SELF-HEAL SAFETY NET (makes the class non-fatal even if a future path forgets): island BG COMPLEATE + doors.ini non-empty + zero door props alive ⇒ respawn + log; BG COMPLEATE + census rows > 0 + zero census spawns ⇒ re-run + log.** **OTHER TWO ITEMS FROM THE SAME PASS: (i) door does not animate open/close on entry — **EXPECTED, that is №91 (not yet built)**, no action needed. (ii) FALL-THROUGH-FLOOR NEAR A WALL — **HISTORY RESEARCH ITEM (mine): re-examine `adapt_dzb`'s attribute clearing (`w0 & ~0x00FFC000`, the original through-flag fix) to confirm none of those bits carry WALL semantics in WW's data, and weigh it against TP Link's larger body/capsule vs WW Link's.** User flags it as minor now but potentially structural — agreed, and it is worth settling before the interiors rollout multiplies the surface.** |
| **№95 WW START SPAWN (Aryll's Lookout) — when, and why relocation does NOT threaten it (user planning question):** | **CANON CONFIRMED BY THE EXTRACTION: Aryll's own line 3025 — "This is my FAVORITE spot to gaze out at the sea… I call it 'Aryll's Lookout'" — and the whole birthday scene (3022-3033: "Big Brother, it's your birthday!", "Grandma has been waiting") plays THERE. WW's opening is Link on the lookout, Aryll climbing the tall ladder to wake him. **The user's proposed start point is correct WW canon.** **RELOCATION IS A NON-ISSUE — the №22 anchor/host split already pays for it: every authored position (spawns, doors, returns, census rows) is stored ANCHOR-RELATIVE in WW world space, so moving Outset is a ONE-LINE `host_pos` edit and everything follows. **A start spawn authored today survives relocation untouched. The only thing relocation would break is a HARDCODED ENGINE CONSTANT — which is exactly why the start spawn must be DATA, never code.** **PREFERRED FORM — DO NOT AUTHOR IT, EXTRACT IT: Outset ships 24 Nintendo PLYR entries; one is very likely WW's own opening spawn at the lookout. **User to supply the location (or a screenshot from up there); History matches it against the extracted PLYR list and binds the start to NINTENDO'S OWN ENTRY** — same principle as the door spawns/returns (№56). Extracted beats authored, every time.** **TIMING (three items, only one urgent): (1) NOW = №94 empty-world fix (urgent). (2) THEN interiors rollout → EXTERIOR RELOCATION to its own stage (№76 mapping) — do this BEFORE the start spawn so host coordinates are touched once. (3) THEN quest spine + START SPAWN together: "begin the WW questline" is a STORY ENTRY CONDITION (a mod flag deciding whether a new game starts in Ordon or on Outset), so the spawn is part of the spine, not a standalone task. Cutscenes follow (№92). **⇒ Nothing to do now; History holds the slot and will have the spawn ready as data so it costs nothing at spine time.** |
| **№96 LIGHTING AFTER RELOCATION — same system, different record sizes; WW's sky is extractable (History; both stage.dzs compared):** | **Q: if Outset leaves Lake Hylia for its own stage, what lights it? A: whatever OUR stage.dzs declares. Today Outset inherits **F_SP115's palettes** — which is exactly why it has been lit like a TP lake. A new stage escapes that (a win), but Cursor's shell currently carries stub env entries (n=1) ⇒ flat/default until filled.** **THE STRUCTURAL FINDING — WW and TP share the environment system AND the chunk names, but TP EXTENDED the records: WW `sea/stage.dzs` = **EnvR n=52 (8 B/entry) · Colo n=10 (12 B) · Pale n=57 (44 B) · Virt n=37 (36 B)**; TP = **Env0 (65-66 B) · Col0 (12-15 B) · PAL0 (52 B) · VRB0 (21 B)**. ⇒ **NOT copy-paste, but a FIELD-MAPPING job: same concepts (per-time-band ambient/light/fog/sky, colour sets, vr-box), different shapes. That is DATA work (History), not engine work — TP's kankyo reads our stage's chunks natively, so correctly-shaped values give Outset WW's authored light with TP's clock driving time-of-day.** **BONUS — THE GREAT SEA'S SKY IS RIGHT THERE: `sea/Stage.arc` ships `vr_sky.bdl`, `vr_back_cloud.bdl`, `vr_kasumi_mae.bdl` (haze) and `vr_uso_umi.bdl` (the horizon "false sea" plane) — the assets that make the Great Sea READ as the Great Sea. TP drives a vr-box from the same chunk family, so these mount like any other WW model.** **INCIDENTAL CONFIRMATION OF №76: WW's sea stage declares RTBL n=50 / MULT n=50 (fifty sector rooms) and SCLS n=212 (every island entrance in the game) — Nintendo's own table backs the sea-as-one-stage-with-sector-rooms mapping.** **TIMING: do lighting IN THE SAME PASS AS RELOCATION — the new stage's env chunks are authored then anyway, so filling them with WW values costs little more than filling them with stubs. Per the №63 ladder, safe intermediate = install Outset's authored DAY palette first (island looks right), then extend to full time-of-day once the mapping is proven. **HISTORY TASK (can start now): map WW Pale/Colo/Virt fields onto TP PAL0/Col0/VRB0.** **MINOR, LOGGED: user reports the Outset ladder is no longer interactable (post-№94). Watch item — if it persists after the next build, it is likely the same spawn-generation family (ladder/climb actor not re-registered on reload).** |
| **№97 INTERIORS ROLLOUT — HISTORY DATA COMPLETE, Cursor work order (user: "get Cursor ready for all Outset interiors"):** | **STATE (amended): door demo + STG×6 + OPEN fix SHIPPED (Cursor). OPEN residual closed (premature cutEnd). Rooms 1–5 verify waits on №100/R2. **ALL SIX INTERIOR ROOMS ARE NOW AUTHORED AND VERIFIED (History, this entry).** **ROOM MAP (room = bgN − 1; identity anchor throughout, so WW room-local coords ARE stage coords): **R_DL01 room 0 = EXT_BG1 LinkRM (exemplar, working) · room 1 = EXT_BG2 LinkUG basement · room 2 = EXT_BG3 Ojhous2R1 Sturgeon · room 3 = EXT_BG4 Ojhous2 Orca · room 4 = EXT_BG5 Omasao Mesa · room 5 = EXT_BG6 Onobuta Abe & Rose.** **HISTORY DELIVERED NOW: (a) `R01_00.arc` … `R05_00.arc` generated from the proven 68-byte skeleton, each carrying **Nintendo's own PLYR spawn** for that interior — LinkUG (349,0,−34)@−16384 · Sturgeon (546,−20,327)@−32768 · Orca (1,0,626)@−32768 · Mesa (127.3,0,625)@−32768 · Onobuta (301.7,0,620.3)@−32768 — all re-parsed and verified. (b) manifests `ext_bg2..6.ini` bound to `host_stage=R_DL01` + their `host_room`, with `host_pos=0,0,0` / `anchor=0,0,0`. **No further History data is needed for the rollout.** **CURSOR WORK ORDER: (1) STAGE-SIDE TABLES (yours): add `room1.dzs`…`room5.dzs` FILI entries to `STG_00.arc` and grow `RTBL`/`MULT` to cover 6 rooms — TP's own 6-interior stage `R_SP01` uses **RTBL n=8 / MULT n=6** as the reference shape. (2) GENERALISE THE TRANSPORT: confirm the native stage-change path is driven purely by `host_stage`/`host_room` from the manifest (nothing hardcoded to EXT_BG1), so all five new rooms work by data alone. (3) Population + exit knob per room (the №90 hook, applied per mount). (4) Door OPENING anim (the trivial residual above).** **WALKTHROUGH DEVIATION (user flagged it): A_mori (EXT_BG9), Cave09 (EXT_BG10) and Pjavdou (EXT_BG8) do NOT belong in the interiors stage — they are outdoor/natural spaces reached by WALK-THROUGH gates, and TP keeps caves/grottos as their own stages too. **They need the same native stage-change transport but WITHOUT the door demo (no door to open): plane-cross → fade → change → place → release, per the №60 walkthrough class.** They can stay on the current working lane until the interiors rollout is proven, then migrate to their own host stages.** |
| **№98 ⚠ LADDERS/LEDGES LOST = WW SPACES ARE REGISTERED AS *MOVING OBJECTS*, NOT WORLD (History; source-verified — and it makes relocation a GAMEPLAY requirement):** | **USER: Outset lost WW-level collision interactivity — the ladder is no longer climbable and Link no longer catches ledges; suspects interiors too.** **FINDING: TP's OWN room collision registers as `mpBgW->Set(dzb, cBgW::GLOBAL_e, NULL)` (`d_a_bg.cpp:220`) — world collision, NO matrix. **OUR WW mounts register as `Set((cBgD_t*)dzb, cBgW::MOVE_BG_e, &a->mBgMtx)` (`d_ext_npc_mount.cpp:2129`) — and EVERY OTHER `MOVE_BG_e` user in TP is a MOVING OBJECT: ice blocks, gates, `lv3WaterB`, `obj_kgate`, `b_zant_sima`, `bg_obj`.** ⇒ **to TP's player code, Outset and its interiors are one enormous MOVING OBJECT, not terrain.** **WHY THAT BREAKS CLIMB/LEDGE: `daAlink_c` branches on this class repeatedly — `ChkMoveBG(...)` / `ChkMoveBG_NoDABg(...)` at `d_a_alink.cpp:3885, 5484, 9289, 19086, 19113, 19663` (ground checks, ride logic, magnetic lines, start modes). Behaviours gated on WORLD collision (ledge-grab, wall-climb, ladder attach) naturally misbehave on "a moving platform".** **WHY WE DID IT: the matrix was required to offset WW data into a FOREIGN stage's coordinate space (the №22 anchor/host split). GLOBAL_e takes a NULL matrix — no offset possible.** **⇒ RELOCATION IS THE FIX, AND IT IS NOW A GAMEPLAY REQUIREMENT, NOT TIDINESS: a WW space in its OWN stage sits at `host_pos=0,0,0` / `anchor=0,0,0` — the matrix is IDENTITY, so there is nothing to offset and the space can register `GLOBAL_e` exactly like TP's own rooms.** **ONE-LINE EXPERIMENT AVAILABLE NOW (clear prediction): the R_DL01 interiors ALREADY sit at identity — CURSOR: register those mounts with `GLOBAL_e` (NULL matrix) instead of `MOVE_BG_e`. **Prediction: ladders/ledges/climb return inside Grandma's house.** If confirmed, Outset gets the same fix the moment it relocates to its own stage with WW's native coordinates (where its matrix also becomes identity). Keep MOVE_BG_e only for genuinely moving WW collision (if we ever have any).** **NOTE: this SUPERSEDES the earlier suspicion that `adapt_dzb`'s attribute clearing caused the fall-through/ladder loss — that review stays open but drops in priority; the registration class is the far better explanation and is testable in one line.** |
| **№99 REHOME PLAN (audit L-1…L-4 ratified; the predicted leak, and the structural fix that ends the class):** | **AUDIT ACCEPTED IN FULL. Verified in source: `include/dusk/map_loader_definitions.h:607` now carries `RegionEntry("Outset Interiors", { MapEntry("Link's House (interior)", "R_DL01", …) })` — a TRACKED, MODIFIED file, so it commits on the next push. **The auditor is right that №90 predicted exactly this** ("the compile-time map table would put WW-side labels in the binary… enumerate warp entries from the mod manifests instead"). It was added anyway for good reason — to make the warp testable — but every future WW interior added this way is another gate failure.** **THE AUDITOR'S SHARPEST POINT, AND IT SHOULD BE DOCTRINE: L-2 ("Link's House (interior)") is a label the GATE CANNOT SEE, because "Link's House" is ALSO a real TP location (Ordon, same file line 109). Greplisting it would fire forever on TP's own entry — the Ganondorf/Bokoblin exemption trap again. ⇒ **THE GATE PROVES ABSENCE OF UNAMBIGUOUS WW TOKENS; IT CANNOT PROVE ABSENCE OF WW LABELS THAT TP ALSO USES. Detection has a hard ceiling — only PREVENTION BY CONSTRUCTION clears it.** **REHOME PLAN — R1…R6 (ownership marked): **R1 (CURSOR, immediate, unblocks commit): neutralise both labels in `map_loader_definitions.h` — no WW semantics in source. Simplest neutral form: use the stage/room identity itself, e.g. `RegionEntry("Mod Stages", { MapEntry("R_DL01 r0", "R_DL01", {{0,{0}}}) })`. Keeps the warp testable, passes the gate, costs one edit.** **R2 (CURSOR, THE ACTUAL FIX — kills L-1 AND L-2 permanently): manifest-driven warp enumeration.** The engine already registers providers from manifests at boot; extend that to feed the warp UI: for each mod BG manifest read `host_stage` + `host_room` + `warp_label` (+ points from the authored PLYR ids) and populate a runtime region named from the mod folder. **Labels then live in the mod folder ⇒ ZERO WW strings in the exe BY CONSTRUCTION, ambiguous ones included, and every future island/interior appears automatically as data.** **R3 (CURSOR, cosmetic but removes a risk class): rename `include/d/d_ext_ww_save_guard.h` → `d_ext_save_guard.h` (+ includes). The auditor tested it clean (0 hits, ASCII and UTF-16) — this is naming drift, not a breach — but a `ww` token in a shipping-source FILENAME can reach the binary via `__FILE__`/assert macros, so the convention should hold.** **R4 (USER RULING, still open from №55): comment-class rule. L-4 empirically confirms comments are inert (the "Arylls" comment compiles away; gate reports Aryll = 0). History's standing proposal: COMMENTS ALLOWED (docs-class), STRING LITERALS GATED — and the gate should scan literals + shipped tree, not raw source, so it stops drowning in noise.** **R5 (hygiene, either lane): gitignore/remove the strays that would commit — `tmp_r00_names.txt`, `tools/_symcrash.*`, `.cursor/`. No WW content; ordinary tidiness.** **R6 (DOCTRINE, record it): any PLAYER-VISIBLE STRING describing WW content must come from DATA, never from source. That makes the L-2 class structurally impossible rather than merely undetectable — the same move as save-neutral stages and the E1 exact-match resolver: prevent by construction, do not detect after the fact.** **CREDIT WHERE DUE (auditor's own note): `R_DL01` is properly neutral — the №79 naming guidance held under pressure.** |
| **№100 CRASH ON THE №97 BUILD — the island arc is RELEASED WHILE STILL IN USE (History verification of the ferried assessment; log 233432 + source):** | **FERRIED HYPOTHESIS (other instance): "release says keep-cache-shared, purge drops it anyway ⇒ purge ignores the refcount." **VERIFIED AND CORRECTED — both that instance AND History's own first read were wrong on this point.** `releaseArcModels` (`d_ext_npc_mount.cpp:1623-1641`) logs `live=N (keep cache — shared)` ONLY while the count is still > 0; the FINAL release (N→0) erases and purges **without logging a release line**. ⇒ the `Ko` pair (`release live=1` then `purged`) is **two releases** — 2→1 logged, 1→0 silent-then-purge — i.e. CORRECT. Same for `Ptubo`, `Vlupy`, `Knob`, `Oyashi`, `Kn`, `WwAlways`. **No keep/purge contradiction exists; the purge is refcount-gated and there are only two call sites, both inside `releaseArcModels`.** **THE REAL BUG, AND IT IS THE CRASH: `Outset` — models cached (`bg:Outset/model.bdl` +2), `№73 retain arc 'Outset' live=1`, then **`№73 purged model cache for arc 'Outset' (models=3 pristine=0)`**. A purge can only happen via last-release or the no-retain-recorded branch ⇒ **SOMETHING RELEASED THE ISLAND'S ARC WHILE THE ISLAND WAS STILL MOUNTED AND DRAWING.** Its J3D data is freed, a `PLAY_SCENE` create (name=11) follows immediately, and the fault address is a HEAP address (not null) — the exact signature of a dangling pointer into freed model data. **Same family as №65/69/73/83/88/89/94: teardown ordering across a transition — here, the exterior released during the stage change to R_DL01.** **CURSOR — THREE ITEMS: (1) LOG THE RELEASE CALLER (one line): include the call site/context in `releaseArcModels`, so "who released Outset, and when" is answered by the next run instead of by inference. (2) DO NOT RELEASE A MOUNT'S ARC WHILE THE MOUNT LIVES: on stage change, the exterior mount must either survive with its retain intact, or be fully destroyed (draw/actor references gone) BEFORE its arc is released — same order rule as №69 (place → release → free). (3) SAFETY ASSERT: refuse + log a purge for an arc that any live mount still references. Cheap, and it converts this whole class from a crash into a log line.** **SECONDARY (noted, likely benign): BG arcs purge with `pristine=0` while NPC arcs show `pristine=N` — BG models do not stash pristine raw copies (those exist for the №50-C BMT re-parse path, which BGs do not use). Asymmetry explained, not a defect; confirm when convenient.** **CONTAINMENT (auditor's note, ratified): the leak did NOT multiply — the interiors work stayed data-side, the compile-time table is still one region/one entry. **AND THEIR FORWARD FLAG IS THE SCHEDULING POINT: №97 wires five more rooms; if each lands as a labelled `MapEntry`, that is up to six WW labels in the exe, most invisible to a flat greplist exactly like "Link's House". ⇒ DO R2 (manifest-driven warp enumeration) BEFORE the remaining five rooms get warp entries.** That ordering is now the cheapest moment and it holds the count at one.** |
| **№101 ENTRY TO WW LAND — "Fado ticket" design (user proposal; TTW-inspired). Feasible; the real decision is the STATE MODEL:** | **USER PROPOSAL: buy a ticket in the ALBW shop → unlocks Fado's locked door in Ordon → the door leads to Outset, where the first cutscene plays and the questline begins; plus a save-menu choice to start in WW or TP; plus a way back.** **VERIFIED PREMISE: TP's Ordon interiors stage `R_SP01` ships rooms 0,1,2,4,5,7 — **rooms 3 and 6 DO NOT EXIST on disc.** Fado's interior was never built, so this fills a hole rather than overwriting anything.** **ALL PIECES ALREADY EXIST: ALBW shop (fork content) for the ticket · mod-flag store for purchased/unlocked · the native stage-change transport (№84-№89) for the crossing · the save-menu entry condition reserved in №95 for the WW start spawn.** **PURITY CONSTRAINTS (№31 + R6): the ticket must be FORK/ALBW content, never a WW item — a WW model in a TP shop is cross-pollination. The door is TP's own; nothing WW-side is needed in Ordon. The crossing itself is fine (a stage change is a boundary, not a mixture). The ticket's visible name should come from DATA and need not name WW at all (a "passage ticket"/"sea chart" reads better AND keeps the exe clean).** **RETURN PATH: make the crossing BIDIRECTIONAL at the same pair — arrive on Outset, and the same passage returns to Fado's doorway. WW has no Ordon door to honour, so this is our own connective tissue; symmetry avoids inventing a second mechanism.** **⇒ THE REAL DECISION IS NOT THE DOOR, IT IS THE STATE MODEL. A mid-game TP save arrives with Master Sword, Midna, wolf form and a full inventory, into a story that opens with a boy who owns nothing. Options: (1) VISIT/PORTAL — keep TP state, WW as a side area: cheap, but cannot carry WW's questline. (2) SEPARATE CAMPAIGN — the WW choice starts a genuine WW playthrough with its own progression: narratively correct, more bookkeeping. (3) HYBRID — **HISTORY RECOMMENDS**: the SAVE-MENU CHOICE is the real WW campaign (primary path, uses the №95 Aryll's-Lookout start), and **FADO'S DOOR IS THE DISCOVERY ENTRANCE that OFFERS to begin that campaign** — the ticket idea intact as the thing players find, without a Master-Sword Link on Outset's dock.** **USER RULING (2026-07-19): option 3 HYBRID adopted, with an amendment — **an overpowered mid-game crossing is FINE and intentional** (WW content as bonus/endgame material for a TP player, which is what a purchased ticket implies), plus an OPTIONAL "HUMBLED" SETTING that starts you with nothing to recollect. **Consequences, all benign: (a) the split falls out naturally — a SAVE-MENU WW start has no TP inventory to strip, so it is humbled BY DEFINITION; the toggle therefore only ever applies to the FADO DOOR crossing. (b) The quest spine gates on OUR flags, not inventory, so every WW beat fires regardless of what Link carries; item grants simply land on top of existing gear (same "credit" pattern as rupees). (c) The only oddity is narrative — Aryll warning it is "too dangerous without something to defend yourself with" (msg 3014-3015) while you wear the Master Sword; harmless in bonus mode, and exactly what the humbled toggle fixes for purists.** **HARD RULE ON "HUMBLED": STASH, NEVER DELETE — TP gear moves into the mod store on crossing and is restored on return; abandoning the crossing loses nothing. Fail-closed doctrine: a mode that eats a hundred-hour save is unforgivable in a way a missing prop never is.** **TIMING: lands with the QUEST SPINE + start spawn (same slot as №95) — "begin the WW questline" is the shared entry condition for all three. Nothing to build now; recorded so the era opens with the design settled.** |
| **№102 "GRANDMA'S ROOM MISSING" = THE RTBL POINTER ARRAY IS GARBAGE (History; log 235433 + byte-diff vs TP):** | **SYMPTOM: entering R_DL01 gives an empty world — and the log shows WHY by omission: `№94 bump gen=5 (enter 'R_DL01')` → all six `room-lane register EXT_BG1..6 → rooms 0..5` (so History's rollout data reads correctly) → `PLAY_SCENE` + `CAMERA` created → **then NOTHING: no `ROOM_SCENE`, no `dScnRoom phase_2`, no `room.dzr` load, no mount, no population.** No crash. Compare the last WORKING run (220615): PLAY_SCENE → register → **ROOM_SCENE** → `dScnRoom phase_2 arc='R00_00' roomNo=0 roomInfo=1` → mount → room. **The regression point is the stage growing 1 → 6 rooms.** **ROOT CAUSE, BYTE-PROVEN: the RTBL POINTER ARRAY holds INVALID OFFSETS. TP `R_SP01` (n=8, table@0xb8) points to clean 12-byte records laid out AFTER the array — `01 00 00 00 | 00 00 01 18 | 01 00 00 00`, `…0119…`, `…011a…` (one incrementing field per room). **OURS (n=6, table@0xe8) points at 0x18/0x20/0x28 — BACKWARD INTO THE DZS CHUNK-HEADER DIRECTORY — so the "records" literally contain chunk tags: `4d554c54` = "MULT", `5243414d` = "RCAM".** ⇒ the play scene resolves NO rooms ⇒ no ROOM_SCENE ⇒ no mount, no population, no doors. **Everything downstream is a symptom of this one table.** **CURSOR FIX: rebuild RTBL as TP lays it out — `[n × u32 pointer][n × 12-byte record]`, pointers = offsets (dzs-relative) to each record, records following the array, not preceding it. TP's three room records are quoted above as the reference shape; the only per-room variation visible is a single incrementing field. **Verify after building by re-reading the file: each RTBL pointer must land past the pointer array and must NOT resolve to ASCII chunk tags.** (A one-line sanity assert — "RTBL pointer resolves inside the chunk directory" ⇒ refuse + log — would have caught this at build time.)** **HISTORY DATA ALIGNED THIS ENTRY: Cursor corrected room 0's PLYR params low byte 0x3F(63)→0x00 (the `plyr-room63` backup). My generated rooms still carried 0x3F, so they are now set to their own room numbers — `R01→…a001` … `R05→…a005` (low 6 bits = room). Backups kept as `.room63-bak`. **Note for the record: TP's own `R_SP01` room-0 file ships 0x3F, so 63 may be a legitimate "inherit" sentinel; Cursor's change is what the working run used, so we follow the proven value rather than the shipped one — worth revisiting if room-number semantics ever matter again.** |
| **№103 WW ITEMS — TP's ITEM ID SPACE IS FULL (256/256); the two-inventory instinct is FORCED, not preference (History; `d_item_data.h`):** | **(A THE ROOM TABLE WAS NOT REHOMING.** Rehoming = covenant work on LABELS IN THE EXE; the RTBL is STAGE-DATA AUTHORSHIP. It broke when the stage grew 1→6 rooms (pointer-offset bug), unrelated to the leak fix. **And the mod ALREADY owns that table** — `STG_00.arc` is served from `files/res/Stage/R_DL01/` as an overlay; nothing was added to the game tree. **HISTORY PROPOSAL: have the engine BUILD `RTBL`/`MULT` IN MEMORY AT LOAD from the manifests (which already declare each room) instead of shipping hand-built binary tables. That removes the offset math that just failed and makes every future room a manifest line — the same "data, not authored bytes" move that fixed the warp labels.** **(B THE DECISIVE FINDING — `d_item_data.h` enumerates ALL 256 ids 0x00-0xFF with ZERO gaps, `0xFF = dItemNo_NONE_e`. **TP's item table is FULL.** ⇒ WW items cannot receive native TP ids without (i) widening the id type u8→u16 (deep: item tables, menus, save data) or (ii) aliasing existing ids (the save-bit collision class again). **⇒ THE USER'S TWO-INVENTORY IDEA IS NOT A UX PREFERENCE — IT IS FORCED BY THE ENGINE.** **(C THE SPLIT (cheaper than it sounds): **EQUIPMENT (Hero's Sword, Hero's Shield) NEEDS NO NEW IDS — make them MODEL SWAPS on TP's existing sword/shield**, precedent = the sumo outfit body-swap already in this fork; combat/blocking/animation keep working because it is still TP's equipment underneath. That covers two of the three imminent items. **USABLE ITEMS (telescope, later sail/baton/grappling hook) get a MOD-SIDE REGISTRY** — our own ids, icons and models, surfaced as a SECOND PAGE in the item menu (the user's D-pad left/right idea). A view over OUR data, never a hack into TP's 256, so it scales to the full WW item set.** **(D BEHAVIOUR COMES FREE BY BORROWING TP MECHANISMS: the TELESCOPE ≈ TP's HAWKEYE (zoom) — already implemented. "TP mechanism, WW content": our id/model/icon, TP's proven behaviour. Same pattern for the rest where an analogue exists.** **(E DEPENDENCY TO CATCH NOW: WW item OWNERSHIP must persist — that is the №81 SAVE-EXTENSION BLOCK, which is not built yet. **Its spec must cover item ownership alongside chests/keys/quest flags.** Widen the design before it is written rather than migrating it later.** **(F) USER AMENDMENT (2026-07-20) - model-swap is NOT sufficient when BEHAVIOUR differs: the MIRROR SHIELD reflects light beams, and TP has no shield-based reflection (its mirror puzzles are world objects). More such cases will appear. **REFINED TAXONOMY, two axes:** (1) TP slot + TP behaviour = MODEL SWAP ONLY (Hero's Sword, Hero's Shield). (2) TP slot + NEW behaviour = SLOT AS VEHICLE + OUR CAPABILITY LAYER (Mirror Shield: keeps TP's shield slot so blocking/equip/animation are free; our record says which shield it is; our code hooks the reflect). (3) No TP slot + TP behaviour = OUR REGISTRY + BORROWED MECHANISM (Telescope = Hawkeye zoom). (4) No TP slot + new behaviour = OUR REGISTRY + NEW CODE (Wind Waker baton, sail). **LOAD-BEARING PRINCIPLE: OUR REGISTRY IS THE SOURCE OF TRUTH; TP's slot is only the VEHICLE** - the same shape as crediting rupees to TP's wallet while the visible object stays WW's. **Therefore the registry needs CAPABILITIES, not just ids: {our_id, model, icon, slot (TP equipment slot or none), capabilities[reflect|zoom|wind|sail|...]}. That lets a shield be a shield PLUS something without asking TP's full 256-id table for a slot it does not have.** **SAVE CONSEQUENCE (sharpens E): the extension block cannot store mere 'owns item N' booleans - it must record WHICH WW items are owned AND equipped, because two shields sharing one TP slot are distinguishable only in our data. Widen that spec before it is written.** **TIMING: sword + shield + telescope are the first three obtainables (Grandma's shield, Aryll's telescope, the training sword), so this lands with the QUEST SPINE — the same slot as №95/№101. Decision recorded now so the spine builds toward it.** |
| **No.104 TWO PRECISE FAULTS (History; log 001209) - rooms 1-5 are INVISIBLE not broken, and exterior re-entry skips population:** | **GOOD NEWS FIRST: Grandma's (room 0) enter+exit WORKS, and STURGEON'S ROOM IS NOT BROKEN - the log shows it fully loading: `dScnRoom phase_2 arc='R02_00' roomNo=2 roomInfo=1` -> `room2 mount 'EXT_BG3'` -> `BG COMPLEATE EXT_BG3 host=(0,0,0) anchor=(0,0,0)` -> **population spawns correctly**: `Aj1` (Sturgeon) at (-8.5,-20,106.9), his Plants, his Otana shelves. **The content is present at the right WW coordinates.** **FAULT 1 - ROOMS != 0 ARE NEVER ACTIVATED (no draw, no collision). Room 0 works because it is the default active room; rooms 1-5 mount and populate but TP never marks them visible/solid, so the player sees a black void and falls (user screenshot: `Room: 1`, y=14625 falling, velocity -52.8). **CURSOR: on entering room N, set the room's active/visible state through TP's room control (room switch / status flags / `dComIfGp_roomControl`) exactly as a native room change would - the same symmetric bookkeeping `daBg_c` clears on teardown (No.70). Acceptance: enter Sturgeon's and SEE the study with a floor.** **FAULT 2 - RETURNING TO THE EXTERIOR SKIPS POPULATION. After `bump gen=8 (enter 'F_SP115')` -> `dScnRoom phase_2 roomNo=0 stage='F_SP115'` -> `room0 mount 'EXT_BG0'` -> `BG COMPLEATE EXT_BG0`, **the only spawn logged is the exit knob (`src=door:exit:ojhous`) - ZERO census rows.** The generation bumped, so the No.94 generation check is simply not covering this path (exterior re-entry after a stage change). **CURSOR: the population pass must re-run for a BG whose generation differs from the one it was marked at, on EVERY mount COMPLEATE - including the exterior's. The No.94 self-heal net (BG complete + census rows > 0 + zero spawns => re-run + log) would also catch this; confirm it is armed on this path.** **Both faults are bookkeeping across a transition - the same family as No.65/69/73/83/88/89/94/100. Neither is a data problem: History's room arcs, manifests and census are all verified correct in this very log.** |
| **№105 CRAWL UNUSABLE AFTER HOLD-A RETIREMENT — research + plan (Cursor; user report; NO CODE THIS PASS):** | **USER: crawl became unusable after earlier crawl edits; suspects roll conflict, notes roll should require movement.** **WHAT WE SHIPPED THEN: free-ground hold-A crawl (`game.enableHoldACrawl`, default ON) was retired because it stole A whenever DoStatus looked empty and raced door/talk prompts. Comment claimed native `BUTTON_STATUS_ENTER` from crawl holes / wall codes remained. UI option removed; config key kept unused.** **SOURCE FINDINGS (not a roll bug as primary): (1) `setDoStatus(BUTTON_STATUS_ENTER)` appears once in `d_a_alink.cpp` (~9296) — only on wall_code==6 under water/swim flags (ladder enter path). Crawl start on ENTER (`checkNormalAction` ~12328 → `procCrawlStartInit`) therefore only fires if something else set ENTER; crawl holes are NOT that path in this tree. (2) Front roll is `BUTTON_STATUS_UNK_121` when stick > `getFrontRollRate()` (~0.75) → `checkMoveDoAction` → `procFrontRollInit`. Standing still (stick ≤ rate) does not arm roll — user instinct correct; "A does nothing standing / A rolls when pushing stick" can feel like a conflict when crawl never arms. (3) Order: `decideDoStatus` → `checkNormalAction` (ENTER/crawl) → later `checkMoveDoAction` (121/roll). No mutual exclusion bug if ENTER were set; ENTER simply is not set at Outset holes. (4) `adapt_dzb` (`adapt_bdl_arcs.py`) only clears TP through-cluster bits (`~0x00FFC000`) — History's W1 note "WW attr → TP wall-code in adapt_dzb" is **not implemented**; no crawl wall-code remap exists. (5) Hold-A stay-down while crawling was also removed — once started, vanilla stand-up-on-clearance returns.** **WHY IT FELT FINE BEFORE: hold-A free-ground was the practical crawl entry on WW spaces (and open ground), papering over missing hole DoStatus. Retirement removed the paper without a native replacement.** **PLAN (ordered, still research-gated — do not code until user GO): P0 Probe — at LinkUG crawlspace / known Outset crawl hole, log per A-press: DoStatus, stick value, wall_code from front linchk, room; confirm ENTER never appears. P1 Preferred (TP-native): History names WW crawl poly attrs + TP wall-code that should set crawl Enter (or equivalent); Cursor extends `adapt_dzb` (or a dedicated pass) so Outset holes arm a real prompt; keep roll untouched. P2 Interim (if P1 data lag): restore **gated** hold-A crawl — only when `DoStatus==NONE` AND stick ≤ frontRollRate AND NOT door/talk attention within range AND ground hit; never steal A from OPEN/SPEAK/ENTER. Optionally restore hold-A stay-down only inside low clearance. P3 Acceptance: stand at hole → Enter/A → crawl in; stick-forward roll still works away from holes; door A and talk A unaffected.** **Division: History owns attr→wall-code truth; Cursor owns alink gating + adapt tool once values are named.** |
| **No.105 ROOMS 1-5 BLACK/NO-FLOOR = WW COLLISION CLAIMS WW'S OWN ROOM NUMBER (History; source + data, FIXED this entry):** | **STATE: no crashes; all six rooms exist WITH their actors and NPCs; Ordon/Outset keep their inhabitants across warps (No.104 fault 2 FIXED). Remaining: only room 0 (Grandma's) renders mesh + collision; rooms 1-5 are black with no floor although their props/NPCs are visible.** **ROOT CAUSE, SOURCE-VERIFIED: TP reads a polygon's room from THE COLLISION FILE ITSELF - `cBgD_Grp_t.m_room_id` (`d_bg_w.h:92`), walked parent->parent in `d_bg_w.cpp:894` (`GetGrpRoomIndex`). It is DATA, not a setter. **WW dzb files carry WW's OWN room numbering, which does not match our TP room assignment:** LinkRM claimed room 0 (= our room 0, so it WORKED); **Ojhous2R1 claimed room 1 while mounted in TP room 2; Onobuta/Ojhous2/Omasao/LinkUG claimed room 0 while mounted in rooms 5/3/4/1.** Mismatch => TP's room-filtered collision rejects the polys => Link falls. **This also explains why room 0 was the ONLY one that ever worked - coincidence of numbering, not correctness.** **HISTORY FIX APPLIED (data, no rebuild): each interior's dzb group tree re-tagged to the TP room it is mounted into - LinkUG->1, Ojhous2R1->2, Ojhous2->3, Omasao->4, Onobuta->5 (LinkRM already 0). The 0xFFFF 'no room' sentinel groups are left untouched. Backups `.roomid-bak` per arc. **Prediction: rooms 1-5 gain their floors immediately on the next run.** **REMAINING FOR CURSOR (mesh draw, if it does not clear with the collision fix): the room's MESH is drawn by our BG mount, not by TP's `daBg_c`. If rooms 1-5 still render black once collision works, the mount's draw is being culled by room - check that the BG mount actor is created in ITS room (the log already shows `room2 mount 'EXT_BG3'`, so the actor room looks right) and that the room's status flag/visibility is set as `daBg_c` does (`onStatusFlag(roomNo, 0x10)` mirror - No.70). Note props/NPCs ALREADY draw in rooms 1-5, so per-actor room culling is working; only the BG model lags.** **GENERAL RULE WORTH KEEPING: WW room data is authored for WW's OWN room numbering. Any WW space mounted into a TP room slot must have its collision room ids re-tagged to match. Fold this into the adaptation pipeline (a flag on adapt_bdl_arcs) rather than patching per arc, once the room map is stable.** |
| **No.106 M5b PASSED + interior lighting explained (History) + commit order posted:** | **MILESTONE: M5b FOLDER-ABSENT SMOKE PASSED - validly run for the first time (F-2 had shown rename != disable, so it had never actually been performed). The containment lane's oldest open item is CLOSED. All six interiors are also in with their actors/NPCs.** **INTERIOR LIGHTING ODDITIES EXPLAINED (user: Link fully black in one room, others over-lit; Rose's room dark/red) - THIS IS HISTORY'S DATA, and it was never designed: (a) Cursor's stage shell carries STUB environment chunks (`Env0/Col0/PAL0/VRB0` n=1 each, No.96); (b) **every room skeleton History authored copied its `FILI` room-info block VERBATIM from TP's Bo's House (`R_SP01/R00`)**, so all six rooms request Bo's-House light/env indices against placeholder palettes. Arbitrary per-room results are exactly what that produces. **FIX = the same work as Outset's: author real env/palette data and give each room a correct FILI. Deferred by the user ('we can tweak those later'), and it folds naturally into the relocation lighting pass (No.96) rather than being separate work.** **COMMIT ORDER POSTED TO THE BUS (user-approved 1-5): rename `d_ext_ww_save_guard.h` FIRST (still untracked - free now, permanent in history after the first commit), add the untracked engine sources (54 tracked files include them), add tools+docs incl. `greplist.txt`, exclude strays, and GATE THE BUILT EXE before pushing. Open user call: whether `tools/.../dialogue/outset_quests.txt` (game quest text, WW place-name filename) may commit under the config-text exemption - History recommends NO, keep seed dialogue mod-side.** |
| **do-not** | Commit WW arcs; WW files in game tree; WW names in exe; **share ANY exe that hasn't passed the gate today** |
| **queued** | **№105 crawl restore (research done — see ledger);** Mesa TGDR door; quest-spine polish; M5b folder-absent; M6 greplist gate; identity pass |
| **crawl** | **BROKEN after hold-A retirement (№105).** Assumed native `BUTTON_STATUS_ENTER` covers crawl holes — **false**: ENTER is only set for wall_code==6 (ladder path). `adapt_dzb` clears through-bits only — **no WW→TP crawl wall-code remap**. Free-ground hold-A was the real entry; race with door/talk was real; roll is NOT the primary conflict (roll needs stick > ~0.75). Plan in №105 — no code yet. |
| **workorder** | **W1-COMPLETION** — acceptance after Outset whole through pirate-departure offer. |
| **rupees** | **FINAL (№31+№36 C):** WW visual `Vlupy.arc/vlupy.bdl` + brk/btk frame; pickup grants TP wallet item. №35 H4 “TP pickup carve-out” for visuals is **rescinded**. |
| **folder** | Live mod = `WW-Crew-Restoration` (51 arcs; skeleton bak = `.SKELETON_BAK`). №37 alert about `.DISABLED` is stale. |
| **updated** | 2026-07-20 (№106 local commit `dce09e87ca`; no push — ask first) |

---

## Tip

**Note:** flag store migrated v1→v2; existing mod flags in a save are reset once.

**Seed folder content:** `python tools/ww_crew_restoration_skeleton/seed_o2_content.py`

**Playtest path:** Warp → Outset Island → wait for `[ExtNpcPop]` → A at door (not walk-in) → expect `[Doors] enter EXT_BG1 → EXT_BG1`. Link's house should spawn Grandma (`Ba1`). Confirm no Tetra/Helmaroc until flags. Confirm log has no mismatched audio twin resident (REFUSED if Linkle enabled).
